# 03 — 실험 착수 절차 (장기기억)

> **왜 이 문서가 있나**: 같은 계열 사고가 **반복**됐다.
> 미구현 상태로 배치를 놓은 것이 **2회**(P031 `--infer-repeat`, P022 `--fp8`),
> "도구가 의도한 대상이 아닌 것을 쟀다" 가 **4회**(결과 018 §4 / 019 §2 / 023 §2 / 023 §7).
> 규범은 이미 `CLAUDE.md` 에 있었다 — **강제되지 않았을 뿐이다.** 이 문서가 절차로 만든다.

---

## 1. ★배치를 만들기 전 — 구현 게이트

**최상위 `.bat` 은 문서가 아니라 실행 가능한 물건이다.** 거기 있으면 사용자는 돌려도 되는
것으로 읽는다. 실제로 두 번 그렇게 됐다.

| # | 확인 | 방법 |
|---|---|---|
| 1 | 계획이 요구하는 **플래그가 파서에 있는가** | `python scripts/check_batch_flags.py` |
| 2 | 그 플래그가 **실제로 연결돼 있는가**(파서 → 함수 → 동작) | 코드 경로를 **눈으로 따라간다**. ★1번은 이걸 못 본다 |
| 3 | 계획이 요구하는 **동작이 구현돼 있는가** | 계획서 §측정·판정 항목을 한 줄씩 대조 |
| 4 | 코드를 고쳤으면 **스모크** | `run_smoke_check.bat` (`[VERIFY]` 줄만 읽는다) |
| 5 | **린트** | `python scripts/lint_bat.py --fix` |

> **2번이 왜 따로 있나**: 2026-08-06 에 `--doc-filter` 가 `train` 파서에 **있었는데**
> `trainer.train()` → `prepare()` 로 **전달되지 않았다.** 필터 캐시를 만들 수는 있어도
> **그것으로 학습할 방법이 없었다.** 1번 검사는 통과했을 것이다.
>
> **구현이 선결이면 배치를 만들지 않는다. 계획서에만 설계를 남긴다.**
> 불가피하면 헤더 첫 줄에 선결 조건 + `:NOTIMPL` 가드를 넣는다(그러면 1번이 경고로 낮춘다).

---

## 2. ★실험 간 독립성 (사용자 요청, 2026-08-06)

한 실험이 다른 실험을 **막거나 오염시키지 않게** 설계한다. 이유는 두 가지다 —
① 하나가 실패해도 나머지가 살아야 하고 ② **AI 사용량이 소진된 동안 사용자가 긴 학습을
혼자 돌릴 수 있어야** 한다.

| 축 | 규칙 |
|---|---|
| **체크포인트** | 항상 `--tag` 를 붙인다. 정본 `dense`/`tied` 를 덮어쓰지 않는다 |
| **데이터 캐시** | 내용이 달라지면 **디렉터리를 분리**한다(`_filtered` 방식). 기존 캐시 무변 |
| **플래그 기본값** | 새 기능은 **기본 off**. 켜지 않으면 **비트 동일**이어야 한다 |
| **속도 측정** | 다른 실험과 **동시 실행 금지**. 직전 5분 유휴 |
| **선행 의존** | 있으면 배치 헤더에 **명시**하고 `goto ERROR` 로 막는다. 없으면 `[WARN] continuing` |

### 2.1 "무인 실행" 배치의 조건

사용자가 시작만 하고 자리를 뜰 수 있으려면:

- [ ] **중간에 판단이 필요 없다**(분기·확인 프롬프트 없음)
- [ ] **선결 조건을 헤더에서 검사하고 없으면 즉시 멈춘다**(GPU 를 태우기 전에)
- [ ] **예상 소요 시간이 헤더에 있다**
- [ ] **판정 기준이 꼬리말에 있다** — 로그만 받아도 다음 세션이 문서를 쓸 수 있게
- [ ] **다른 실험의 산출물을 건드리지 않는다**

### 2.2 우선순위를 매길 때

| 우선 | 성격 | 왜 |
|---|---|---|
| 1 | **GPU 0 · 수분** | 값싼 진단이 비싼 학습의 게이트다. 먼저 돌려서 설계를 고친다 |
| 2 | **구현 선결이 없는 긴 GPU 런** | AI 사용량과 무관하게 굴릴 수 있다 → **대기시간을 자산으로** |
| 3 | 구현이 필요한 것 | AI 가 있을 때만 진행 |

---

## 3. 실험 한 사이클

```
exp-preflight  (조건 정합성 — docs/EXPERIMENT_BASELINES.md 와 대조)
      +
exp-plan       (계획서 양식 — ★예측 절과 한계 절을 반드시 쓴다)
      ↓
구현 게이트 (§1)
      ↓
run-batch      (.bat 작성 → lint_bat.py + check_batch_flags.py)
      ↓
[사용자가 실행]
      ↓
log-to-result  (결과문서 → ★계획서 본문 갱신 → 연쇄 갱신)
      ↓
핸드오프 (ai_dev_tool/02)
```

**`exp-plan` 과 `exp-preflight` 는 대체 관계가 아니다.** 전자는 문서 양식·절차,
후자는 실험 조건의 정합성. 새 계획을 쓸 때는 **둘 다** 돈다.

---

## 4. 결과를 읽기 전에 예측을 적는다

계획서와 배치 헤더 **양쪽에** 예측을 적는다. 이유:

- 예측이 없으면 **어떤 결과든 "예상대로" 로 읽힌다**
- 예측이 있으면 **틀렸을 때 그것 자체가 결과가 된다**(가설 기각도 성과다)
- 배치 헤더에 적으면 **사용자가 로그를 볼 때 이미 기준을 갖고 본다**

그리고 결과문서에서 **예측 대조 절을 명시적으로** 쓴다. 계획서에 예측을 써 놓고
대조하지 않으면 쓴 의미가 없다.

### 4.1 대조군을 반드시 하나 넣는다

**"아무것도 안 변해야 하는 조건"** 을 같은 배치에 넣는다. 그게 계측 결함을 잡는다.

- P034 단계3C: `p6d`(공유 없음)가 **안 변해야** 한다 → 변하면 환경이 변한 것
- P037 단계2C: `ko-en` 대조군이 **완전 불변**이어야 스코핑이 맞다
- 속도 배치: 기준선 조건을 같은 로그에 넣어 **비율로 읽는다**(열·부하가 상쇄된다)

---

## 5. ★배치와 계획서는 **함께** 만든다 (2026-08-07 추가)

2026-08-06 에 **P039·P040 의 배치를 만들면서 계획서를 안 썼다.**
`실험계획목록.md` 에 한 줄만 등록돼 있었고, 다음 세션이 그걸 발견했다.

> **왜 문제인가**: 계획서가 없으면 **예측 절이 없다.** 예측이 없으면
> **어떤 결과든 "예상대로" 로 읽힌다.** 실제로 P039 는 계획서를 뒤늦게 쓰면서
> **예측 4개 중 2개가 빗나갔다는 것**이 드러났다(빈도0 비율, 노름 부호).
> 배치 헤더에만 적으면 **결과문서를 쓸 때 대조할 대상이 남지 않는다.**

**절차에 추가**: §1 의 구현 게이트를 통과했으면, **배치를 만들기 전에**

- [ ] `test_plan/P0NN_{요약}.md` 가 **있는가**. 없으면 **먼저 쓴다**(`exp-plan` 스킬)
- [ ] 그 문서에 **§예측** 과 **§한계** 가 있는가
- [ ] `실험계획목록.md` 에 **파일 링크**가 있는가(한 줄 요약만 있으면 안 된다)

**점검 한 줄**:

```
python -c "import re,pathlib; L=pathlib.Path('test_plan/실험계획목록.md').read_text(encoding='utf-8'); \
import glob,os; have={os.path.basename(f).split('_')[0] for f in glob.glob('test_plan/P*.md')}; \
want=set(re.findall(r'\*\*(P\d+[A-Z]?)', L)); print('계획서 없는 항목:', sorted(want-have))"
```

---

## ★2026-08-07 이관 — CLAUDE.md 에서 내린 배치 규약 전문

- **★★최상위 실험 배치는 "지금 돌아갈 때"만 만든다 — 2회 재발.** 구현이 선결이면 배치를 만들지 말고
  **계획서에만 설계를 남긴다.** 최상위 `.bat` 은 문서가 아니라 실행 가능한 물건이라, 거기 있으면
  돌려도 되는 것으로 읽힌다. `run_P031_repeat.bat`(`--infer-repeat` 미구현, 2026-07-31) ·
  `run_P022_stage1_fp8.bat`(`--fp8` 미구현, 2026-08-06) 둘 다 사용자가 실행했다.
  **가드가 있어 GPU 는 안 태웠지만 사용자 시간을 쓴 것은 사실**이고, 가드는 작성자가
  기억했을 때만 붙는 장치다. → **배치를 만든 직후 반드시:**
  ```
  python scripts/check_batch_flags.py     # 플래그가 파서에 있는가
  python scripts/lint_bat.py --fix        # 문법·CRLF·ASCII
  ```
  ⚠️ **`check_batch_flags.py` 는 이름만 본다.** 파서에 있는데 **전달이 안 되는** 결함은 못 잡는다
  (2026-08-06 `--doc-filter` 가 그랬다: 캐시를 만들 수는 있는데 그걸로 학습할 방법이 없었다).
  **파서 → 함수 → 동작 경로를 눈으로 따라가고, 코드를 고쳤으면 `run_smoke_check.bat`.**
  절차 전체는 [`ai_dev_tool/03_실험착수_절차.md`](ai_dev_tool/03_실험착수_절차.md) §1.
- **★실험은 서로 독립이게 설계한다.** 태그로 체크포인트 분리 / 캐시 내용이 달라지면 디렉터리 분리 /
  새 플래그는 **기본 off·비트 동일** / 선행 의존이 있으면 헤더에 명시하고 `goto ERROR`.
  **이유는 둘이다** — 하나가 실패해도 나머지가 살아야 하고, **AI 사용량이 소진된 동안 사용자가
  긴 학습을 혼자 돌릴 수 있어야** 한다. 무인 실행 배치의 조건은 `ai_dev_tool/03` §2.1.

---

## ★§6 신설(2026-08-07 사용자 지시) — **큐는 `call` 만 한다**

> *"night queue 와 같이 돌려놓고 자도 되는 배치파일 작성 요청할 때 **개별 실험 배치파일을
> 만들고 그 개별 실험 배치파일을 불러오는 식으로** 구현바람. 모든 실험을 night queue 배치파일
> 안에 넣어서 각 개별실험에 대한 **완료여부 판정이 난해**함."*

### 규칙

| # | 규칙 |
|---|---|
| 1 | **실험 하나 = 배치 하나.** 큐 안에 학습·진단 명령을 **직접 쓰지 않는다** |
| 2 | 큐는 `call run_P0NN_....bat` **와 `if errorlevel 1 echo [WARN] ... - continuing` 만** 담는다 |
| 3 | 각 배치는 **자기 선결조건·예측·판정 안내·재현 명령**을 자기 헤더에 갖는다 |
| 4 | 그래야 **개별 `-done` 판정**이 가능하고, 실패한 하나만 다시 돌릴 수 있다 |
| 5 | 큐 자체는 **완료되면 삭제 대상**이다 — 실험 자산이 아니라 스케줄러다 |

### ★§6.1 보강(2026-08-07 사용자 지시) — **순서를 하드코딩하지 않는다**

> *"사용자가 간단히 원하는 순서대로 실험 배치파일 작동할 수 있는 배치파일 도구 만들어줄 수 있는지?
> 자기전에 사용자가 직접 적절하게 실험 묶음 구성해서 돌리고 잘 수 있는 도구 필요함."*

`run_night_queue_v4.bat` 는 **작성 시점에 순서가 박혀 있었다.** 그래서 계획을 바꾸려면
**잠자리에서 배치 파일을 편집**해야 했다 — 그건 규약 위반은 아니지만 **쓸 수 없는 도구**다.

**정본 = `run_queue.bat`(대화형·영구 보존).** 메뉴를 찍고 슬롯을 8개까지 물어본 뒤
**확인을 한 번 받고 그 뒤로는 무인**이다. 규칙 1~5 는 그대로 지킨다(`call` 만 한다).

| 규칙 | 적용 |
|---|---|
| 6 | **순서는 실행 시점에 사용자가 정한다.** 배치에 박지 않는다 |
| 7 | 새 실험 배치를 만들면 **`run_queue.bat` 의 메뉴와 `:RESOLVE` 에 한 항목 추가**한다 |
| 8 | 파일이 없는 id 는 **에러가 아니라 `[SKIP]`** — `-done` 으로 이름이 바뀐 실험이 큐를 깨지 않는다 |

⚠️ **`%` 금지 규약 때문에 `for` 루프도 `%1` 도 못 쓴다** → 슬롯은 **번호 변수**(`TL_J1`~`TL_J8`)이고
분배는 서브루틴(`:RESOLVE`)의 `if` 사슬이다. **반복적으로 보이는 것은 제약의 결과지 부주의가 아니다.**

⚠️ **`if COND set A=x& set B=y` 를 쓰지 않는다.** cmd 는 `&` 를 **파서 수준에서** 분리하므로
두 번째 `set` 이 **조건과 무관하게 실행**된다. 괄호 블록을 쓴다(`run_queue.bat` `:RESOLVE` 주석 참조).

### 왜 (2026-08-07 야간큐 v3 에서 실제로 걸렸다)

야간큐 v3 의 **[2] P014C 단계1 을 큐 안에 인라인**으로 넣었다(`python scripts\\check_fused_int8.py`).
그래서 그 실험만 **독립 배치가 없고**, `-done` 을 붙일 대상도 없고, 재실행하려면
**큐 전체를 열어 그 두 줄을 찾아야** 한다. 나머지 둘(P047·P048-2)은 `call` 이라 문제가 없었다.

★**그리고 큐를 지우면 그 실험의 명령이 사라진다.** 그래서 규칙 3(재현 명령을 배치 헤더에)과
**결과문서 §재현 명령**이 이중 보존 장치다 — 큐 삭제 전에 반드시 확인한다.

