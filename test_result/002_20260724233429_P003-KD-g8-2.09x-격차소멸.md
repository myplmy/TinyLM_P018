# 실험 002 — P003: KD + g=8 (더 공격적 타잉)

- 작성: 2026-07-24
- 실험계획: **P003** (KD + g 스윕)
- 아키텍처: m100(v5), mlp_group=8(중간16→2그룹), cla2. dense 교사 재사용.
- 설정: ko-en 300M, 2289스텝, mbs8 seq1024 accum16, lr 1e-3, compile. KD는 --no-ckpt 제외.

## 결과 (vs dense final 3.8241)

| 조건 | final val | 격차 | 파라미터 | 메모리 | 감축 | \\|g\\|max | 시간 |
|---|---|---|---|---|---|---|---|
| t_g8 (g8, KD 없음) | 3.9520 | +0.1280 | 63.5M | 14.8MB | **2.09×** | 1.4 | 89.9분 |
| **t_kd_g8 (g8+KD+init)** | **3.8292** | **+0.0052** | 63.5M | 14.8MB | **2.09×** | 0.5 | 165.2분 |

## 핵심 결론
- **KD가 g=8 격차도 닫는다.** g8 raw 격차 +0.128 → KD+부모초기화로 **+0.0052(소멸)**.
  즉 **2.09× 감축(14.8MB)에서 dense 품질에 도달** — P002의 1.82×(t_kdinit)보다 더 공격적 타잉 성공.
- **새 최적점: t_kd_g8** (2.09×, 격차 ~0). 프로젝트 메모리 목표를 크게 전진.
- t_kd_g8 vs t_all: t_all이 val 0.0105 낮으나(노이즈 이내) **파라미터 1.22×·메모리 18MB**로 더 큼.
  → **t_kd_g8이 메모리에서 우위(2.09× vs 1.72×)**, 사실상 동급 품질. LoRA·FiLM은 KD 있으면 여전히 불필요.
- |g|: t_g8 1.4 → t_kd_g8 0.5. **KD+부모초기화가 안정화**(좋은 출발점).

## KD 학습시간 (~2×) 재분석
- t_kd_g8 165분 vs t_g8 90분 = **1.84×**. 교사 컴파일은 적용됨(eager였다면 더 느림).
- 원인은 **본질적**: KD는 매 micro-step **dense 교사(132.5M) 풀 forward**를 추가하는데, 교사가
  tied 학생(63.5M)보다 **더 큼** → 교사 forward ≈ 학생 fwd+bwd. 컴파일은 eager 오버헤드만 제거.
- **근본 해법 후보: 오프라인 교사 로짓 캐싱**(교사를 데이터에 1회만 돌려 top-k soft target 저장 →
  모든 KD 학생이 캐시 재사용, 학습 중 교사 forward 0). 매트릭스 전체에 교사비용 1회로 상각.

## 추가: t_kd_g16 — 극단 타잉(16층 1MLP 공유) (2026-07-25)

| 조건 | final val | best | 격차(vs dense 3.8241) | 파라미터 | 메모리 | 감축 | \\|g\\|max | 시간 |
|---|---|---|---|---|---|---|---|---|
| **t_kd_g16** | 3.8420 | 3.8044@2289 | **+0.0180** | 58.8M | 13.7MB | **2.25×** | 0.6 | 167.1분 |
| (참고) t_kd_g8 | 3.8292 | — | +0.0052 | 63.5M | 14.8MB | 2.09× | 0.5 | 165.2분 |

- **극단 타잉에서도 KD가 격차를 임계 이내로 유지.** g16(중간 16층이 MLP 1개 공유)에서 raw 격차는
  크지만 KD+부모초기화로 **+0.018(판정 "작음" ≤0.07 성립)**. **새 최공격 지점: 2.25×(13.7MB)**.
- g8 대비: 격차 +0.0128 더 벌어지나 메모리 1.08× 더 작음. **품질-메모리 트레이드오프의 프런티어 확장.**
  (t_kd_g8 = 안전한 최적, t_kd_g16 = 더 공격적 옵션. 손실 0.013을 1.08× 메모리로 사는 셈.)
- 학습 로그 특징: val-train gap 초기 +2.4 → 종료 +1.79(교사 지식 흡수). |g| 0.6 안정. skip 0.
- 학습시간 167분(≈KD 2×, 본질적 — 교사 dense forward). P017/P018로 이 부분 단축 시도.
- **비고(중요):** 여기 "dense"는 full-precision이 아니라 **타잉·CLA만 끈 삼진(TLinear) 기준선**
  (`dense_baseline`). 즉 격차는 "삼진·비타잉 vs 삼진·타잉"의 타잉 비용이지 양자화 비용이 아니다.

## 다음
- 오프라인 KD 캐싱(P015 종결→hidden 정확캐시 P019), KD 가속(P017 skip-forward/P018 압축교사),
  어텐션 타잉(KD로 가능해지는가), 3:4 희소(P016)로 g16 위에 1.25bpw 누적.
