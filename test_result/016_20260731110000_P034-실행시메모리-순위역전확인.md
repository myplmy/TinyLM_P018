# 016 — P034 단계1 실행시(상주) 메모리 : **순위 역전 확인. 저장 MB 로 고른 승자는 상주 기준으로 틀린 선택이었다**

- **계획**: [P034](../test_plan/P034_실행시메모리-실측및패킹추론경로.md) 단계 1
- **로그**: `016_log_20260731_P034-test-logs.txt`
- **배치**: `run_P034_memory.bat` → `scripts/mem_runtime.py`
- **조건**: `max_new=32`, batch 1, CPU 와 CUDA 양쪽. **학습 없음.** `runs/` 미기록
- **환경**: AMD Ryzen 7 9700X(L3 32MB) / psutil 7.2.2

> **한 줄**: 상주 메모리는 **bpw 와 무관하고 유니크 삼진 파라미터 수에만 비례**한다.
> 계획 §1.1 이 세운 검정 가능한 예측이 **그대로 맞았다.**

---

## 1. 측정 결과

**CPU 와 CUDA 의 텐서합산 값은 완전히 동일**하다(같은 모델이므로 당연 — 계측 일관성 확인).

| 모델 | 삼진 유니크 | **저장**(이론·패킹) | **상주**(텐서합산) | latent | dequant `_wq` | 기타(fp32 emb 등) | 저장 대비 |
|---|---|---|---|---|---|---|---|
| `p6d` dense | 123.86M | 30.9 MB | **978.0 MB** | 472.5 | 472.5 | 33.0 | **31.6×** |
| `mC_g8_k4` | **54.85M** | 14.9 MB | **451.5 MB** | 209.2 | 209.2 | 33.0 | 30.4× |
| `mA_g4s34_k4` | 64.29M | **11.7 MB** | **523.5 MB** | 245.2 | 245.2 | 33.0 | **44.8×** |

### 보조 계측(그대로 믿으면 안 되는 값들)

| 모델 | RSS Δ (CPU) | RSS Δ (CUDA 런) | **CUDA 피크** |
|---|---|---|---|
| `p6d` | +3000.2 MB | +957.0 MB | 2973.3 MB |
| `mC_g8_k4` | **−35.1 MB** | +55.4 MB | 1403.6 MB |
| `mA_g4s34_k4` | +490.1 MB | +34.6 MB | 1619.9 MB |

> **RSS 증가분은 이 실행 형태에서 무의미하다.** 세 모델을 한 프로세스에서 **순차 로드**하므로
> 앞 모델이 해제한 힙을 뒤 모델이 재사용한다 — `mC` 가 **−35.1MB** 로 나온 것이 그 증거다.
> 첫 모델(`p6d`)의 +3000.2 만 부분적으로 의미가 있고, 그마저 할당자 오버헤드를 포함한다.
> **판정에 쓸 값은 텐서합산이고, 교차검증은 CUDA 피크로 한다.**

---

## 2. ★핵심 발견

### (1) 순위 역전 — 예측 그대로

```
저장 작은 순 :  mA(11.7)  <  mC(14.9)  <  p6d(30.9)
상주 작은 순 :  mC(451.5) <  mA(523.5) <  p6d(978.0)
```

**`mA` 는 저장이 가장 작은데 상주는 `mC` 보다 16% 크다.**
CUDA 피크(mC 1403.6 < mA 1619.9)도 **같은 순서**를 낸다 — 두 독립 계측이 일치한다.

### (2) 기전 — 상주는 bpw 를 전혀 보지 않는다

측정값이 다음 식과 **소수점까지 맞는다**:

```
상주(MB) = 삼진유니크파라미터 × 4B × 2벌(latent + dequant사본) + 33.0(fp32 임베딩·norm)
```

| 모델 | 계산 | 실측 |
|---|---|---|
| `p6d` | 123.86M × 8B = 945.0 + 33.0 = **978.0** | 978.0 ✅ |
| `mC_g8_k4` | 54.85M × 8B = 418.5 + 33.0 = **451.5** | 451.5 ✅ |
| `mA_g4s34_k4` | 64.29M × 8B = 490.5 + 33.0 = **523.5** | 523.5 ✅ |

`bpw`(1.95 vs 1.25)가 **식에 등장하지 않는다.** 3:4 희소는 **저장만** 줄인다.

| 레버 | 저장 | 상주 | 전이율 |
|---|---|---|---|
| **MLP 타잉**(g4→g8) | ↓ | **↓** | **108%** (`mC`) |
| **3:4 희소**(1.95→1.25bpw) | ↓ | **0** | **53%** (`mA`, 타잉 g4 분만 전이) |

> 전이율 = (상주감축−1)/(저장감축−1). `mC` 의 108% 는 고정항(33MB)이 상주에서 차지하는 비중이
> 저장에서보다 작기 때문이며, **초과 이득이 아니라 회계 효과**다. 유니크 파라미터 비(2.258×)가
> 상한이고 `mC` 의 상주감축 2.17× 는 그 아래에 있다.

### (3) ★계측 함정 — **CUDA 피크는 배포 메모리가 아니다** (신규 발견)

`mem_runtime.py` 는 `reset_peak_memory_stats()` 를 **`load_model()` 앞에** 두므로
피크에 `torch.load` 가 끌어온 것이 전부 들어간다. 그런데 우리 체크포인트는 **옵티마이저
상태를 포함**한다(`trainer._do_eval` 이 `{"model", "opt", "step", "cfg"}` 를 저장):

| 파일 | 실제 크기 | = 모델 크기 × ? |
|---|---|---|
| `m100_ko-en_300M_p6d.pt` | 1,516.8 MB | 505.5 × **3** (model + AdamW `exp_avg` + `exp_avg_sq`) |
| `m100_ko-en_300M_mA_g4s34_k4.pt` | 835.0 MB | 278.3 × **3** |
| `m100_ko-en_300M_mC_g8_k4.pt` | 726.9 MB | 242.3 × **3** |

`p6d` 피크 분해: 체크포인트 1516.8 + 모델 505.5 + `_wq` 472.5 = 2494.8,
잔여 ≈ 478 은 `ternary()` 가 만드는 **전체 크기 중간 텐서**(`abs`·`mask` 등)로 설명된다.
**합계가 실측 2973.3 과 맞는다.**

> **배포는 옵티마이저 상태를 로드하지 않는다.** 따라서 **CUDA 피크는 배포 상주량을 약 3배
> 과대 보고**한다. 이번 결론은 텐서합산으로 냈으므로 영향이 없고, **두 계측이 같은 순서를
> 주므로 역전 판정도 무사**하다. 그러나 **이 숫자를 배포 메모리로 인용하면 안 된다.**
> (CLAUDE.md 함정: "계측이 재려는 양을 재고 있는지 의심할 것" — 세 번째 사례)

### (4) 저장 대비 상주 = **31~45배**

프로젝트가 지금까지 보고해 온 "30.9MB / 14.9MB / 11.7MB" 는 **아직 존재하지 않는 패킹 포맷의
이론값**이다. 현재 구현이 실제로 들고 있는 것은 그 **31.6~44.8배**다.
`mA` 의 배율이 가장 큰 것(44.8×)은 분모가 가장 작기 때문이며, **분자는 mC 보다 크다.**

---

## 3. 판정

| 계획의 질문 | 답 |
|---|---|
| **Q1** 실제 상주량 | **978 / 451.5 / 523.5 MB**(텐서합산). L3 32MB 는 **어림도 없다** |
| **Q2** `weight` 와 `_wq` 동시 상주 | **확인**. 정확히 절반씩(472.5+472.5 등) → **단계 2 는 정확히 2배 절감** |
| **Q5** L3(32MB) 상주 가정 | **불성립.** 현재 구현으로는 **30배 초과**. 1차 리뷰·결과 011 의 "L3 상주 가능" 주장은 **저장 기준일 때만** 참 |
| **Q6** 저장↓ 이 상주↓ 와 단조·비례인가 | **아니다. 순위 역전 확인** — 계획 §1.1 예측 적중 |
| Q3(int8) · Q4(5비트 패킹) · Q7 | 미착수(단계 3·4) |

### ★파급 — REVIEW1 승자 선정 근거 (사용자 확인 필요)

REVIEW1(결과 012)은 **A 와 C 가 품질에서 구분 불가**(Δ −0.0183 = 1.5σ)라
**"메모리가 작은 쪽"으로 A 를 뽑았다.** 그 "메모리"가 **저장 MB** 였다.

| 기준 | 승자 | 근거 |
|---|---|---|
| 저장(packed, **이론**) | `mA_g4s34_k4` 11.7MB | REVIEW1 의 판단 |
| **상주(runtime, 실측)** | **`mC_g8_k4` 451.5MB** | 본 결과. mA 대비 **−13.8%** |
| 품질(final val) | 구분 불가 — `mC` 3.6862 / `mA` 3.7003 (Δ 0.0141 = 1.2σ) | 결과 012 |

**품질이 같다면 `mC` 가 상주에서 더 작다.** 그리고 그 우위는 **단계 2·3 을 거쳐도 유지**된다
(둘 다 유니크 파라미터에 비례하므로):

| 구현 단계 | `mC` 상주 | `mA` 상주 | 승자 |
|---|---|---|---|
| 현재(latent+dequant fp32) | 451.5 | 523.5 | **mC** |
| 단계2(latent 해제) | 242.2 | 278.2 | **mC** |
| 단계3(dequant int8) | 85.3 | 94.3 | **mC** |
| **단계4(5비트 패킹 + 패킹 커널)** | **12.8** | **9.6** | **mA** |

> **`mA` 의 우위는 단계 4 가 만들어져야 실현된다. `mC` 의 우위는 오늘 실측된다.**
> 이것이 승자 재검토의 핵심 논점이다. **어느 쪽으로 확정할지는 사용자 판단 사항**이며,
> 이 문서는 **관측과 조건을 제시할 뿐 REVIEW1 을 아직 고치지 않았다**(log-to-result §7).

---

## 4. 후속

### [필수] P034 단계 2 — latent 해제 (커널 불필요, **상주 정확히 절반**)

`freeze_quant()` 이후 추론에는 역전파가 없으므로 `TLinear.weight` 는 죽은 무게다.
측정값이 latent 와 `_wq` 가 **정확히 같은 크기**임을 보였으므로 절감은 **정확히 2배**다.
가장 값싼 실질 레버이고, 승자 논쟁과 무관하게 이득이다.

### [필수] `deploy_mb` → `packed_mb` / `runtime_mb` 표기 분리

`mem_breakdown()` 이 `runtime_mb` 를 함께 계산하고, `compare.py`·`report()`·결과 json 이
**두 값을 나란히** 내도록 한다. 지금 표기(`deploy_mb`)는 **어느 쪽인지 말하지 않아 오해를 낳는다** —
이 프로젝트가 실제로 겪은 오해다.

### [중] 계측 수정 — CUDA 피크에서 옵티마이저 상태 배제

`mem_runtime.py` 가 ① 모델만 담긴 임시 체크포인트로 재측정하거나
② `torch.load` 직후 `st` 를 해제하고 `reset_peak_memory_stats()` 를 **다시** 호출하도록 한다.
현재 값은 배포 상주량을 약 3배 과대 보고한다(§2-(3)).

### [중] 결과 011·1차 리뷰의 "L3 32MB 상주 가능" 문구에 조건 명시

**저장 기준일 때만 참**이다. 현재 구현 기준으로는 30배 초과다.

### [저] 단계 3(int8) → 단계 4(5비트 패킹)

단계 4 는 커스텀 커널이 필요하다(계획 §1.2). **단계 2 를 먼저 하고 그 다음 판단**한다.

---

## 5. 재현 명령

```
run_P034_memory.bat
REM 또는 개별
python scripts/mem_runtime.py --device cpu  --max-new 32
python scripts/mem_runtime.py --device cuda --max-new 32
```
학습 없음. `runs/` 에 아무것도 쓰지 않는다.

---

## 6. 한계

- **RSS 증가분은 판정에 쓸 수 없다**(순차 로드로 힙 재사용 — `mC` 가 음수). 텐서합산이 정본.
- 텐서합산은 **활성값·KV 캐시·임시버퍼를 제외**하므로 **하한**이다. `max_new=32`, batch 1 기준.
- **CUDA 피크는 옵티마이저 상태 오염**을 포함한다(§2-(3)). 배포 수치로 인용 금지.
- 이 값들은 **오늘의 구현**에 대한 것이다. 단계 2~4 가 바꾸려는 대상이 바로 이 기준선이다.
- 승자 재검토는 **"어느 메모리 기준을 프로젝트 목표로 삼는가"** 라는 가치 판단을 포함한다.
  본 문서는 그 판단을 대신하지 않는다.

---

# 7. 단계 1.5 (2026-07-31) — 3:4 희소 감사 : **"희소"가 아니었고, 2.64× 는 회계 착시였다**

로그: `016_log_20260731_P034-1point5-sparse34-test-logs.txt` · 배치 `run_P034_sparse34.bat`
→ `scripts/diag_sparse34.py`. CPU 수 분, 학습·GPU 없음.

## 7.1 [A] 상주 차이는 **전부 타잉(g) 몫** — 3:4 는 무관하다

| 모델 | 유니크 삼진 | 상주(결과 016 §1) |
|---|---|---|
| `mA_g4s34_k4` | 64.29M | 523.5 MB |
| `mC_g8_k4` | 54.85M | 451.5 MB |
| 차이 | **9.44M** | **72.0 MB** |

중간층 MLP 1개 = `3 × 768 × 2048` = 4.718M. g4 는 4그룹(18.87M), g8 은 2그룹(9.44M) →
**차이 9.44M 이 정확히 일치**하고, `9.44M × 8B = 72.0MB` 도 정확히 일치한다.

> **3:4 는 파라미터 개수를 1개도 줄이지 않는다**(값을 0으로 만들 뿐). §2-(1)의 순위 역전은
> **순전히 g4 vs g8** 이며 sparse34 구현 문제가 아니다. **[A] 종결.**

## 7.2 [B] ★"3:4 희소"는 표준 경로보다 **덜 희소하다**

| 모델 | 표준 TWN 비영 | **3:4 비영** | 차이 |
|---|---|---|---|
| `mA_g4s34_k4` | 54.40% (0 이 45.60%) | **75.00%** | 3:4 가 **+20.6%p 조밀** |
| `mC_g8_k4` | 56.19% (0 이 43.81%) | 75.00% | +18.8%p |
| `p6d` | 56.76% (0 이 43.24%) | 75.00% | +18.2%p |

표준 경로의 임계 `|w| ≥ 0.7·mean|w|` 는 **가중치의 43~46% 를 이미 0으로 만들고 있었다.**
3:4 는 그 0 을 **25% 로 줄인다.**

> **★따라서 이 기법의 메모리 이득은 "0 이 많아서"가 아니라 오직 "5비트 무낭비 패킹"에서 온다.**
> **"희소"라는 이름이 기전을 잘못 가리킨다** — 정확히는 **N:M 준정형 구조 강제(패킹 가능성)** 다.
> 층별 편차도 거의 없다(TWN 비영 52.4~55.3%) → 전역 단일 bpw 로 다뤄도 되는 것은 맞다.

## 7.3 [C] 품질 비용의 기전 — 0 이 늘어서가 아니라 **잘못된 0**

| 결정 (mA 기준, 64.3M 가중치) | 비율 |
|---|---|
| 둘 다 유지 | 51.06% |
| **TWN 유지 → 3:4 제거** | **3.34%** ← 블록 규칙 때문에 죽인 **큰 가중치** |
| TWN 제거 → 3:4 유지 | 23.94% ← 억지로 살린 작은 가중치 |
| 둘 다 제거 | 21.66% |

상대 재구성 오차: TWN **0.4573** vs 3:4 **0.5877** → **3:4 가 1.285× 나쁘다**
(mC 1.151× / p6d 1.172×).

> **`mA` 는 3:4 로 학습된 모델인데도** 3:4 재구성 오차가 가장 나쁘고 "잘못 죽인 비율"도 가장 높다.
> 결과 008 의 희소 단독비용(+0.035~0.056)은 **"0 을 25% 만들어서"가 아니라 "TWN 이 살렸을 3.3% 를
> 죽이고 대신 쓸모없는 23.9% 를 살려서"** 로 읽힌다.
> **단 이것은 상관 진단이다** — 재구성 오차는 학습 목적함수가 아니고, STE 로 학습된 모델의
> 기능은 `||W−Ŵ||` 이 결정하지 않는다. **인과는 학습 런으로만 확인된다.**

## 7.4 [D] ★★bpw 회계 — **순위는 유지되지만 격차가 붕괴한다**

측정된 삼진 코드 엔트로피 = **1.554 bpw**(0 비율 43.2%), + 그룹 스케일 0.125 = **1.679 bpw**.
문서값 **1.71 과 일치**한다 → **1.95 = 1.679(코드+스케일) + 0.27(컨테이너)** 로 분해가 확정됐다.
반면 sparse34 의 **1.25 는 순수 코드공간**이고 스케일도 컨테이너도 없다.

| 모델 | 유니크 | **현행 A** | (B) 코드+스케일 | (C) B+컨테이너 | bpw A / B / C |
|---|---|---|---|---|---|
| `mA_g4s34_k4` | 64.29M | **11.6 MB** | 12.5 | **14.6 MB** | 1.25 / 1.38 / 1.65 |
| `mC_g8_k4` | 54.85M | 14.7 MB | 13.0 | **14.7 MB** | 1.95 / 1.68 / 1.95 |
| `p6d` | 123.86M | 30.8 MB | 26.8 | 30.8 MB | 1.95 / 1.68 / 1.95 |

**순위는 세 회계 모두 동일**(mA < mC < p6d). 그러나 **격차가 무너진다**:

| 지표 | 현행 A | (B) | (C) |
|---|---|---|---|
| `mA` 가 `mC` 보다 작은 정도 | **3.1MB (21.1%)** | 0.5MB (3.8%) | **0.1MB (0.7%)** |
| `mA` 감축비 (vs p6d) | **2.66×** | 2.14× | **2.11×** |
| `mC` 감축비 (vs p6d) | 2.10× | 2.06× | **2.10×** |

> **★`mC` 의 2.10× 는 세 회계에서 불변이고, `mA` 의 추가 이득(2.10 → 2.66×)은 전부 회계 규약
> 차이에서 나왔다.** 같은 규약으로 맞추면 두 모델의 저장은 **14.6 vs 14.7MB — 0.7% 차이, 사실상 동률**이다.

## 7.5 ★근거 논문 재확인 (`arXiv:2601.07892`, Sherry) — 문서 해석 오류 3건

논문 원문 대조 결과, `docs/methods/02_memory.md` 의 서술에 실질적 오류가 있다.

| # | 우리 문서 | 논문 | 판정 |
|---|---|---|---|
| 1 | "현 **1.95** 대비 **×1.56** 추가절감" | 비교 기준이 **2-bit 정렬 패킹** 또는 **1.67-bit 비정렬 패킹**. 주장 절감은 **25%**(= 1.67 → 1.25) | ❌ **1.95 는 논문에 없는 숫자**(Bonsai/PrismML 출처를 섞었다). 우리 실측 동일회계 절감은 **15~18%** |
| 2 | "**25% 강제-0 의 용량손실**" | 3:4 는 **비영을 75% 로 고정**하는 제약 | ❌ 방향이 반대. 우리 표준 경로는 이미 **43~46% 가 0** 이다(§7.2) |
| 3 | "Arenas = **우리 삼진 어닐의 특수형**(이미 보유)" | Arenas = **annealing residual synapse**. 3:4 를 그냥 씌우면 **weight trapping**(gradient 균질화 → 국소 축적 → representational collapse)이 난다고 논문이 **명시**하고 그 대응으로 도입 | ❌ **근거 없는 동일시.** 우리 `quant_anneal` 은 STE 위 어닐 계수이고 residual synapse 가 아니다. **우리는 대응책 없이 3:4 를 씌웠다** |

**논문의 진짜 요지는 메모리가 아니라 속도다.** 1.67-bit 비정렬 패킹은 SIMD 정렬이 깨져 추론이
느려지는데, 5비트는 정렬을 회복한다. 보고된 이득은 **비트 25% 절약 + 속도 10%**(LLaMA-3.2 1B,
Intel i7-14700HX). 우리 문서·1차 리뷰는 이를 **순수 메모리 레버**로 읽었다.

> **★그리고 우리에겐 Arenas 가 없다.** 논문이 "그냥 씌우면 무너진다"고 경고한 바로 그 조건에서
> 결과 008 의 +0.035~0.056 이 나왔다. §7.3 의 "잘못 죽인 3.3% / 억지로 살린 23.9%" 가
> weight trapping 의 징후일 수 있다. **가설이며 미검증이다.**

## 7.6 판정 — REVIEW1 승자 근거에 미치는 영향

| 기준 | `mA_g4s34_k4` | `mC_g8_k4` | 승 |
|---|---|---|---|
| 품질(final val) | 3.7003 | 3.6862 | 구분 불가(1.2σ) |
| 저장, **현행 회계** | **11.6MB** | 14.7MB | mA (−21%) |
| 저장, **동일 회계(C)** | 14.6MB | 14.7MB | **사실상 동률(−0.7%)** |
| **상주(실측)** | 523.5MB | **451.5MB** | **mC (−13.8%)** |
| 구현 리스크 | Arenas 미구현, 5비트 커널 미구현 | 없음(표준 경로) | **mC** |

> **REVIEW1 은 "품질 동률 → 메모리 작은 쪽"으로 mA 를 뽑았다.** 그 "메모리"를 같은 회계로 다시
> 재면 **저장에서도 동률**이고, **상주에서는 mC 가 13.8% 작다.** 게다가 mA 의 이론적 우위는
> ① 5비트 패킹 커널 ② 논문의 Arenas — **둘 다 미구현**인 것에 의존한다.
> **판단 재료는 이것으로 갖춰졌다. 결정은 사용자 몫이며 이 문서는 아직 REVIEW1 을 고치지 않았다.**

## 7.7 한계

- 회계 (B)(C) 의 컨테이너 오버헤드 **0.274 bpw 는 우리 문서에서 역산한 값**이다. 실제 GGUF/
  커스텀 포맷이 3:4 코드를 어떻게 담는지는 **여전히 미검증**(P016 §한계와 동일).
- §7.3 은 **학습된 가중치에 두 규칙을 씌워 비교**한 것이다. `mA` 의 가중치는 3:4 에 적응돼 있으므로
  "TWN 이었다면"의 반사실이 아니다. **모델 간 절대비교보다 모델 내 규칙 차이**를 읽어야 한다.
- 논문의 25%·10% 는 **LLaMA-3.2 1B / 다른 커널 / 다른 스케일**의 값이다. 우리 132M·파이썬
  경로에 그대로 적용되지 않는다.

---

# 8. ★회계 불일치의 치명도 판정 — **"전부 철회·재실험" 수준이 아니다**

사용자 질문: *"회계불일치가 지금까지 실험계획을 모두 철회하고 새로 실험해야 하는 정도의
치명적 오류인가?"* → **아니다.** 근거를 분해한다.

## 8.1 무엇이 오염됐고 무엇이 아닌가

| 산출물 | 영향 | 이유 |
|---|---|---|
| **`val_loss` · `bpb` · `grad_max` · `n_skip`** | **없음** | `mem_breakdown()` 은 **보고 전용**이다. 학습·손실·역전파 어디에도 들어가지 않는다 |
| **학습 시간 · `ms/step` · VRAM** | **없음** | 동일 |
| **모든 품질 서열**(006 타잉격차 / 005·012 KD k4 / 015 wsd) | **없음** | 전부 `val_loss` 기반 |
| **저장 MB · 감축비(`2.64×`·`3.00×`)** | **오염** | **sparse34 런에서만.** 비-s34 런은 1.95 단일 규약이라 내부적으로 일관 |
| **REVIEW1 승자 선정** | **오염** | 근거가 "메모리 작은 쪽"이었고 그 격차가 21% → 0.7% 로 붕괴 |
| **1차 리뷰의 헤드라인 서술** | **오염** | "3.00× 로 dense 절반 이하" 류 |

**재학습이 필요한 런: 0개.** 오염된 것은 **런이 끝난 뒤 계산되는 파생 숫자 하나**뿐이고,
그건 체크포인트 없이도 다시 계산된다(§7.4 가 실제로 그렇게 했다).

## 8.2 순위는 안 바뀌었다 — 바뀐 것은 **간격**이다

세 회계 모두 `mA < mC < p6d`. 즉 **"어느 것이 작은가"는 견고**하고
**"얼마나 작은가"만 틀렸다**(21% → 0.7%).

> 이 구분이 중요하다. 만약 순위가 뒤집혔다면 **결과 008 의 설계 결론**(g4+s34 채택)까지
> 다시 봐야 했을 것이다. 뒤집히지 않았으므로 **재실험 근거가 없다.**

## 8.3 그러나 **두 가지는 실질적으로 바뀐다**

1. **REVIEW1 승자.** 0.7% 차이는 판정 근거가 될 수 없다. 게다가 **상주 기준에서는 역전**된다.
   → **결정 보류**가 옳다(§7.6).
2. **프로젝트 헤드라인.** "dense 대비 절반 이하 메모리"(CLAUDE.md 1행)는 **저장 기준으로도
   2.11×**(=47%)로 여전히 성립하지만, **상주 기준으로는 1.87×**(mA) / 2.17×(mC) 다.
   **어느 기준인지 매번 명시**해야 한다.

## 8.4 왜 이런 오류가 났는가 — 재발 방지

**두 출처의 숫자를 한 식에 섞었다.**

| 숫자 | 출처 | 규약 |
|---|---|---|
| **1.95** | Bonsai/PrismML "배포 밀도"(`docs/custom_ternary_kernel.md`) | 코드 + 그룹스케일 + 컨테이너 |
| **1.25** | Sherry 논문 `C(4,3)·2³=2⁵` | **순수 코드공간** |

논문(Sherry)의 비교 기준은 **1.67bpw** 이고 — 우리 실측 `코드 1.554 + 스케일 0.125 = 1.679`
와 **정확히 일치**한다 — 주장 절감은 **25%**(1.67→1.25)다.
**1.95 는 논문에 없는 숫자**인데 우리가 분모로 썼다.

> **재발 방지 규칙(기준표 §7 등재)**: *bpw 를 인용할 때 **무엇이 포함된 값인지**를 함께 적는다.*
> `코드만` / `코드+스케일` / `코드+스케일+컨테이너` 세 가지를 구분하고,
> **서로 다른 규약의 두 값으로 비율을 만들지 않는다.**
> 코드 조치: `mem_breakdown()` 이 **단일 규약**으로 계산하고 `bpw_convention` 을 json 에 기록.

## 8.5 그래서 필요한 것은 **재실험이 아니라 세 가지**

| # | 조치 | 종류 | 상태 |
|---|---|---|---|
| 1 | `mem_breakdown()` 단일 규약 통일 + `packed_mb`/`runtime_mb` 분리 | **코드 수정** | 미착수(P034 §5) |
| 2 | 오염된 문서에 배너·정정 | **문서** | ✅ 완료(008·012·016·P016·리뷰) |
| 3 | **Arenas 검증** — 논문이 필수라 한 부품을 빼고 측정했다 | **신규 실험** | [P036](../test_plan/P036_Arenas-잔차시냅스-weight-trapping.md) 신설 |

**3번만이 새 GPU 시간을 요구한다.** 그리고 그건 회계 오류 때문이 아니라
**논문의 다른 부분(Arenas)을 놓쳤기 때문**이다 — 별개의 오독이다.

## 8.6 ★Arenas 관련 — 논문 본문에서 확인한 것 (§7.5 보강)

논문 §3.2 원문을 확보해 기전과 수식을 확인했다.

```
Y = X·Tα + λ_t·X·W                     (식 7)
∂L/∂X = (∂L/∂Y)(Tα + λ_t·W)ᵀ           (식 8)   ← W 가 backward 에 들어간다
```

`λ_t` 는 학습 종료 시 0 → **추론 오버헤드 0**.

**우리 어닐과의 차이가 명확해졌다**: `refresh_quant` 는
`_wq = wq + (1−a)·(w − wq).detach()` 로 **순전파 값만 혼합**하고,
`.detach()` 때문에 **`w` 가 gradient 에 들어가지 않는다.**
Arenas 의 존재 이유가 정확히 그 gradient 경로이므로, **우리는 그 기능을 갖고 있지 않다.**

추가로 논문 ablation(Fig.6)은 **3:4 뿐 아니라 1-bit·1.67-bit 순수 삼진에서도** Arenas 가
이득이라고 보고한다 → **`mC`·`p6d` 같은 비-희소 모델에도 적용 대상**이다.
P036 이 이를 sparse34 전용으로 좁히지 않은 이유다.

---

# §9. **단계 2 — latent 해제** (2026-07-31, 완료)

- 배치: `run_P034_stage2_latent.bat` · 로그: `016_log_20260731_P034-stage2.txt`
- 로드맵 **R2**. 학습 없음, GPU 0.

## 9.1 ★로짓 동등성 게이트 — **3모델 전부 `max|Δlogit| = 0.000e+00`**

해제 전후로 **비트 하나도 안 바뀌었다.** 해제는 계산을 바꾸지 않으므로 정확히 0 이어야 하고,
실제로 0 이다. → 아래 메모리 수치는 **같은 모델을 잰 것**이 맞다.

## 9.2 상주 메모리 — **1.86~1.93배 감축**

| 모델 | 해제 전 | 해제 후 | 감축 | 내역 |
|---|---|---|---|---|
| `p6d` | 978.0 MB | **505.5 MB** | **1.93×** | latent 472.5 → **0** |
| `mC_g8_k4` | 451.5 MB | **242.2 MB** | **1.86×** | latent 209.2 → **0** |
| `mA_g4s34_k4` | 523.5 MB | **278.2 MB** | **1.88×** | latent 245.2 → **0** |

- **저장 MB 는 변하지 않았다**(27.1 / 13.1 / 12.4) — 저장은 패킹 포맷의 이론값이므로 **정상**이다.
- 이론 상한 2.00× 에 못 미치는 부분(0.07~0.14)이 **임베딩·norm·gate 등 삼진이 아닌 33MB**다.
  타잉 모델일수록 그 고정분의 비중이 커서 감축비가 낮다(mC 1.86 < p6d 1.93). **예상대로다.**
- **상주 순위 불변**: `mC 242.2 < mA 278.2 < p6d 505.5`. 해제 전과 같다.
  → P034 §1.1 의 "상주는 유니크 파라미터 수를 따라간다" 가 **해제 후에도 성립**한다.

## 9.3 ★속도 — **거의 변하지 않는다** (CPU 1스레드, 캐시 on)

| 모델 | 해제 전 tok/s | 해제 후 tok/s | 변화 | TTFT 변화 |
|---|---|---|---|---|
| `p6d` | 30.25 | 31.05 | **+2.6%** | 58.0 → 48.5 ms (**−16%**) |
| `mC_g8_k4` | 33.98 | 33.89 | **−0.3%** | 47.2 → 40.9 ms (−13%) |
| `mA_g4s34_k4` | 31.41 | 32.96 | **+4.9%** | 48.4 → 42.9 ms (−11%) |

**상주를 절반으로 줄였는데 속도는 −0.3~+4.9%.** Windows CPU 노이즈(1~2%)를 감안하면
**사실상 무변**이다.

> ### ★이것이 결정적인 이유 — **단일축 실험이었다**
> 결과 014 §10.2 는 "메모리→속도 전이가 없다"를 보였지만, 거기서는 저장·상주·유니크 파라미터가
> **함께** 움직여 어느 축이 원인인지 가릴 수 없었다. 단계2 는 **상주만** 1.9배 움직이고
> 저장·파라미터 수·연산량을 전부 고정한다. 그 조건에서 속도가 안 움직였다.
> → **상주 메모리는 이 크기에서 CPU 디코드 속도의 병목이 아니다.** 병목은
> **dequant 후 fp32 GEMM 의 연산량**이고, 그걸 건드리는 것은 **R8(5비트 패킹 커널)** 뿐이다.
> 014 §10.2 가 제기한 근거를 단계2 가 **단독으로 확증**했다.

TTFT 는 11~16% 줄었다 — prefill 은 큰 행렬 연산이라 메모리 대역폭에 더 민감하다.
다만 **1회성 비용**이라 긴 생성에서는 무의미하다.

## 9.4 판정 — R2 완료

| 질문 | 답 |
|---|---|
| 해제가 출력을 바꾸는가 | **아니다**(정확히 0) |
| 상주가 절반이 되는가 | **된다**(1.86~1.93×, 이론 상한 2.00 대비 93~97%) |
| 순위가 바뀌는가 | **안 바뀐다**(mC < mA < p6d 유지) |
| 속도가 따라오는가 | **안 따라온다**(−0.3~+4.9%) |

**조치**: 이후 배포 상주량은 **해제 후 값**을 정본으로 인용한다
(`p6d 505.5 / mC 242.2 / mA 278.2 MB`). 여전히 **L3 32MB 상주는 성립하지 않는다**(7.6~15.8배 초과).

## 9.5 한계

- `drop_latent()` 는 **되돌릴 수 없다.** 학습 재개 불가, 커스텀 커널 경로와 배타적이다.
- 텐서합산 기준이다. RSS 는 할당자 때문에 해제분을 OS 에 즉시 안 돌려준다(로그의 RSS 참조).
- batch 1 · Windows CPU 노이즈. **1~2% 차이는 읽지 않는다.**

## 9.6 재현 명령 (배치 삭제 대비 보존)

```bat
run_P034_stage2_latent.bat
:: [0/3] 캐시 게이트 (tool_kvcache_gate.bat 경유)
python scripts\diag_kvcache.py --models mA_g4s34_k4 mC_g8_k4 p6d --device cpu --tol 1e-3 --max-new 24
:: [1/3] 해제 전 기준선 (tool_mem_profile.bat 경유, cpu + cuda)
python scripts\mem_runtime.py --device cpu  --max-new 32 --models mA_g4s34_k4 mC_g8_k4 p6d
python scripts\mem_runtime.py --device cuda --max-new 32 --models mA_g4s34_k4 mC_g8_k4 p6d
:: [2/3] ★단계2 — latent 해제(로짓 동등성 게이트 내장)
python scripts\mem_runtime.py --device cpu  --max-new 32 --models mA_g4s34_k4 mC_g8_k4 p6d --drop-latent
python scripts\mem_runtime.py --device cuda --max-new 32 --models mA_g4s34_k4 mC_g8_k4 p6d --drop-latent
:: [3/3] 속도 — 해제 전/후
python scripts\bench_infer.py --device cpu --threads 1 --max-new 128 --reps 3
python scripts\bench_infer.py --device cpu --threads 1 --max-new 128 --reps 3 --drop-latent
```

### §1 표의 저장 MB 인용 주의

§1 은 구 규약(30.9/14.9/11.7)으로 쓰였다. **정본은 안 B**: `p6d 27.1 / mC 13.1 / mA 12.4 MB`.
상주는 해제 전 `978.0 / 451.5 / 523.5`, **해제 후(배포 정본) `505.5 / 242.2 / 278.2 MB`**.

---

# §10. **단계 3 — int8 저장** (2026-08-06, 완료)

- 배치: `run_P034_stage3_int8.bat` · 로그: `016_log_20260806_P034-stage3.txt`
- 학습 없음, GPU 0. **§9(단계2) 위에 얹은 것**이다.

## 10.1 ★검증하려던 것 vs 실제로 검증된 것

| 검증하려던 것 | 결과 |
|---|---|
| int8 저장이 삼진 항을 1/4 로 줄이는가 | ✅ **정확히 그렇다**(472.5→121.8 / 209.2→53.9 / 245.2→63.2 MB) |
| 출력을 바꾸지 않는가 | ✅ **3모델 전부 `max\|Δlogit\| = 0.000e+00`** |
| 예측(mA 96.2MB)이 맞는가 | ✅ **실측 96.2MB — 소수점까지 적중** |
| 느려지는가(예측: 그렇다) | ✅ **약 3배 느려졌다** — 예측된 트레이드오프 |
| **이론값(12.4MB)에 닿는가** | ❌ **7.8배 남았다** — 이것도 예측대로 |

## 10.2 메모리 — 예측이 소수점까지 맞았다

| 모델 | 단계1 | 단계2 | **단계2+3** | 단계1 대비 | 삼진 사본 | 게이트 |
|---|---|---|---|---|---|---|
| `p6d` | 978.0 | 505.5 | **154.8** | **6.32×** | 472.5 → **121.8** | 0.000e+00 |
| `mC_g8_k4` | 451.5 | 242.2 | **86.9** | **5.19×** | 209.2 → **53.9** | 0.000e+00 |
| `mA_g4s34_k4` | 523.5 | 278.2 | **96.2** | **5.44×** | 245.2 → **63.2** | 0.000e+00 |

- 삼진 항이 **정확히 1/3.88**(=1 + 4/128 바이트 대 4 바이트)로 줄었다. 회계식대로다.
- **남은 33.0MB 가 전부 fp32 임베딩·norm·gate** 다. 세 모델 모두 같은 값이고,
  `mC` 에서는 이제 **상주의 38%** 를 차지한다. → **다음 레버는 삼진이 아니라 임베딩이다.**
- 저장 MB 는 변하지 않았다(27.1/13.1/12.4). 정상이다.

## 10.3 ★속도 — **3배 느려졌다.** 예측한 대가이고, 예측보다 크다

| 모델 | 단계2 tok/s | 단계2+3 tok/s | 변화 | TTFT |
|---|---|---|---|---|
| `p6d` | 34.55 | **11.26** | **−67%** | 45.1 → 106.7 ms |
| `mC_g8_k4` | 40.94 | **11.72** | **−71%** | 41.2 → 95.7 ms |
| `mA_g4s34_k4` | 36.37 | **11.81** | **−68%** | 40.7 → 99.0 ms |

배치 헤더에 "**EXPECT THIS TO BE SLOWER**" 를 미리 적어 둔 그대로다. 결과 014 §10·016 §9 가
우리는 **연산 바운드**라고 했으므로 메모리를 연산과 바꾸면 이렇게 된다. **실패가 아니라 거래다.**

> **그러나 −67% 는 "약간" 이 아니다.** 31~41 tok/s 는 엣지에서 쓸 만한 수치였고
> **11 tok/s 는 아니다.** 단계3 은 **"RAM 이 부족해서 못 올리는 경우의 마지막 수단"** 이지
> 기본 배포 경로가 아니다. **이 문장을 결론으로 못박는다.**

## 10.4 ★새로 보인 것 — **모델 간 속도 차이가 사라졌다**

int8 상태에서 세 모델이 **11.26 / 11.72 / 11.81 tok/s** 로 **5% 이내**다.
파라미터 수는 123.86M vs 54.85M 으로 **2.26배** 차이인데도 그렇다.

단계2 에서도 이미 이상했다 — 34.55 vs 40.94 = **1.19배**뿐(파라미터는 2.26배).
int8 을 얹으니 그 1.19배마저 1.04배로 줄었다.

**가설(미검정)**: decode 속도가 **파라미터 수가 아니라 층 수(=커널 런치 횟수)** 에 지배된다.
세 모델은 전부 **20층**이고, dequant 는 층당 1회 추가 커널이므로 런치 수도 층 수에 비례한다.
batch 1·토큰 1 의 GEMM 은 너무 작아서 연산량보다 **런치·파이썬 오버헤드**가 크다.

> **이 가설이 맞다면 단계4(패킹 커널)도 같은 벽에 부딪힌다** — 패킹된 것을 직접 읽어도
> **층당 런치 횟수는 그대로**다. 즉 단계4 의 이득은 **메모리에만** 있고 속도에는 없을 수 있다.
> **R8 의 기대값을 낮춰야 한다는 첫 신호**다.
>
> **값싼 검정법이 이미 있다**: `--infer-repeat` 로 층 통과 수를 12~36 으로 바꿔가며 tok/s 를 재면
> 된다(P031 단계0 구현 완료). P031 은 val_loss 만 쟀고 **tok/s 는 안 쟀다.**
> → **P030 단계4 로 신설**: "decode 속도는 파라미터 수와 층 수 중 무엇을 따라가는가".

## 10.5 그래서 "이론값까지 내려갈 수 있는가" 에 대한 답 (사용자 질문)

| 단계 | 상주(mA) | 이론 12.4MB 대비 | 상태 |
|---|---|---|---|
| 단계1 | 523.5 | 42.2× | — |
| 단계2 | 278.2 | 22.4× | ✅ §9 |
| **단계3** | **96.2** | **7.8×** | ✅ **본 절** |
| 단계3+임베딩 int8 | ~69.7 | 5.6× | 미구현 — **다음 레버** |
| 단계4(삼진 패킹) | ~43.5 | 3.5× | 미구현 |
| 단계4+임베딩 패킹 | **12.4** | **1.00×** | 미구현 |

**닿을 수 있다. 단 조건이 셋이고 지금 둘이 남았다** — ①삼진 패킹 ②**임베딩 패킹**
③커널이 패킹된 것을 직접 읽기. **③이 §10.4 의 가설과 충돌할 수 있다는 것이 새 정보다.**

## 10.6 한계

- `to_int8()` 은 **되돌릴 수 없다**(추론 전용, 학습 재개 불가, 커널 경로와 배타적).
- 속도는 batch 1 · Windows CPU · 중위값 3회다. 다만 **−67% 는 노이즈(1~2%)의 30배**라 방향은 확실하다.
- §10.4 는 **가설이다.** 층 수를 고정하고 파라미터만 바꾼 대조가 없다.
- 텐서합산 기준. RSS 는 할당자 때문에 해제분을 즉시 안 돌려준다.

## 10.7 재현 명령

```bat
run_P034_stage3_int8.bat
:: 내부
python scripts\mem_runtime.py --device cpu --max-new 32 --drop-latent
python scripts\mem_runtime.py --device cpu --max-new 32 --drop-latent --int8-store
python scripts\bench_infer.py --device cpu --threads 1 --max-new 128 --reps 3 --drop-latent
python scripts\bench_infer.py --device cpu --threads 1 --max-new 128 --reps 3 --drop-latent --int8-store
```

---

# §11. ★**§10 의 속도 수치 정정 — 동시 실행 오염** (2026-08-06)

**사용자 지적으로 확인했다.** 로그 타임스탬프 대조:

| 작업 | 구간 |
|---|---|
| **P034 단계3**(§10) | 04:08:47 ~ 04:12:14 |
| **P037 단계2B** | 04:07:56 ~ 04:12:04 |

**거의 완전히 겹친다.** 결과 015 가 이미 기록한 "동시 실행 시 +14% 느림" 이 그대로 걸렸다.
그리고 **P030 단계4**(04:49:54~04:55:09, **단독**)가 같은 조건을 다시 쟀으므로 **정정할 수 있다.**

## 11.1 정정표

| 조건 | §10 값(오염) | **정정값**(단독) | 오차 |
|---|---|---|---|
| 단계2 `p6d` | 34.55 | **36.78** | −6.1% |
| 단계2 `mC` | 40.94 | **45.01** | −9.0% |
| 단계2 `mA` | 36.37 | **42.48** | −14.4% |
| int8 `p6d` | 11.26 | **14.20** | **−20.7%** |
| int8 `mC` | 11.72 | **14.89** | **−21.3%** |
| int8 `mA` | 11.81 | **15.08** | **−21.7%** |

**int8 쪽이 20% 이상 과소 측정됐다.** 오염이 더 무거운 쪽에 더 크게 걸린 셈이다.

## 11.2 정정된 결론 — **방향은 그대로, 크기는 완화**

| 항목 | §10 서술(오염) | **정정** |
|---|---|---|
| int8 감속 | −67~71% | **−61.4% / −66.9% / −64.5%** |
| "단계3 은 마지막 수단" | 유효 | **유효**(여전히 약 3배 느리다) |
| 메모리 수치 | **영향 없음** | 메모리는 동시 실행과 무관. §10.2 그대로 |
| 게이트 `0.000e+00` | **영향 없음** | 그대로 |

**§10 의 메모리 결론은 전부 그대로다.** 정정 대상은 **속도 수치뿐**이다.

## 11.3 ★§10.4 의 가설은 **철회한다**

§10.4 는 "파라미터 2.26배 차이인데 tok/s 5% 이내 → 런치 오버헤드 바운드" 라고 적었다.
**결과 014 §11.3 이 전제를 무너뜨렸다** — 타잉은 저장을 줄이지 **컴퓨트를 안 줄인다.**
`p6d` 와 `mC` 는 **층당 FLOPs 가 정확히 같다.** 속도가 비슷한 것은 이상현상이 아니다.

**설명이 필요한 이상현상이 애초에 없었으므로, 가설도 필요 없다.**

남는 진짜 관측은 **`mC` 가 층당 21.5% 싸다**는 것이고, 설명 후보는 **캐시 지역성**이다
(g8 은 연속 8층이 같은 MLP 가중치를 재사용). 상세는 결과 014 §11.3.

## 11.4 재측정이 필요한가 — **부분적으로 불필요**

P030 단계4 가 **R=1.0 의 단계2·int8 을 단독 조건에서 이미 다시 쟀다**(§11.1 의 정정값).
따라서 **핵심 수치는 재측정 없이 확정된다.**

남은 것은 **cuda 조건**과 **스레드 스윕**인데, 그건 §10 의 판정에 쓰이지 않았다.
그래도 조건을 통제한 단일 로그가 하나 있으면 이후 인용이 편하므로
**`run_P034_stage3b_remeasure.bat`** 를 만들어 두었다(수 분, 선택 사항).
**단독 실행 확인 절차**를 배치 첫머리에 넣었다.

---

# §12. **단계 3B 단독 재측정 — 재현되지 않았다. 그리고 CUDA 를 처음 쟀다** (2026-08-06)

- **로그**: `016_log_20260806_P034-stage3b.txt`   - **배치**: `run_P034_stage3b_remeasure-done.bat`
- **실행**: 05:41:36 ~ 05:45:52, **단독**(직전 작업 P037 단계3 는 05:39:38 종료)

## 12.1 메모리 — **완전히 재현. 이건 애초에 의심 대상이 아니었다**

| 모델 | 상주(기준) | 상주(단계2+3) | 감축 | 게이트 |
|---|---|---|---|---|
| `p6d` | 978.0MB | **154.8MB** | 6.32× | `0.000e+00` ✅ |
| `mC_g8_k4` | 451.5MB | **86.9MB** | 5.19× | `0.000e+00` ✅ |
| `mA_g4s34_k4` | 523.5MB | **96.2MB** | 5.44× | `0.000e+00` ✅ |

§10.2·§11 과 **소수점까지 동일**. 동시 실행은 메모리를 안 바꾼다는 §11 의 진술이 확인됐다.

## 12.2 ★속도 — **014 §11 과 2~9% 어긋난다.** 단독 실행만으로는 부족했다

**CPU 1스레드, 캐시 on, `--drop-latent`** (tok/s):

| 조건 | 모델 | 014 §11(정정값) | **3B** | 차이 | ms/토큰 증가 |
|---|---|---|---|---|---|
| 단계2 | `p6d` | 36.78 | **36.09** | −1.9% | +0.52 |
| 단계2 | `mC` | 45.01 | **43.49** | −3.4% | +0.77 |
| 단계2 | **`mA`** | 42.48 | **38.81** | **−8.6%** | **+2.23** |
| int8 | `p6d` | 14.20 | **13.73** | −3.3% | +2.4 |
| int8 | `mC` | 14.89 | **14.23** | −4.4% | +3.2 |
| int8 | `mA` | 15.08 | **14.33** | −5.0% | +3.5 |

배치 헤더가 미리 정한 판정 규칙: *"몇 % 넘게 다르면 뭔가 같이 돌았거나 열 상태가 다르다.
**두 값을 평균내지 말고 어느 쪽이 깨끗했는지 알아내라**"*.

### 무엇이 달랐나 — 후보

| 후보 | 근거 | 판정 |
|---|---|---|
| 동시 실행 | 로그 타임스탬프상 **두 런 모두 단독** | 🚫 배제 |
| **열 상태** | 3B **직전 12.5분간 `prepare` 토큰화가 CPU 를 포화**시켰다(05:27~05:39). 014 §11 은 GPU 벤치 뒤였다 | ✅ **가장 유력** |
| 측정 노이즈 | 로그 자체가 "Windows CPU 는 1~2% 차이를 읽지 않는다" | ⚠️ 1.9~5.0% 는 설명하나 **mA 8.6% 는 못 한다** |

### ★`mA` 만 2.2ms/토큰 벌어진 것은 열로 설명 안 된다

열 상태라면 세 모델이 **비슷한 비율로** 느려져야 한다. 실제로는 `p6d` +0.52 / `mC` +0.77 /
**`mA` +2.23 ms** 다. `mA`/`mC` 속도비 자체가 **0.944 → 0.892** 로 움직였다.

**가설(미확정)**: `mA` 는 상주 278.2MB 로 셋 중 **L3(32MB) 경계에서 가장 불리한 지점**에 있고,
캐시 거동은 경계 근처에서 비선형·쌍안정이다. 워킹셋이 조금만 밀려도 재현성이 나빠진다.

> **⚠️ 결과 014 §11 의 `b`(층당 비용) 단조성 — `g8 0.913 < g4 1.022 < dense 1.163` —
> 은 여전히 유효하지만, `mA` 값의 재현성이 이번에 흔들렸다.**
> **"타잉은 속도에도 작은 이득" 은 유지하되, `mA` vs `mC` 의 속도 서열은 매기지 않는다.**

## 12.3 ★★CUDA — **처음 쟀다. 그리고 두 가지가 크게 다르다**

| 조건 | `p6d` | `mC` | `mA` |
|---|---|---|---|
| CUDA 단계2 | 42.47 | 45.42 | **46.78** |
| CUDA int8 | 36.22 | 39.66 | **41.37** |
| **int8 감속** | **−14.7%** | **−12.7%** | **−11.6%** |
| (참고) CPU int8 감속 | −62.0% | −67.3% | −63.1% |

### 발견 1 — ★**int8 언팩 비용은 CPU 현상이다**

CPU 에서 −62~67% 이던 int8 대가가 **CUDA 에서는 −12~15%** 다. **5배 차이.**

> **§10·§11·결과 014 §11.4 의 "R8 은 융합 커널이 아니면 3배 느려진다" 는
> CPU 한정 진술이다.** GPU 에서 분리 언팩은 대역폭 바운드 elementwise 커널이라 싸다.
>
> **프로젝트 타깃이 CPU·엣지이므로 배포 판정은 안 바뀐다.** 다만 **논문·외부 비교에서
> "언팩은 비싸다" 를 무조건 명제로 쓰면 틀린다** — 디바이스를 명시해야 한다.

### 발견 2 — ★**GPU 가 CPU 1스레드보다 겨우 1.18배 빠르다**

`p6d` 단계2: CPU 1스레드 **36.09** vs CUDA **42.47** tok/s.

RTX 4070 Ti Super 가 **단일 CPU 스레드 대비 18%** 다. 배치 1 디코드는 **연산이 아니라 커널
런치·지연 바운드**라는 뜻이고, 이 모델 크기(132M)에서는 **GPU 가 사실상 무의미**하다.

> **이건 프로젝트 논지에 유리한 관측이다** — "CPU 배포" 가 타협이 아니라 **이 크기에서는 정답**이다.
> ⚠️ 배치 1·단일 스트림 한정. 서버 처리량은 완전히 다른 이야기다.

### 발견 3 — CUDA 에서 모델 서열이 뒤집힌다 (⚠️ 해석 보류)

| 디바이스 | 빠른 순 |
|---|---|
| CPU 1스레드 | `mC` > `mA` > `p6d` |
| **CUDA** | **`mA` > `mC` > `p6d`** |

셋은 **층당 FLOPs 도 커널 개수도 같다**. 그런데 CUDA 에서 10% 벌어지고 순서까지 다르다.
**reps=3 중위값 1회 측정이라 해석하지 않는다.** 필요하면 반복 측정을 별도로 설계한다.

## 12.4 판정

| 항목 | 판정 |
|---|---|
| §11 의 정정값이 재현되는가 | ⚠️ **부분 재현.** 방향·서열은 같고 크기가 2~9% 낮다 |
| 어느 값을 정본으로 쓸 것인가 | **014 §11(정정값) 유지.** 3B 는 직전 CPU 부하가 있었다. **평균내지 않는다** |
| 메모리·게이트 | ✅ **완전 재현.** 변경 없음 |
| CUDA int8 | ✅ **신규 확정** — 감속 −12~15%(CPU 의 1/5) |
| 속도 측정 규범 | ⚠️ **개정 필요** — "단독 실행" 만으로 부족. **직전 부하·열 상태**도 조건이다 |

## 12.5 ★규범 개정 — 속도 측정 절차

결과 015("동시 실행 +14%") → 결과 016 §11("또 걸렸다") → **§12.2("단독인데도 어긋났다")**.
같은 계열 3번째다. 절차를 하나 더 추가한다:

1. 속도 배치는 **단독 실행**한다 (기존)
2. **직전 5분 이상 유휴** 상태에서 시작한다 (신규) — 특히 `prepare`·토큰화 직후 금지
3. **같은 배치 안에 기준선 조건을 하나 포함**해 절대값이 아니라 **비율로 읽는다** (신규·권장)

3번이 가장 값싸다 — 열·부하 상태가 **모든 조건에 공통으로** 걸리므로 비율은 살아남는다.

## 12.6 한계

- reps=3 중위값, **각 조건 1회 측정**. §12.2 의 mA 이상은 재측정으로 확인해야 한다.
- 열 상태를 **직접 재지 않았다**(클럭·온도 미기록). §12.2 의 원인은 **가설**이다.
- CUDA 는 스레드 개념이 없어 CPU 와 직접 비교 시 조건이 대칭이 아니다.

## 12.7 재현 명령

```
python scripts\bench_infer.py --device cpu  --threads 1 --max-new 128 --reps 3 --drop-latent
python scripts\bench_infer.py --device cpu  --threads 1 --max-new 128 --reps 3 --drop-latent --int8-store
python scripts\bench_infer.py --device cuda              --max-new 128 --reps 3 --drop-latent
python scripts\bench_infer.py --device cuda              --max-new 128 --reps 3 --drop-latent --int8-store
python scripts\mem_runtime.py --device cpu --max-new 32 --drop-latent --int8-store
```

---

# §13. **단계 3C 1차 — 대조군이 구현 결함을 잡았다.** 가설은 살아 있다 (2026-08-06)

- **로그**: `016_log_20260806_P034-stage3c.txt`   - **배치**: `run_P034_stage3c_unpackcache.bat`
- **실행**: 15:03:38 ~ 15:09:34, 단독
- **설계 근거**: [`docs/20260806_레이어타이잉-메모리와속도-원인분석.md`](../docs/20260806_레이어타이잉-메모리와속도-원인분석.md) §5

## 13.0 ★한 줄 결론

> **`p6d`(대조군)가 13.85 → 8.26 tok/s 로 40% 느려졌다.** 배치 헤더가 미리 적어 둔 판정 규칙은
> *"p6d 가 움직이면 모델이 아니라 측정이 틀린 것"* 이었고 — **이번엔 측정도 아니고 구현이었다.**
> **원인 확정: 캐시가 ① 공유되지 않는 모듈까지 켜졌고 ② 버퍼를 다음 forward 까지 들고 있었다.**
> dense 는 적중이 **0**인데 **fp32 20층분(472.5MB)** 을 상주시켰다 — P034 단계2·3 이 없앤 바로 그 사본을
> 되살린 것이다. **가설(§5)은 반증되지 않았다.** 구현을 고치고 다시 잰다.

## 13.1 게이트 — 통과. 즉 **정확성 문제가 아니다**

| 모델 | `max\|Δlogit\|` | 판정 |
|---|---|---|
| `p6d` / `mC` / `mA` | 전부 **`0.000e+00`** | ✅ 통과 |

캐시는 **직전에 계산한 그 텐서**를 돌려주므로 수치가 바뀔 여지가 없다. 설계대로다.
**이건 자원(resource) 결함이지 계산 결함이 아니다.**

## 13.2 속도 — 대조군이 무너졌다

**CPU 1스레드, int8, `--drop-latent`** (같은 배치 안의 기준선/처리 쌍이라 **비율이 유효**하다):

| 모델 | [2/4] 기준선 | [3/4] 캐시 ON | **비율** | 예측 | 판정 |
|---|---|---|---|---|---|
| **`p6d`(g1)** | 13.85 | **8.26** | **0.60×** | **1.00× (불변)** | 🚫 **대조군 붕괴** |
| `mC`(g8) | 14.48 | 15.18 | 1.05× | 1.51× | ⚠️ 방향만 맞음 |
| `mA`(g4) | 14.25 | 14.29 | 1.00× | 1.41× | ⚠️ 효과 없음 |

**CUDA**:

| 모델 | 기준선 | 캐시 ON | 비율 |
|---|---|---|---|
| **`p6d`** | 39.22 | **33.44** | **0.85×** |
| `mC` | 37.92 | **44.94** | **1.19×** |
| `mA` | 44.02 | 42.59 | 0.97× |

> **CUDA 에서 `mC` 가 +18.5%** 인 것은 **오염된 조건에서도 이득이 보인다**는 뜻이다.
> 그리고 `p6d` 가 CUDA 에서 −15%(CPU 는 −40%)인 것은 **메모리 압력 현상**과 정합한다 —
> GPU 는 대역폭이 훨씬 크므로 같은 양의 여분 버퍼가 덜 아프다.

## 13.3 ★원인 — 코드를 읽어 확정했다 (가설 아님)

1차 구현은 `enable_unpack_cache(True)` 에서 **모든 `TLinear`** 에 세대 카운터를 켰다:

```python
for m in self._tlinear_cache:          # ← 어텐션 q/o/k/v 포함, dense MLP 포함
    m.set_unpack_gen(0 if on else None)
```

그리고 `_wq_from_i8()` 이 결과를 `self._i8_cache` 에 넣은 뒤 **아무도 지우지 않았다.**
세대가 바뀌면 *다음 호출에서* 덮어쓰지만, **그때까지는 계속 살아 있다.**

| 모델 | 공유되는 MLP | 캐시 적중 | **남는 fp32 버퍼** |
|---|---|---|---|
| `p6d` | **0개** (모든 층이 독립) | **0회** | **472.5MB** (삼진 전량) |
| `mC` | 2개(중간 g8) | 중간 14회 | **209.2MB** |
| `mA` | 4개(중간 g4) | 중간 12회 | **245.2MB** |

**`p6d` 는 이득이 0인데 대가만 전부 치렀다.** 그래서 −40%.
그리고 이 대가는 **P034 단계2·3 이 없앤 dequant 사본을 그대로 되살린 것**이다.

## 13.4 ★★계측이 이걸 못 봤다 — 새 함정

`mem_runtime.py` 가 보고한 상주는 **§12 와 똑같은 154.8 / 86.9 / 96.2MB** 였다.

원인: `tensor_mb()` 가 `parameters()` 와 `_wq`/`_i8`/`_alpha` 만 센다.
**파이썬 속성(`_i8_cache`)으로 들고 있는 텐서는 세지 않는다.**

RSS 는 움직였다(다만 할당자 노이즈가 커서 증거로는 보조적이다):

| 모델 | §12(캐시 없음) RSS 해제후 | §13(캐시 ON) | 차 | 이론 버퍼량 |
|---|---|---|---|---|
| `p6d` | 792.7MB | **1164.6MB** | **+371.9** | 472.5 |
| `mC` | 1322.3MB | 1414.2MB | +91.9 | 209.2 |
| `mA` | 1891.5MB | 1935.7MB | +44.2 | 245.2 |

> **★새 함정: "상주 회계" 는 파라미터·버퍼만이 아니라 *의도적으로 들고 있는 모든 텐서* 여야 한다.**
> 이 프로젝트의 1순위 지표가 상주 메모리인데, **그 지표가 신규 코드에 눈이 멀어 있었다.**
> → `tensor_mb()` 에 `_i8_cache` 추가(2026-08-06). 앞으로 상주에 영향을 주는 필드를 만들면
> **`mem_runtime.py` 를 함께 고친다.**

## 13.5 ★가설은 살아 있다 — 데이터로 이득의 크기를 역산한다

1차 실험에는 **두 효과가 섞여 있다**: **(A) 언팩 절감(+)** 과 **(B) 여분 fp32 버퍼 상주(−)**.
`p6d` 는 **A = 0** 이므로 **B 만 단독으로 측정된 셈**이다.

| 단계 | 계산 |
|---|---|
| `p6d` B | 13.85 → 8.26 tok/s = 72.2 → 121.1 ms/tok → **+48.9 ms/tok** (버퍼 472.5MB) |
| 단위 대가 | 48.9 / 472.5 = **0.1035 ms/tok per MB** |
| `mC` B 추정 | 209.2MB × 0.1035 = **+21.6 ms/tok** |
| `mC` 실측 순변화 | 14.48 → 15.18 = 69.1 → 65.9 = **−3.2 ms/tok** |
| **`mC` A 추정** | −3.2 − 21.6 = **−24.8 ms/tok** |
| **수정본 예측** | 69.1 − 24.8 = 44.3 ms/tok = **약 22.6 tok/s (1.56×)** |

⚠️ **"대가가 버퍼 MB 에 선형" 이라는 가정 하나에 전부 걸려 있다.** 캐시 거동은 비선형이고
표본도 1점이다. 그래도 **원래 예측 1.51× 와 독립적으로 같은 자릿수**가 나온 것은 의미가 있다.
**수정본 실측이 정한다.** `mA` 는 같은 방식으로 −25.6 ms/tok 가 나오는데 `mC`(14층 절감)보다
절감 층이 적어(12층) **더 커야 할 이유가 없다** → 이 추정은 `mA` 에서 신뢰도가 낮다.

## 13.6 조치 — 2차 구현 (2026-08-06 완료)

| # | 결함 | 수정 |
|---|---|---|
| ① | 공유 안 되는 모듈까지 캐시 | **층이 2회 이상 참조하는 MLP 의 `TLinear` 에만** 켠다. `Counter(id(l.mlp[0]) for l in self.layers)` 로 판정 → **dense 는 대상이 0개라 종전 경로와 완전히 동일**(코드 경로가 아예 안 켜진다) |
| ② | 버퍼 미해제 | `forward()` 가 **그룹을 벗어나는 순간** `_clear_mlp_unpack()` 호출. 층 스케줄이 `mid_mlps[j//g]` 라 같은 MLP 가 연속으로 오므로 **동시에 사는 버퍼는 MLP 한 벌(약 18.9MB)** 이 상한 |
| ③ | 계측 실명 | `tensor_mb()` 가 `_i8_cache` 를 센다 + `Transformer.unpack_cache_mb()` 신설 |

**로직 검산**(torch 없이 스케줄만 시뮬레이션):

| 모델 | 유니크 MLP | **캐시 대상(공유)** | **동시 최대 버퍼** | MLP 언팩 횟수 |
|---|---|---|---|---|
| `p6d` | 20 | **0** | **0벌** | 20/20 (**불변**) |
| `mA`(g4) | 8 | 4 | **1벌** | **8**/20 |
| `mC`(g8) | 6 | 2 | **1벌** | **6**/20 |

## 13.7 판정

| 항목 | 판정 |
|---|---|
| 언팩 캐시가 타잉을 속도 레버로 만드는가 | ⏸ **미확정.** 1차는 구현 결함으로 무효 |
| 로짓 동등성 | ✅ 통과(`0.000e+00` 3/3). **정확성은 문제없다** |
| 대조군 설계 | ✅ **작동했다.** `p6d` 를 안 넣었으면 "mC +4.8%" 를 이득으로 기록할 뻔했다 |
| §12 의 속도 수치 | 영향 없음(기준선 [2/4]가 §12 와 1~2% 안) |
| 상주 회계 | ⚠️ **결함 확인·수정.** 파이썬 속성 텐서를 안 셌다 |

## 13.8 후속

1. **[필수] 2차 실행** — `run_P034_stage3c2_unpackcache.bat`. 게이트에 **`p6d` 불변 검사**를 추가했다.
2. **[중] 상주 회계 감사** — `_i8_cache` 말고도 파이썬 속성으로 텐서를 드는 곳이 더 있는지 전수.
3. **[저] CUDA `mC` +18.5%** — 오염 상태에서도 나온 값이라 수정본에서 더 커질 수 있다. 함께 잰다.

## 13.9 한계

- §13.5 의 역산은 **"대가 ∝ 버퍼 MB" 가정 1개 + 표본 1점**이다. 자릿수 판정으로만 쓴다.
- RSS 는 할당자 때문에 과대·불안정하다. §13.4 의 증거력은 **방향뿐**이다.
- 배치 1 단일 스트림 디코드. 학습·메모리 절대값은 **아무것도 바뀌지 않았다.**

## 13.10 재현 명령

```
python scripts\mem_runtime.py --device cpu --max-new 32 --drop-latent --int8-store --unpack-cache
python scripts\bench_infer.py --device cpu --threads 1 --max-new 128 --reps 3 --drop-latent --int8-store
python scripts\bench_infer.py --device cpu --threads 1 --max-new 128 --reps 3 --drop-latent --int8-store --unpack-cache
python scripts\bench_infer.py --device cuda --max-new 128 --reps 3 --drop-latent --int8-store
python scripts\bench_infer.py --device cuda --max-new 128 --reps 3 --drop-latent --int8-store --unpack-cache
```

> ⚠️ 위 명령을 **2026-08-06 이후 코드**로 돌리면 1차 결과가 재현되지 않는다(그게 수정의 목적이다).
> 1차를 재현하려면 커밋 이전 리비전이 필요하다.

---

# §14. **단계 3C 2차 — 타잉이 처음으로 속도를 벌었다.** 그리고 게이트가 잔여 결함을 잡았다 (2026-08-06)

- **로그**: `016_log_20260806_P034-stage3c2.txt`   - **배치**: `run_P034_stage3c2_unpackcache-done.bat`
- **실행**: 17:43:56 ~ 17:48 경, 단독

## 14.0 ★한 줄 결론

> **`mC` +17.1% / `mA` +11.3%** — 타잉이 **처음으로 속도 축에서 이득**을 냈다.
> **그런데 `p6d`(대조군)가 또 움직였다**(15.24 → 11.76, **0.77×**), 그리고 이번엔
> **메모리 게이트가 이유를 정확히 짚었다** — 캐시 ON 에서 상주가 **154.8 → 267.3MB**.
> 잔여 결함은 **어텐션 TLinear 까지 무장된 것**이고 **바이트 단위로 확인**했다(§14.3).
> **가설은 이제 확인 단계에 들어갔다** — 대가를 걷어내면 **`mC` 약 1.7×** 로 추정된다.

## 14.1 속도 — 같은 런 안의 기준선/처리 쌍이라 비율이 유효하다

**CPU 1스레드, int8, `--drop-latent`**

| 모델 | [3/5] 캐시 OFF | [4/5] 캐시 ON | **비율** | 1차(§13) |
|---|---|---|---|---|
| **`p6d`(대조군)** | 15.24 | **11.76** | **0.77×** 🚫 | 0.60× |
| **`mC`(g8)** | 16.06 | **18.76** | **1.17×** ✅ | 1.05× |
| **`mA`(g4)** | 15.45 | **17.19** | **1.11×** ✅ | 1.00× |

**CUDA**

| 모델 | OFF | ON | 비율 |
|---|---|---|---|
| `p6d` | 39.04 | 36.56 | 0.94× |
| `mC` | 43.42 | 43.75 | 1.01× |
| `mA` | 45.08 | 44.06 | 0.98× |

> **CPU 에서 타잉 모델이 확실히 빨라졌다**(1차에서는 mA 가 1.00× 였다).
> **CUDA 는 거의 평평하다** — 대역폭이 남아 언팩이 병목이 아니기 때문으로 보인다(§12.3 과 정합).

## 14.2 ★메모리 게이트가 작동했다 — §13 에서 고친 계측이 값을 했다

| 모델 | [2/5] 캐시 OFF | [1/5] 캐시 ON | **차이** |
|---|---|---|---|
| `p6d` | 154.8MB | **267.3MB** | **+112.5** |
| `mC` | 86.9MB | **188.2MB** | **+101.3** |
| `mA` | 96.2MB | **197.5MB** | **+101.3** |

배치 헤더가 미리 정한 규칙: *"[1/5] 와 [2/5] 의 상주 MB 는 일치해야 한다. 캐시 ON 쪽이 크면
버퍼가 해제되지 않는 것이고, 그러면 속도는 메모리에서 빌려 온 것이다."*
→ **커야 할 이유가 없는데 컸다.** 게이트가 제 일을 했다.

> **★§13 에서 `tensor_mb()` 에 `_i8_cache` 를 추가하지 않았다면 이번에도 못 봤다.**
> 그때는 RSS 만 흔들렸고 정본 지표는 조용했다.

## 14.3 ★원인 확정 — **바이트가 정확히 맞는다** (추정 아님)

| 모델 | CLA | 어텐션 삼진 파라미터 | fp32 환산 | **로그 잉여** | 차이 |
|---|---|---|---|---|---|
| `p6d` | 1 | 29.49M | **112.5 MiB** | **112.5** | **0.0** |
| `mC` | 2 | 26.54M | **101.2 MiB** | **101.3** | 0.0 |
| `mA` | 2 | 26.54M | **101.2 MiB** | **101.3** | 0.0 |

**잉여분 = 어텐션 `TLinear`(q/o/k/v)의 언팩 캐시와 정확히 일치한다.**

코드에서 확인한 경로:

```python
# enable_unpack_cache(): 공유 MLP 만 무장한다 (여기까지는 맞았다)
for m in self._tlinear_cache: m.set_unpack_gen(None)
for mlp in self._cacheable_mlps: ... sub.set_unpack_gen(0)

# forward(): ★그런데 여기서 전부에 세대를 다시 심었다
for _m in self._tlinear_cache:        # ← 어텐션까지 포함
    _m._unpack_gen = self._unpack_gen  # ← None 을 덮어써 되살린다
```

**어텐션은 층당 1회 호출**이라 캐시 적중이 **0** 이다 — 이득 없이 버퍼만 남는다.
그리고 MLP 캐시는 `_clear_mlp_unpack` 이 지우지만 **어텐션은 지우는 코드가 없다.**

**→ `p6d` 는 이득 0 인데 112.5MB 를 짊어졌다. 그래서 0.77×.**
**같은 계열 결함이 §13 에 이어 두 번째다** — 1차는 "무장 범위", 2차는 "세대 심는 범위".

## 14.4 조치 — 3차 수정 (2026-08-06 완료)

`_armed_tlinears` 목록을 `enable_unpack_cache` 에서 만들고 **`forward()` 는 그 목록만 돈다.**
`p6d` 는 무장 대상이 0개이므로 **루프가 한 번도 안 돌고**, 캐시 경로가 아예 실행되지 않는다.

## 14.5 ★★대가를 걷어낸 추정 — **`mC` 약 1.7×**

`p6d` 는 **이득 0 · 대가만** 이므로 대가의 단위 계수를 다시 측정한 셈이다:

| 단계 | 계산 |
|---|---|
| `p6d` 대가 | 15.24 → 11.76 tok/s = 65.62 → 85.03 ms/tok = **+19.42 ms** (버퍼 112.5 MiB) |
| **단위 대가** | 19.42 / 112.5 = **0.1726 ms/tok per MiB** |
| `mC` 가 낸 대가 | 101.2 × 0.1726 = **+17.47 ms/tok** |
| `mC` 실측 순변화 | 16.06 → 18.76 = 62.27 → 53.30 = **−8.97 ms** |
| **`mC` 언팩 절감** | −8.97 − 17.47 = **−26.44 ms/tok** |
| **3차 예측** | 62.27 − 26.44 = 35.83 ms/tok = **약 27.9 tok/s (1.74×)** |
| `mA` 언팩 절감 | 같은 방식 **−24.02 ms/tok** → **약 24.6 tok/s (1.59×)** |

### ★내부 정합성 검사 — 두 모델이 서로를 지지한다

| 모델 | 절감된 MLP 언팩 횟수 | 절감 총량 | **1회당** |
|---|---|---|---|
| `mC`(g8) | 중간 16층 → 2회 = **14회 절감** | 26.44 ms | **1.89 ms** |
| `mA`(g4) | 중간 16층 → 4회 = **12회 절감** | 24.02 ms | **2.00 ms** |

**서로 6% 안에서 일치**하고, 결과 014 §11.4 의 "int8 이 층당 +2.13~2.24ms" 중
MLP 몫 추정(76.2% → 약 1.7ms)과도 같은 자릿수다.
**독립적인 세 경로가 같은 값을 가리킨다** → 역산을 자릿수 이상으로 믿을 근거가 생겼다.

⚠️ 여전히 **"대가 ∝ 버퍼 MiB" 가정 1개**에 의존한다. 3차 실측이 정한다.

## 14.6 판정

| 항목 | 판정 |
|---|---|
| 언팩 캐시가 타잉을 속도 레버로 만드는가 | ⏸ **거의 확정.** 대가를 지고도 **`mC` +17.1% / `mA` +11.3%** |
| 게이트 3/3 `0.000e+00` | ✅ 통과. **정확성 문제는 두 차례 다 없었다** |
| 대조군 | 🚫 **또 움직였다.** 단 **이번엔 원인이 바이트 단위로 확정**됐다 |
| 메모리 게이트 | ✅ **작동.** §13 의 계측 수정이 즉시 값을 했다 |
| CUDA | ⚠️ **거의 평평**(0.94~1.01×). 대역폭이 남아 언팩이 병목이 아니다 |
| §12·§13 의 다른 수치 | 영향 없음 |

## 14.7 ★교훈 — 같은 계열 실수를 두 번 했다

| 차수 | 결함 | 공통점 |
|---|---|---|
| 1차(§13) | 무장 범위가 넓었다(전 TLinear) | **"어디에 적용되는가" 를 두 곳에서 따로 정했다** |
| 2차(§14) | 무장은 좁혔는데 **세대 심는 루프가 넓었다** | 같은 것 |

> **★규범**: 어떤 기능의 **적용 대상 집합은 한 곳에서만 정한다.**
> 두 군데에서 각각 순회하면 한쪽만 고쳐도 통과해 버린다.
> 지금은 `_armed_tlinears` **단일 목록**이 그 역할을 한다.

## 14.8 한계

- 조건당 1회 측정(reps=3 중위값). §14.5 는 **표본 1점 역산**이다.
- 배치 1 단일 스트림 디코드. **학습·메모리 절대값은 아무것도 안 바뀐다.**
- CUDA 결과는 1회라 §14.1 의 CUDA 열을 서열로 읽지 않는다.

## 14.9 재현 명령

```
python scripts\mem_runtime.py --device cpu --max-new 32 --drop-latent --int8-store --unpack-cache
python scripts\mem_runtime.py --device cpu --max-new 32 --drop-latent --int8-store
python scripts\bench_infer.py --device cpu --threads 1 --max-new 128 --reps 3 --drop-latent --int8-store
python scripts\bench_infer.py --device cpu --threads 1 --max-new 128 --reps 3 --drop-latent --int8-store --unpack-cache
python scripts\bench_infer.py --device cuda --max-new 128 --reps 3 --drop-latent --int8-store
python scripts\bench_infer.py --device cuda --max-new 128 --reps 3 --drop-latent --int8-store --unpack-cache
```

> ⚠️ **3차 수정 이후 코드로는 이 결과가 재현되지 않는다**(그게 수정의 목적이다).
> 재실행 배치 = `run_P034_stage3c3_unpackcache.bat`.

---

# §15. **단계 3C 3차 — 확정. 타잉이 속도 레버가 됐다** (2026-08-06)

- **로그**: `016_log_20260806_P034-stage3c3.txt`   - **배치**: `run_P034_stage3c3_unpackcache-done.bat`

## 15.0 ★한 줄 결론

> **`mC` +42.7% / `mA` +33.7%, 그리고 대조군 `p6d` 는 −2.5%(±3% 밴드 안).**
> 세 번 만에 **대조군이 버텼다.** 로짓 게이트 3/3 `0.000e+00`, **상주 MB 도 ON/OFF 완전 일치**.
> **P034 단계3C 확정 — 타잉은 이제 메모리 축뿐 아니라 속도 축의 레버다.**

## 15.1 속도 (CPU 1스레드, int8, 같은 런 안의 쌍이라 비율 유효)

| 모델 | [3/5] OFF | [4/5] ON | **비율** | 3C2(§14) | 예측(§14.5) |
|---|---|---|---|---|---|
| **`p6d`(대조군)** | 12.86 | **12.54** | **0.975×** ✅ | 0.77× | **1.00×** |
| **`mC`(g8)** | 13.36 | **19.07** | **1.427×** ✅ | 1.17× | 1.74× |
| **`mA`(g4)** | 13.61 | **18.19** | **1.337×** ✅ | 1.11× | 1.59× |

**CUDA**: `p6d` 0.967× / `mC` 0.987× / `mA` 1.026× — **거의 평평**(§12.3·§14 와 일관).

## 15.2 게이트 — **둘 다 통과**

| 게이트 | 결과 |
|---|---|
| 로짓 동등성 | `p6d`·`mC`·`mA` 전부 **`0.000e+00`** ✅ |
| **상주 MB (ON vs OFF)** | **154.8 / 86.9 / 96.2 = 완전 일치** ✅ |

**§14 에서 +112.5·+101.3MB 로 벌어졌던 것이 사라졌다** — 어텐션 캐시가 더는 안 생긴다.
그리고 §14.2 의 판정 규칙("캐시 ON 쪽이 크면 속도는 메모리에서 빌려 온 것")을 **이번엔 통과**했다.

## 15.3 ★예측 대조 — 방향은 맞고 크기는 과대였다

| 항목 | 예측 | 실제 | 판정 |
|---|---|---|---|
| `p6d` 불변 | 1.00× | **0.975×** | ✅ **적중**(±3% 밴드) |
| `mC` | 1.74× | **1.427×** | ⚠️ **과대** — 방향·자릿수 적중, 크기 −18% |
| `mA` | 1.59× | **1.337×** | ⚠️ 같은 방향 |

**왜 과대였나**: §14.5 의 역산이 **"대가 ∝ 버퍼 MiB" 를 선형으로 가정**했고 그 계수를
**`p6d` 표본 1점**에서 뽑았다. 실제 대가는 그보다 작았고, 그만큼 절감분 추정이 부풀었다.

### ★그런데 층당 값은 이번에도 일치한다

| 모델 | 절감 총량 | 절감 언팩 횟수 | **1회당** |
|---|---|---|---|
| `mC`(g8) | 74.85 → 52.44 = **−22.41 ms/tok** | 중간 16 → 2 = **14회** | **1.60 ms** |
| `mA`(g4) | 73.48 → 54.98 = **−18.50 ms/tok** | 중간 16 → 4 = **12회** | **1.54 ms** |

**서로 4% 안**이고, 결과 014 §11.4 의 MLP 몫 추정(층당 +2.24ms × 76.2% ≈ **1.7ms**)과도 일치.
**독립 3경로가 세 번째로 같은 값을 가리킨다.**

> **★교훈**: **역산으로 얻은 "비율"은 과대였지만 "층당 절대값"은 정확했다.**
> 앞으로 예측은 **비율이 아니라 층당 ms 로** 세운다.

## 15.4 ⚠️ 기준선이 또 흔들렸다 — 그래서 비율로 읽는 설계가 옳았다

| 조건 | 3C2 [3/5] | 3C3 [3/5] | 차 |
|---|---|---|---|
| `p6d` int8 OFF | 15.24 | 12.86 | **−15.6%** |
| `mC` int8 OFF | 16.06 | 13.36 | **−16.8%** |
| `mA` int8 OFF | 15.45 | 13.61 | −11.9% |

**절대값이 12~17% 흔들린다.** 열·부하 상태 차이다(§12.5 가 이미 규범화).
**같은 런 안에 기준선과 처리를 함께 넣었기 때문에 비율은 살아남았다** —
이 배치 설계가 아니었으면 3C3 을 "3C2 보다 느려졌다" 로 오독했을 것이다.

## 15.5 판정

| 항목 | 판정 |
|---|---|
| 언팩 캐시가 타잉을 속도 레버로 만드는가 | ✅ **확정.** `mC` **+42.7%** / `mA` +33.7% |
| 대조군 | ✅ **버텼다**(0.975×, ±3% 안) |
| 정확성 | ✅ 로짓 `0.000e+00` 3/3 |
| **메모리 무손상** | ✅ **상주 MB ON/OFF 완전 일치** |
| CUDA | ⚠️ **이득 없음**(0.97~1.03×). 대역폭이 남아 언팩이 병목이 아니다 |
| 기본값 | **유지: off.** `--unpack-cache` 로 켠다(추론 전용) |

## 15.6 ★그래서 무엇이 바뀌나

1. **`CLAUDE.md` 의 "타잉은 컴퓨트를 안 줄인다" 에 예외가 확정됐다** —
   GEMM 은 안 줄지만 **언팩은 유니크 모듈당으로 묶인다.** 이득이 **g 에 비례**한다(g8 > g4 > g1=0).
2. **int8 경로의 대가가 완화됐다**: `mC` 기준 단계2 대비 감속이 −67% → **약 −53%**.
   여전히 크지만 "RAM 부족 시 마지막 수단" 에서 **한 걸음 나왔다.**
3. **`mC` 가 `mA` 를 또 앞선다**(1.427× vs 1.337×) — 이득이 g 에 비례하므로 구조적이다.
   REVIEW1 의 mC 권고에 **속도 축 근거가 하나 추가**됐다.
4. ⚠️ **P014(LUT 커널)가 오면 이 이득은 흡수된다** — 거긴 언팩 자체가 없다.
   **3C 는 P014 가 실패하거나 늦어질 때의 보험**이고, 그 값이 방금 확정됐다.

## 15.7 세 번 걸린 이유 — 기록

| 차수 | 결함 | 무엇이 잡았나 |
|---|---|---|
| 1차(§13) | 무장 범위가 전 TLinear | **대조군 `p6d` −40%** |
| 2차(§14) | 무장은 좁혔는데 **세대 심는 루프가 넓었다** | 대조군 −23% + **메모리 게이트**(§13 에서 고친 계측) |
| **3차(§15)** | — | ✅ |

**둘 다 "적용 대상 집합을 두 곳에서 따로 정한" 실수**다.
`_armed_tlinears` **단일 목록**으로 통일한 뒤 통과했다.

## 15.8 한계

- 조건당 1회(reps=3 중위값). **절대값은 12~17% 흔들린다** — **비율만** 인용한다.
- 배치 1 단일 스트림 디코드. **학습은 아무것도 안 바뀐다.**
- CUDA 이득 0 — GPU 배포에는 의미가 없다.
- 이득은 **int8 저장(`--int8-store`) 경로 전용**이다. fp32 경로에는 언팩이 없다.

## 15.9 재현 명령

```
python scripts\mem_runtime.py --device cpu --max-new 32 --drop-latent --int8-store --unpack-cache
python scripts\mem_runtime.py --device cpu --max-new 32 --drop-latent --int8-store
python scripts\bench_infer.py --device cpu --threads 1 --max-new 128 --reps 3 --drop-latent --int8-store
python scripts\bench_infer.py --device cpu --threads 1 --max-new 128 --reps 3 --drop-latent --int8-store --unpack-cache
python scripts\bench_infer.py --device cuda --max-new 128 --reps 3 --drop-latent --int8-store
python scripts\bench_infer.py --device cuda --max-new 128 --reps 3 --drop-latent --int8-store --unpack-cache
```
