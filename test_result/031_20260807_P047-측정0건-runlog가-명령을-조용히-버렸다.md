# 031 — P047: **측정 0건. `runlog.py` 가 명령을 조용히 버렸다**

- **계획**: [P047](../test_plan/P047_표준코퍼스-ko-en-의-스팸률.md) 단계0
- **로그**: [`031_log_20260807_P047-stage0.txt`](031_log_20260807_P047-stage0.txt) — **84바이트**
- **배치**: `run_P047_stage0_spam_rate.bat` (야간큐 v2 [1/4], 01:12 이전)

---

## 0. 한 줄

> **로그 전체가 두 줄, 84바이트다.** 두 단계 모두 **종료코드 0 = "성공"** 으로 끝났고
> 큐는 다음 실험으로 넘어갔다. **측정은 한 건도 일어나지 않았다.**
>
> 원인: `runlog.py` 에 **`--note` 와 실행 명령을 한 줄에** 줬다. 구현이
> `if a.note: ... return 0` 으로 **명령을 버리고 0 을 돌려준다.**
> **P045·P046·P048 은 두 줄로 나눠 써서 정상 동작했다 — 4개 중 1개만 틀렸다.**

로그 전문:

```
[1/2] POSITIVE CONTROL ko-edu-en - must find spam
[2/2] ko-en the standard corpus
```

`--note` 로 준 **제목 두 줄뿐**이다. `runlog` 세션 배너(`====` / `cmd=`)조차 없다 —
**자식 프로세스가 아예 시작되지 않았다는 뜻**이고, 그게 진단의 결정적 단서였다.

---

## 1. 원인 — 코드 경로

```python
# scripts/runlog.py (수정 전)
if a.note:
    ...                      # 텍스트만 쓴다
    return 0                 # ★여기서 끝난다. cmd 는 읽지도 않는다
```

배치가 쓴 것:

```
python scripts\runlog.py --name P047-stage0 --note "[1/2] ..." -- python scripts\diag_spam_rate.py ...
                                            ^^^^^^^^^^^^^^^^^^^   ^^ 이 뒤가 전부 무시됐다
```

P045·P046·P048 이 쓴 것(정상):

```
python scripts\runlog.py --name P045_mC_g16 --note "[1/2] train"
python scripts\runlog.py --name P045_mC_g16 -- python run100m.py train ...
```

---

## 2. ★왜 기존 게이트 4개가 전부 통과했나

| 게이트 | 결과 | 왜 못 잡았나 |
|---|---|---|
| `lint_bat.py` | ✅ 0건 | 문법·ASCII·`pause`·`--tag` 를 본다. **인자 조합의 의미는 안 본다** |
| `check_batch_flags.py` | ✅ 0건 | *"배치가 쓰는 플래그가 파서에 있는가"* — `--note` 도 `--data` 도 **다 있다** |
| `check_call_kwargs.py` | ✅ 0건 | `cli.py` 호출 키워드만 본다. `runlog` 는 대상 아님 |
| **배치의 `if errorlevel 1`** | ✅ 통과 | **`runlog` 가 0 을 돌려줬다.** 실패로 볼 근거가 없었다 |

> **★`CLAUDE.md` 함정 10 이 경고한 것과 정확히 같은 구조의 다른 얼굴이다.**
> 함정 10 = *"파서에 있는데 **전달이 안 되는** 결함은 못 잡는다"*.
> 그때는 `--doc-filter`(2026-08-06)였고, 이번은 `--note` + 명령이다.
> **둘 다 "이름은 맞는데 실행 경로가 다르다".**
>
> ⚠️ **그리고 이번이 더 나쁘다** — `--doc-filter` 때는 프로그램이 **죽어서** 알았다.
> 이번은 **성공으로 끝났다.** **조용한 무동작이 가장 나쁜 실패 양식**이다.

⚠️ **`runlog._verify()` 는 84바이트를 찍었다.** 0바이트만 경고하도록 만들어 놨다.
**"파일이 있고 크기가 0 이 아니다" 를 성공으로 취급한 것**이 두 번째 사각지대다.

---

## 3. 조치 — 3중 방어 (구현 완료)

| # | 어디 | 무엇 |
|---|---|---|
| **1** | `runlog.py` | **`--note` 와 명령을 함께 주면 종료코드 2 로 거절**한다. 무엇이 문제이고 어떻게 나누는지 4줄로 안내. **회귀 검증 통과**(거절 확인) |
| **2** | `lint_bat.py` **규칙 9c** | 배치 작성 시점에 잡는다 — `runlog.py` + `--note` + ` -- ` 가 한 줄에 있으면 **에러**. 실행 전에 걸린다 |
| **3** | `runlog.py` + `lint_bat.py` **규칙 9d** | **`--name` 에 계획번호(P0NN)가 없으면 경고.** §4 참조 |

★ 방어를 **도구(1)와 린터(2) 양쪽에** 둔 이유: 린터는 사람이 돌려야 하고, 도구는 항상 돈다.
`pause` 사고(결과 028 §8)에서 *"가드는 작성자가 기억했을 때만 붙는 장치"* 라고 적었는데,
**같은 교훈을 하루에 두 번 쓰고 있다.** 그래서 이번엔 실행 경로 자체에 넣었다.

---

## 4. 부수 조치 — 로그 파일명에 계획번호 (사용자 지적)

야간큐 v2 는 `--name mC_g16` / `mC_e128` / `mC_p1c1` 로 돌았고, 로그가
`log_20260807_mC_g16.txt` 가 되어 **파일명만으로 어느 실험인지 알 수 없었다.**
사용자가 `029_log_20260807_P045_mC_g16.txt` 로 **손수 고쳐야 했다.**

| 조치 | 내용 |
|---|---|
| 배치 4개 수정 | `--name P045_mC_g16` · `P046_mC_e128` · `P048_mC_p1c1` · `P047_stage0` (각 6곳) |
| `runlog.py` | `--name` 이 `P\d{3}` 으로 시작하지 않으면 **경고**(막지는 않는다 — 도구·진단 로그는 P번호가 없을 수 있다) |
| `lint_bat.py` 9d | 최상위 `run_*.bat` 에 한해 경고. `scripts/batch/` 는 `!TL_LOGNAME!` 로 **호출자가** 주므로 대상 아님 |

**규약**: 실험 로그 이름 = **`P{계획번호}_{태그}`**.

---

## 5. 손실과 재실행

| | |
|---|---|
| **GPU** | **0** — 이 실험은 원래 GPU 를 안 쓴다 |
| **벽시계** | 거의 0(즉시 종료) — **야간큐의 나머지 3단계는 정상 진행됐다** |
| **실질 손실** | **P047 의 답을 하루 늦춘 것.** 그게 남은 레버 1위였다 |

> **★야간큐 설계가 손실을 막았다.** *"각 단계는 경고하고 계속한다"* 규약 덕분에
> P047 이 죽어도 P046·P048·P045 **10.5시간이 그대로 돌았다.**
> 종전처럼 `goto ERROR` 였으면 **하룻밤 전체를 잃었다.**

**재실행**: `run_P047_stage0_spam_rate.bat` (수정 완료). GPU 0, 20~60분.
**야간큐 v3 의 [1] 로 그대로 남긴다.**

---

## 6. 아직 답이 없는 질문 (P047 의 본체)

이 결과문서는 **도구 사고 기록**이고 **실험 결과가 아니다.** 원래 질문은 그대로다:

> **표준 코퍼스 `ko-en` 에 스팸이 있는가.** 있으면 우리 **전 기준선**이 오염된 데이터 위에 있고,
> 그건 REVIEW1·REVIEW2 를 고치는 문제가 아니라 **기준선을 다시 만드는** 문제다.
> 결과 011 §2 가 `ko-edu-en` 에서 같은 조치로 **−0.296 nats(24.7σ)** 를 얻었다.

**REVIEW2 앞에 이것을 먼저 안다** — 판단은 바뀌지 않았다.
