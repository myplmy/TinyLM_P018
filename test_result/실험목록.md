# 실험 목록

측정·실험 결과의 색인. 상세는 각 `{번호}_{작성시간}_{요약}.md` 참조.
새 실험을 기록하면 이 표에 한 줄 추가한다.

| 번호 | 계획 | 요약 | 작성일 | 파일 |
|---|---|---|---|---|
| 001 | P002 | dense vs tied 300M 기준선(+0.132)·격차축소 매트릭스 — **KD+부모초기화가 격차 소멸(+0.001, 메모리 0)** | 2026-07-23 | [001](001_20260723222103_dense-tied-300M-기준선-손실격차.md) |
| 002 | P003 | KD+g=8 — **KD가 g8 격차도 소멸(+0.005, 2.09× 감축)**, 새 최적점 t_kd_g8 | 2026-07-24 | [002](002_20260724233429_P003-KD-g8-2.09x-격차소멸.md) |
| 003 | P014 | 커스텀 커널 — ① 어닐 버그 수정 ② Triton `arange` constexpr 에러 원인확정·수정+폴백1회화 | 2026-07-25 | [003](003_20260725001000_P014-커널-어닐버그.md) |
| 004 | P015 | 오프라인 top-k KD — 고엔트로피 교사(ppl45)엔 부적합(질량 손실), 미사용 권고 | 2026-07-25 | [004](004_20260725003907_P015-오프라인KD-부적합.md) |
| 002+ | P003 | t_kd_g16 극단타잉 — KD로 격차 +0.018 유지, **2.25× 감축**(13.7MB) 새 최공격 | 2026-07-25 | [002](002_20260724233429_P003-KD-g8-2.09x-격차소멸.md) |
| 005 | P017 | Skip-Forward/Dynamic KD — **accum16 재측정(정본): 3런 모두 dense 능가(2.09×↓), 정적 k4가 품질·시간 최적(-0.0366, 141분)**. dyn 우선권장 철회 <br>⚠️ 006과 병기 필요 — dense가 300M 풀 핸디캡 상태였을 가능성(공정풀 k4 확인런 대기) | 2026-07-25 | [005](005_20260725_P017-SkipForward-Dynamic-KD.md) |
| 006 | P007 | 토큰스윕 클린판(600M 단일 풀) — **타잉 격차는 토큰으로 안 닫힘(+0.149/+0.168/+0.166)**, KD 이득은 예산 커질수록 감소(-0.110→+0.062→+0.098), **풀 다양성 2×만으로 dense -0.12** | 2026-07-30 | [006](006_20260730114800_P007-토큰스윕-클린판.md) |
| 007 | P021 | 속도벤치 — **`--no-ckpt` -17.3%(13.5GB)**, k4+압축교사+no-ckpt로 교사 오버헤드 +19%. 250스텝은 `grad_max` 10~35 불안정 구간이라 품질비교 불가<br>**+추가검증 B(P021B)**: micro_bs 폐기 **부분 철회** — 진짜 규칙은 **`M=mb×seq` 무릎 8192**(M=4096이면 -8.7%), **VRAM도 M으로 결정**(8192→13.5GB / 12288→OOM). P013 설계조건 확보 | 2026-07-30 | [007](007_20260730114900_P021-속도벤치-nockpt17퍼센트.md) |
| 008 | P016 | 3:4 희소 삼진 1.25bpw 실측 — **g8_s34 10.3MB(3.00×) +0.0606 / g4_s34 11.7MB(2.64×) +0.0364**, 희소 단독비용 +0.035~0.056에 메모리 1.45×, 시간 영향 없음. compare.py bpw 회계 버그 발견 | 2026-07-30 | [008](008_20260730115000_P016-3대4희소삼진-실측.md) |
| 009 | P012 | 큐레이션 한국어(ko-edu-en) — 관측은 열세(bpb 1.614 vs 1.234)지만 **토크나이저·val셋이 달라 판정 불가**, train/val 3-nat 괴리 이상징후. KD 정칙화 효과는 교차검증(-0.2109) → P028 | 2026-07-30 | [009](009_20260730115100_P012-큐레이션한국어-데이터효율.md) |
| 011 | P028 | 단계0 캐시 진단 + **단계0.5 인과확인**(GPU 추론만) — **H1·H2 함께**이나 **비중이 뒤집혔다**: 분포이동 +1.040 / **미관측 +2.198**(단계0 의 'H2 주범' 판정을 부분 수정). **val 의 23.5%가 단일 문서**(필터링 문제) — 단계0 도구 사각지대. 원문: H2 확인: ko-edu-en val JS가 train 최대의 **9.9×**(대조군 ko-en 1.26×), 한글비율 범위 이탈. H1(중복 5.69% vs 2.03%) 보조. **P012 무효 확정·캐시 폐기**. **말미 val 분할은 전 데이터셋 구조적 결함**(ko-en도 누설 1.8%) | 2026-07-30 | [011](011_20260730135000_P028-단계0-캐시진단.md) |
| 012 | REVIEW1 | 최적안 검증 **완료** — **①σ=0.012**(판정기준 ±0.07 유지). **②🏆후보 A(g4+s34+k4, 11.7MB/2.64×) 승격** — 격차 -0.0042 = dense 와 구분 불가. B(10.3MB) +0.0250 은 성립하나 A에 2.78σ 열세로 탈락, C(14.9MB) -0.0183 은 A와 구분 불가·메모리 27%↑. **③005 헤드라인 유효 확정**(공정풀에서도 k4가 dense 능가). **④희소 단독비용 +0.0433 재확인, 타잉×희소 초선형 경합 재현**(g4→g8 비용이 희소 있으면 7.5배). **⑤"loss 2↔4 진동·val-train +1.6"은 KD 혼합손실 보고 artifact — 학습은 정상, 과적합 없음(val≈train)**. `2σ=0.024 < 0.07` 이라 **판정기준 유지**. 기존 결론 재평가 시 006 타잉격차 16σ·풀효과 11σ·008 s34 3.5~5.8σ 로 **노이즈에 무너지는 결론 없음**. 구 로그 풀은 `bytes_per_token` 역산으로 판별 가능 | 2026-07-30 | [012](012_20260730150000_REVIEW1-최적안검증.md) |
| 013 | P029 | 정성 프로브 1차 — **위키 문체·구조를 확실히 학습**(섹션헤더·카테고리·인물문서 정형 재현). **반복 붕괴가 지배적 실패모드**(t=0.7에서 심함, p6d가 더 심각). 사실은 전부 틀림(예상대로). **mA vs p6d 정성 차이 관측 안 됨 → val_loss 결론과 일치.** "영문 난입"은 문서경계 학습 + `sample()` eos 미정지. 추론 느림 = **KV 캐시 미구현** → P030 | 2026-07-30 | [013](013_20260730190000_P029-정성프로브-1차.md) |
| 010 | P022 | FP8 0단계 게이트 **통과** — 순수 GEMM **1.63~2.08×**(Ada 2:1 비율과 일치), "소형GEMM 무의미" 가설 기각, 환경 문제 없음. **단 GEMM 이 스텝의 50%뿐 → end-to-end 기대 -15% 내외**(상한 -23%). 1단계는 σ 실측·REVIEW1 후로 | 2026-07-30 | [010](010_20260730130000_P022-FP8-0단계게이트-통과.md) |
| 014 | P030 | CPU 추론 실측 — **측정된 것은 추론이 아니라 매 forward 재양자화**(`load_model` 이 `refresh_quant` 미호출). mC 2.0× 는 양자화량 감소, mA 26% 지연은 sparse34 마스크 재계산 <br>**+§8 재실행(2026-07-31): [0/4] 캐시 게이트 실패로 측정 0건.** mA 2/5 불일치·dense 5/5 통과 → **배치가 지목한 RoPE·마스크는 무죄**(dense 와 공유 코드). 가설 H1 수치정밀(bf16 + 다른 SDPA 커널 + 그리디 24스텝 증폭) / H2 CLA 캐시 공유. **근본 원인 = 게이트가 "로짓 동등성" 대신 "그리디 문자열 일치"를 잰다** — 014 에 이은 **두 번째 계측 함정** | 2026-07-31 | [014](014_20260731003000_P030-CPU추론실측-양자화오버헤드.md) |
| 017 | P001B | **LR 재탐색(accum16)** — **`lr 1e-3` 확정**(dense a16 grid 권장 정확히 1.0e-3, `\|g\|max` 5.92). **P033 차단 해제**. ★**배치 의존성 실측**: accum 8→16(유효배치 2배)에 발산 LR 1.91e-3→3.14e-3 = **1.64×**, grid 권장 6e-4→1e-3 = **1.67×** — linear(2.0)과 sqrt(1.41) 사이 → **P033 은 자체 lrfind 필수**. ⚠️ tied grid 권장이 6e-4 로 낮게 나왔으나 **LR 변경 근거 아님** — 60스텝 프로브의 warmup 이 12스텝(실런 100스텝)이라 부풀려졌고, 2289스텝 실런 3건(`p6t`·`mA`·`mC`)이 lr 1e-3 에서 `grad_max` 0.66~1.33 으로 **발산 없음** | 2026-07-31 | [017](017_20260731163000_P001B-LR재탐색-accum16.md) |
| 015+ | P026 | **단계5 완료 = P026 종결.** `qb_wsd60_s85` 3.7086 vs `qb_wsd80_s85` 3.7080 = **+0.0006(0.05σ)** → **정렬 불필요 확정**(2289 에서 1.07σ, 1946 에서 0.05σ — 두 예산 모두 검출 실패). ★**"15% 절감 공짜"는 cosine 기준선일 때만** — wsd 기준선 대비로는 **+0.081 비용**. 두 효과가 상쇄된 것이지 공짜가 아니다. **표준조건 개정: `--sched wsd`, `anneal_end` 0.60 유지, steps 2289 유지.** 부수: 동시 실행(P001B) 중 측정한 런이 **+14.0% 느림** → **속도 비교는 단독 점유에서만** | 2026-07-31 | [015](015_20260731102500_P026-cooldownQAT-스케줄정렬.md) §7 |
| 016++ | P034 | **§8 회계 오류 치명도 판정** — **"전부 철회·재실험" 아니다.** `mem_breakdown()` 은 **보고 전용**이라 `val_loss`·시간·VRAM 영향 **0**, **재학습 필요 런 0개**, **순위도 불변**(세 회계 모두 mA<mC<p6d) — 바뀐 건 **간격**(21%→0.7%). 실질 변화는 ①REVIEW1 승자 근거 ②헤드라인 감축비. 필요한 건 재실험이 아니라 **①코드 단일규약 ②문서 정정 ③Arenas 검증(P036)**. ★**§8.6 논문 본문 확보**: `Y=XTα+λ_t·XW`, `∂L/∂X=(∂L/∂Y)(Tα+λ_tW)ᵀ` — 우리 어닐은 `.detach()` 라 **W 의 gradient 경로가 없다**. ablation 은 **1-bit·1.67-bit 순수 삼진에도** 이득 보고 | 2026-07-31 | [016](016_20260731110000_P034-실행시메모리-순위역전확인.md) §8 |
| 016+ | P034 | **단계1.5 3:4 감사** — ★**"희소"가 아니었다**: 표준 TWN 이 이미 43~46% 를 0 으로 만드는데 3:4 는 0 을 **25% 로 줄인다**(비영 75%). 파라미터 감소 **0** → 상주 차이는 **전부 g4/g8**(9.44M·72.0MB 정확히 일치). ★**bpw 회계 불일치 확인**: 1.95 = 코드1.554+스케일0.125+컨테이너0.274 인데 1.25 는 순수 코드뿐 → **동일회계로 mA 14.6 vs mC 14.7MB = 0.7% 차이**, 감축비 **2.66×→2.11×**(mC 2.10× 불변). ★**논문 재확인 결과 문서 오류 3건**(1.95 는 논문에 없음/"25% 강제-0 손실"은 방향 반대/**Arenas ≠ 우리 어닐, 미구현**). Sherry 실이득은 **비트 25%(1.67 대비)+속도 10%** | 2026-07-31 | [016](016_20260731110000_P034-실행시메모리-순위역전확인.md) §7 |
| 014+ | P030 | **단계1.5 캐시 게이트 재설계** — ★**KV 캐시 정확성 증명 완료**. fp32 teacher-forced `max\|Δlogit\|` **1.1~1.4e-05**·argmax **100% 일치**(15프롬프트), 오차 평탄(RoPE·마스크·CLA 전부 정상). §8 의 게이트 실패는 **오경보**. bf16 오차 0.125~0.219 = **bf16 ULP 1~2개**(더 작을 수 없는 값)이고 **dense 가 오차 최대인데 통과** → 실패를 가른 건 **near-tie 빈도**. 분기점 top-2 간격 0.0625/**0.0000(완전 동점)**. **단계2 진행 가능** | 2026-07-31 | [014](014_20260731003000_P030-CPU추론실측-양자화오버헤드.md) §9 |
| 011+ | P028 | **단계0.6 문서단위 val 분해** — ★**수정 범위가 `ko-edu-en` 으로 한정**됐다(전 데이터셋 재생성 회피). ko-edu-en: 거대 문서 **7개**(각 4.8~5.9%, 손실 8.1~9.6) = 토큰 37%·손실 53%, 상위10 제외 **−1.525** → 폐기·재생성 확정. **ko-en 은 건강**(최대 문서 2.2%, 상위10 제외 +0.020, 중위 3.805≈전체 3.7999) → **무작위 분할 재작성 불필요**. ★**단계0.5 의 "val 23.5% 단일문서(352,399)" 철회** — `eval_slices.py` 가 eos 를 토크나이저 조회 대신 **빈도 추정**(그것도 train 앞 5M 에서)해 경계를 절반만 찾았다. 실제 최대 **88,350(5.9%)**. 도구 수정 완료 | 2026-07-31 | [011](011_20260730135000_P028-단계0-캐시진단.md) §0.6 |
| 016 | P034 | 단계1 실행시(상주) 메모리 — **★순위 역전 확인**(계획 §1.1 예측 적중). 저장 mA 11.7 < mC 14.9 인데 **상주 mC 451.5 < mA 523.5MB**. 상주 = 유니크삼진 × 4B × 2벌(latent+dequant) + 33MB 로 **bpw 가 식에 없다** → 3:4 희소는 저장만 줄인다. **저장 대비 상주 31.6~44.8배**, L3 32MB 상주 가정 **불성립**(저장 기준일 때만 참). 단계2(latent 해제) = **정확히 2배 절감** 확정. ★신규 계측함정: **CUDA 피크는 체크포인트의 AdamW 상태(모델의 3배)를 포함 → 배포 상주량 약 3배 과대**. **REVIEW1 승자 근거 재검토 필요**(품질 동률이면 상주 기준 승자는 mC. mA 우위는 **단계4 패킹 커널이 있어야** 실현) — 사용자 확인 대기 | 2026-07-31 | [016](016_20260731110000_P034-실행시메모리-순위역전확인.md) |
| 015 | P026 | cooldown-QAT 정렬 — **가설 기각, 대조군이 이겼다**. 정렬 고유 기여 **−0.0128(1.1σ) = 구분 불가**, 실제 이득은 **wsd 자체 −0.0755(6.3σ)**. **[4] 15% 스텝 절감은 성립(+0.0050, 0.4σ)** 이나 wsd/정렬 귀속 미확정(**빠진 칸 `qb_wsd60_s85`**). 부수: **grad-ckpt 수치 드리프트 −0.0016(0.13σ) → `--no-ckpt` 는 품질 무료·시간 −17.6%**(사용자가 관측한 "학습 속도 단축"의 전량. 결과 014 `freeze_quant` 수정과 **무관**). **P030 코드 변경의 학습경로 오염 없음**(step0 loss 동일·Δ 0.13σ). ★새 함정: **주기 eval 50it / 최종 eval 100it + best 는 추첨 최소값 → `best` 로 런 비교 금지**(이번에 부호가 뒤집혔다) | 2026-07-31 | [015](015_20260731102500_P026-cooldownQAT-스케줄정렬.md) |
| 014++ | P030 | **단계2B 완료 = 첫 유효 CPU 추론 측정.** ★헤드라인 **CPU 1스레드 31~35 tok/s**(132M 삼진). ★**메모리→속도 전이 거의 없음**(상주 기준 전이율 mC 9.4% / mA 3.4%) — dequant 후 fp32 GEMM 이라 당연하며 **R8(5비트 패킹)의 실측 근거**. 단 **속도 순위는 상주를 따라간다**(mC^>mA^>p6d = 상주 순위, 저장 순위와 반대) → 결과 016 예측 방향 적중. ★**KV 캐시 이득의 부호가 디바이스마다 다르다**: CPU t1 **6.4~6.9×** vs **CUDA 0.88~0.95×(손해)**. ★**스레드는 디코드를 가속하지 않는다**(1→8에서 tok/s −4%, **TTFT 는 −42%**) — prefill 만 병렬화된다. mA −7.4%(mC 대비)는 유니크 파라미터 17% 차이와 정합 → 014 §1 의 −26% 인공물 소멸 확인. ★신규 계측결함: `bench_infer.py` 의 `배포MB` 가 **하드코딩 리터럴**(구 규약 30.9/14.9/11.7)이라 같은 로그에 두 개의 크기가 찍혔다 — 단일 소스로 수정 | 2026-07-31 | [014](014_20260731003000_P030-CPU추론실측-양자화오버헤드.md) §10 |
| 018 | P037 | **단계1 경계 신뢰도 — 경계는 정상, 원인은 스팸이다.** eos 684→문서 685, **16토큰 미만 0개** → **과분할 없음**(단계0.5 형 도구결함 재발 아님). 거대 문서 7개의 정체 = **자격증 덤프 판매 SEO 스팸**(판매문구 + 무관한 소설 + **단어 중간에 키워드 삽입**), 서명은 **줄바꿈 0.0% + 줄 고유율 100%**(=한 줄짜리 초대형 문서). → **단계2 설계 변경: 길이 상한이 아니라 내용 필터.** 대조 `ko-en` 건강 — 상위 문서 제외 시 손실이 **오히려 +0.0013**(상위 기여 문서가 평균보다 **쉽다**). ⚠️ 단 도구가 ko-en 에 대해 자동 출력한 "**무작위 분할 필요**" 판정문은 **−0.001 에서 발화한 근거 없는 문구** — CLAUDE.md 의 보류 결정이 여전히 옳다 | 2026-07-31 | [018](018_20260731190000_P037-단계1-경계는정상-원인은스팸문서.md) |
| 019 | P036 | **단계1 trapping — 판정 불가, 재실행 필요.** 도구 결함 3건: ①**`torch.quantile` 2^24 한계로 dense 대조군이 크래시** — 혼란요인 제거 장치가 통째로 날아갔다(남은 둘은 g4+3:4 vs g8 로 **3:4 와 g 가 함께 움직인다**) ②**모델마다 다른 shape 의 층을 뽑아 놓고 평균 ER 비교**(ER 은 차원 상한이라 2048차원과 768차원을 같은 평균에 넣으면 뽑기 결과를 잰다) ③**`0 비율` 25.00% 는 3:4 의 구조가 정한 값**이지 관측이 아니다(순환논증). 방향만: mA 골짜기 40.87% vs mC 48.09%, ER/차원 37.34% vs 47.17% = **가설과 일치하나 귀속 불가**. ER 추정 자체는 안정(4배치 vs 12배치 +0.7~1.5%). 3건 모두 수정 → **`run_P036_stage1b_trapping.bat`** | 2026-07-31 | [019](019_20260731190000_P036-단계1-trapping-대조군크래시-재실행필요.md) |
| 016+++ | P034 | **단계2(latent 해제) 완료 = 로드맵 R2.** ★**로짓 동등성 게이트 3모델 전부 `0.000e+00`** — 해제가 비트 하나도 안 바꾼다. **상주 1.86~1.93× 감축**(p6d 978.0→505.5 / mC 451.5→242.2 / mA 523.5→278.2MB), 이론 상한 2.00 대비 93~97%(못 미치는 부분 = 삼진 아닌 33MB 고정분). **저장 MB 불변**(정상). **상주 순위 불변**(mC<mA<p6d). ★**속도는 −0.3~+4.9% = 사실상 무변** — 상주만 1.9배 움직이고 저장·파라미터·연산량을 전부 고정한 **단일축 실험**이라, 014 §10.2 의 "메모리→속도 전이 없음"을 **단독으로 확증**한다. 병목은 상주가 아니라 **dequant 후 fp32 GEMM 연산량** → **R8(5비트 패킹)만이 남은 축**. TTFT 는 −11~16%(prefill 은 대역폭 민감, 1회성) | 2026-07-31 | [016](016_20260731110000_P034-실행시메모리-순위역전확인.md) §9 |
| 019+ | P036 | **단계1B(dense 대조군 복구) 완료 = R4.** ★**3:4 로의 귀속이 강해졌다**: shape 일치 (768,768) ER/차원이 **p6d 50.31 ^> mC 45.20 ^> mA 33.20** — 타잉 강도가 원인이면 g8(mC)이 최저여야 하는데 **g4(mA)가 훨씬 낮다** → 타잉 설명 배제, 남는 변수는 **3:4**. 골짜기도 같은 배치(mA 40.87 만 낮고 mC 48.09 / p6d 46.58 은 붙어 있다). ER 은 4배치에서 이미 수렴(12배치와 ±1.5%). ⚠️ (192,768) 행은 **역전**(mA 최고) — 단일 지표로 단정 금지. ⚠️ 도구 자동판정은 "약한 증거"라 찍었으나 **골짜기가 임계 0.85 에 0.013 못 미친 것**이고 두 지표 방향은 같다 → 그대로 옮기지 않는다. **단계2(Arenas on/off, 4.5h) 근거 성립** | 2026-07-31 | [019](019_20260731190000_P036-단계1-trapping-대조군크래시-재실행필요.md) §8 |
| 020 | P031 | **깊이 외삽 — 확장·축소 둘 다 배포 레버 아님. 종결.** R^>1: 개선 없음(**+0.032@1.25 / +0.235@2.0**, 분해능 초과) — **단 붕괴하지 않았다**(계획서는 발산 예측). R^<1: 급격 저하(mC 최선이 **+0.6@0.75**) → 다이얼 불가. ★**얻은 것은 메커니즘 1건**: R=0.5 에서 **dense 가 mC 보다 +0.83 나쁘다**(분해능의 35배) = **타잉이 깊이 축소에 훨씬 관대**하고 **g8^>g4** 로 공유 강도와 양의 상관. 결과 006 의 "타잉 격차 = 용량 상한" 에 붙일 **메커니즘 후보**(위치 특화를 포기하고 위치 불변성을 얻는다). ⚠️ 배치 게이트 문구 오류 발견: "R=1.0 이 학습로그 final 3.7003 과 일치해야 한다"는 **구조적으로 불가능**(추정량이 다르다. 실제 3.7200). ⚠️ **where=front 만 측정**, kv-reuse 미측정 | 2026-08-01 | [020](020_20260801100000_P031-깊이외삽-확장실패-축소는타잉이관대.md) |
| 021 | P032 | **paired 정밀비교 완료 = R6.** ★**SE 0.0005~0.0007 = σ(0.012)의 1/20** — 계획서가 "98런·196시간" 이라 계산하고 폐기한 판정을 **수분에** 해냈다. **재실행 완전 동일**(3.7543/3.7393/3.7608 두 번) → full-val 결정성 확인. bf16 vs fp32 차이 **0.0002**. ★**서열: mC 3.7393 ^< mA 3.7543 ^< p6d 3.7608.** mA vs mC = **+0.0150(t=28.1, 크롭 승률 75.9%)** — mC 가 이긴다. mC vs p6d = −0.0215(t=−29.3, 78.5%). ★**REVIEW1 입력 변화**: mA 가 앞서는 축이 **저장 MB 하나**로 줄었고 그건 R8 이 있어야 실물이 된다(품질·상주·속도·구현리스크 전부 mC). ⚠️ **paired 는 eval 노이즈만 없앤다** — 시드 1개라 확정 범위는 **'두 체크포인트'이지 아키텍처가 아니다.** 0.0150 은 분해능 0.024 미만 | 2026-08-01 | [021](021_20260801100000_P032-paired정밀비교-mC가품질도앞선다.md) |
| 022 | P035 | **어닐 형태 검출 불가 → P026 과 합쳐 cooldown-QAT 계열 종결.** 2×2 완성(대조군은 015 재사용): linear60 3.6275 / linear80 **3.6147** / step60 3.6159 / step80 3.6225. **네 값이 0.0128(1.07σ) 안**이고 **형태 효과의 부호가 전이점에 따라 뒤집힌다**(−0.0116 / +0.0078, 둘 다 ≤1σ) → 노이즈. P026 은 "정렬" 을, P035 는 "형태" 를 검정했고 **둘 다 검출 실패** → 논문이 상정한 'FP 학습 / 별도 QAT' 경계가 **우리 연속 어닐에는 애초에 없다.** 표준 유지(`wsd` + `anneal-end 0.60` + `linear`). 부수: **계단 어닐이 grad_max 를 2.6배로**(1.036→2.666, 아직 안전) → 더 큰 모델·높은 LR 에서는 비권장. 벽시계 +15% 는 점유 조건 차이로 보고 **인용하지 않는다** | 2026-08-01 | [022](022_20260801100000_P035-어닐형태-효과없음-P026과함께종결.md) |
| 019++ | P036 | **단계2(Arenas on/off) 완료 = R5 종결.** ★**무효**: `ar_on` 3.6583 vs `ar_off` 3.6578 = **+0.0005(0.04σ)**, `best` 는 소수점 4자리까지 동일. **trapping 지표도 안 움직인다**(골짜기 −0.09%p, ER/차원 +0.03%p, shape 4종 전부 ±0.8%p 이내) — 손실보다 **이쪽이 더 중요**하다(기전 수준에서 아무 일도 없었다). → **결과 008 의 3:4 품질 비용은 구조 제약 그 자체로 확정**, 후보 A 의 근거는 **R8(5비트 패킹)만 남았다**. 단계1B 가 관측한 양극화는 여전히 사실이나 **"Arenas 부재로 생긴 병리" 가 아니라 3:4 의 정상 귀결**로 읽어야 한다. 논문(1B/3B·10B 토큰) 대비 우리는 132M·300M 이라 **누적 자체가 없다**는 것이 추정 — 확인은 P033 영역. `--arenas` 구현은 **유지**(기본 off, 비트 동일). 부수: λ_t 가 `grad_max` 0.76→1.25 | 2026-08-02 | [019](019_20260802100000_P036-Arenas종결-trapping은실재하나회복불가.md) §9 |
| 023 | P037 | **단계2 — 필터는 작동, 검증은 무효.** 스팸 **1,959개 / 388.0M자(≈13%)** 제거 후 `ko-edu-en_600000000_filtered` 생성 ✅. **그런데 [2/2] 검증이 옛 캐시를 읽었다** — 출력이 결과 018 과 **소수점까지 동일**(전체 6.0879, 상위10 제외 −1.5245, 최대문서 88,351). 원인: `diag_val_docs.py` 가 `prepare()` 를 필터 없이 불러 `ko-edu-en_300000000` 을 찾는다. **"분리해서 쓰는 것" 과 "분리한 것을 읽는 것" 은 별개 작업인데 전자만 했다.** 결과 018 §4·019 §2 에 이은 **세 번째 같은 계열 사고**(도구가 의도한 대상이 아닌 것을 쟀다) — 이번엔 값이 **너무 똑같아서** 잡혔다. → `--doc-filter` 전달 수정 완료, 재검증 배치 `run_P037_stage2b_verify.bat` | 2026-08-02 | [023](023_20260802100000_P037-단계2-필터는작동-검증이엉뚱한캐시를쟀다.md) |
| 024 | P038 | **wsd 표준에서 후보 재확인 완료 = R7.** ★**서열 불변**: mA−mC 가 cosine +0.0150(t=28.1, 승률 24.1%) → **wsd +0.0136(t=25.9, 승률 24.5%)**. 격차 차이 0.0014, 승률 차이 0.4%p → **스케줄×아키텍처 상호작용 없음**(계획서 예측1 적중). `final` mC_wsd **3.6442** ^< mA_wsd 3.6594. `grad_max` 0.85/1.34 발산 없음. ⚠️ **"cosine 대비 개선" 은 이 로그로 말할 수 없다** — 스케줄과 `anneal_end`(0.60→0.80)와 프리셋이 함께 움직였다. ★**REVIEW1: mA 가 앞서는 축이 저장 MB 0.7% 하나뿐**이고 그마저 R8 필요. 019 §9 가 Arenas 유보까지 없앴다 → **`m100R1c`(mC) 를 기본으로 확정 권고**(단 시드 1개이므로 운영 기본값 선택이지 통계적 승자 선언은 아니다) | 2026-08-02 | [024](024_20260802100000_P038-wsd표준에서도-mC우위-유지.md) |
| 016++++ | P034 | **단계3(int8 저장) 완료.** ★**예측이 소수점까지 적중**: mA 상주 278.2→**96.2MB**(단계1 대비 5.44×), p6d 978.0→154.8(6.32×), mC 451.5→86.9(5.19×). **게이트 3모델 전부 `0.000e+00`**. 삼진 항이 정확히 1/3.88 로 줄고 **남은 33.0MB 는 전부 fp32 임베딩·norm·gate** → **다음 레버는 삼진이 아니라 임베딩**(mC 에서 상주의 38%). ★**속도 −67~71%**(34.55→11.26 tok/s) — 예측한 대가지만 **예측보다 크다**. **단계3 = RAM 부족 시 마지막 수단이지 기본 배포 경로 아님.** ★★**신규 관측(§10.4)**: int8 에서 세 모델이 **11.26/11.72/11.81 = 5% 이내**인데 파라미터는 2.26배 차이 → **런치 오버헤드 바운드 가설**. 사실이면 **R8(패킹 커널)은 메모리만 벌고 속도는 못 번다** → `run_P030_stage4_layerscaling.bat` 로 선검정 | 2026-08-06 | [016](016_20260731110000_P034-실행시메모리-순위역전확인.md) §10 |
| 023+ | P037 | **단계2B 재검증 — 또 실패(측정 0건).** 필터 캐시는 **이번엔 제대로 열렸다**(`[data] ... _filtered`). 그런데 **체크포인트가 없어서** 죽었다: `m100_ko-edu-en_600M_dense.pt`. ★원인 — **`--tokens` 하나가 두 가지를 가리킨다**: `prepare()` 의 **풀 크기**(600M 맞음)와 체크포인트 이름의 **학습 예산**(300M 이어야 함). `CLAUDE.md` 가 이미 경고한 혼동인데 **도구 인터페이스가 강제하지 않았다.** 부수: `set TL_DATA2=` 는 cmd 에서 **undefine** 이라 기본값 `ko-en` 이 되살아나 의도치 않은 런이 끼었다 → 센티널 `none` 도입. **같은 계열 사고 4번째**(018 §4 / 019 §2 / 023 §2 / 023 §7) — 단 이번은 "틀린 값" 이 아니라 "아무 값도 없음" 이라 **인용 위험은 없었다**. 수정: `--ckpt-tokens` 신설 → **`run_P037_stage2c_verify.bat`** | 2026-08-06 | [023](023_20260802100000_P037-단계2-필터는작동-검증이엉뚱한캐시를쟀다.md) §7 |
| 014++ | P030 | **단계4 완료 — 디코드 속도의 구조를 밝혔다.** **시간/토큰 = a + b × 층통과** 로 깨끗한 선형(4점). `a`(고정) 3.28~4.25ms = **모델 밖 비용**(LM 헤드 8.6M MAC ^> 층 하나 6.2M, top-k 32768), R=1.0 에서 **전체의 14~19%**. `b`(층당) p6d 1.163 / mA 1.022 / **mC 0.913 ms**. ★**016 §10.4 의 "런치 바운드" 가설 철회** — 타잉은 저장을 줄이지 **컴퓨트를 안 줄인다**(`report()` FLOPs 식에 `mlp_group` 없음). p6d 와 mC 는 층당 FLOPs 가 **같다** → 속도가 비슷한 건 이상현상이 아니었다. **설명할 것이 애초에 없었으므로 가설도 불필요.** ★남는 관측: **mC 가 층당 21.5% 싸다**, g 순서로 단조(g8^<g4^<g1) → **캐시 지역성** 가설. **타잉은 속도에도 작은 이득**. ★★**int8 이 층당 +2.13~2.24ms**(세 모델 거의 동일) → **R8 은 융합 커널이어야만 의미 있고, 융합해도 메모리만 번다** | 2026-08-06 | [014](014_20260731003000_P030-CPU추론실측-양자화오버헤드.md) §11 |
| 016+++++ | P034 | **★§10 속도 수치 정정(동시 실행 오염).** P034 단계3(04:08:47~04:12:14)이 **P037 단계2B(04:07:56~04:12:04)와 거의 완전히 겹쳤다** — 결과 015 의 "동시 실행 +14%" 가 그대로 걸렸고 **int8 쪽에 −20.7~21.7% 로 더 크게** 걸렸다. 정정: 단계2 **36.78/45.01/42.48**, int8 **14.20/14.89/15.08**(P030 단계4 단독 실행분). int8 감속 −67~71% → **−61.4/−66.9/−64.5%**. **메모리 수치·게이트는 영향 없음**(§10.2 그대로). §10.4 가설은 **철회**(014 §11.3). 재측정 배치 `run_P034_stage3b_remeasure.bat`(선택) | 2026-08-06 | [016](016_20260731110000_P034-실행시메모리-순위역전확인.md) §11 |
| 023++ | P037 | **단계2C — 세 번째 시도 성공. 필터가 작동했다.** 조건 검산 통과(`_filtered` 캐시 + `학습예산 300M` 두 줄). ★**val 손실 6.0879 → 4.4771(−1.6108)**, 최대 문서 88,351→**32,948**, 최대 문서 비중 5.9%→**1.1%**. ★★**부호가 뒤집혔다**: 상위10 제외가 **−1.5245 → +0.0144** — 즉 상위 기여 문서가 이제 **평균보다 쉽다**. 이건 정확히 `ko-en`(+0.0197) 패턴이다 → **val 분포가 건강해졌다.** 대조군 `ko-en` **완전 불변**(3.7999) = 스코핑 정확. ⚠️ **−1.61 을 "필터가 모델을 좋게 했다" 로 읽으면 안 된다** — 모델은 필터 전 데이터로 학습된 그대로고 **val 셋만 바뀌었다**. 뜻은 "**필터 전 손실의 26%가 스팸이었다**". ⚠️ 도구가 또 "무작위 분할 필요"를 오발화(018 §4 와 동일). **P028 단계1 차단 해제** | 2026-08-06 | [023](023_20260802100000_P037-단계2-필터는작동-검증이엉뚱한캐시를쟀다.md) §8 |
| 006+ | P007B | **풀 1200M — 관측은 +0.0614 악화, 그러나 비교가 무효다.** ★`[mix]` 계측(P037 단계3)이 같은 날 원인을 잡았다: **한국어 위키가 약 300M 토큰에서 고갈**되고 `_stream()` 이 조용히 `continue` 해서 **1200M 풀의 믹스는 ko 25.0% / en 75.0%**(100M 에서는 50.6/49.4). 그리고 `prepare()` 의 val 은 **스트림 말미 0.5%** 라 **val 셋의 언어 구성까지 바뀐다** — 실측 val 한글 비율 **300M 풀 13.5% / 600M 5.5% / 1200M 0.0%**. ★**`p12d` 는 한국어가 한 글자도 없는 val 로 채점됐다.** → 🚫**판정 불가**. ★★**결과 006 §2-(1) "풀 2×로 dense −0.12" 도 같은 교락 위에 있다**(전면 철회는 아님 — 1200M 이 가장 낮지 않으므로 val 교체가 전부는 아니다. 다만 **두 효과를 이 데이터로 분리 불가**). 표준조건 "풀 ≥ 2×" 는 **비용 0 이므로 유지**. ★부수: **`bytes_per_token` 이 val 이 아니라 스트림 전체를 잰다** → bpb 최대 1.9% 오차, 보정하면 p6d−p12d 격차가 **+0.0252 → +0.0038 로 판정이 뒤집힌다**. 부수2: `grad_max` 4.31→1.12(풀 크면 반복 노출 감소) | 2026-08-06 | [006](006_20260730114800_P007-토큰스윕-클린판.md) §5 |
| 011+ | P028 | **단계1 공통 원문 bpb 완료 — 결과 009 의 "판정 불가" 해소.** SQuAD context 4,000개(2.97MB)에 서로 다른 토크나이저 모델을 통과. ★**데이터셋 간**: `ko-en` **1.3497** vs `ko-edu-en` **1.5182** = **+0.1685**(분해능 0.008 의 21배). ⚠️**단 "큐레이션이 나쁘다" 로 읽으면 안 된다** — P037 단계3 이 같은 날 **영어 예산이 49.4% vs 33.1% = 1.49배 차이**임을 실측했고 이 벤치는 **영문 전용**이다 → 관측 성립, **귀속 불가**. ★★**모델 간**: `mC` 1.3258 < `mA` 1.3290 < `p6d` 1.3342 — **결과 021·024 의 서열이 코퍼스 밖 텍스트에서 재현**(nats 환산 mC−p6d **−0.0254** vs 021 의 −0.0215, mA−mC **+0.0097** vs +0.0150. 부호 3/3 일치). **서열이 val 셋 특성이 아님을 처음 확인.** 단 mA−mC 는 **분해능 미만이라 서열 안 매긴다**. 부수: `ko-en` 토크나이저가 영문을 **8.1% 더 잘 압축**(4.366 vs 4.038 B/tok) → P039 직접 동기 | 2026-08-06 | [011](011_20260730135000_P028-단계0-캐시진단.md) §1 |
| 023+++ | P037 | **단계3 L1 실측 — "한국어 50%" 는 데이터셋마다 다르고 풀 크기마다도 다르다.** 문서 비율은 두 데이터셋 모두 정확히 50%(49.7~50.3%) → `rng.choice` 는 정상. **토큰 비율**: `ko-en` **50.6/49.4(±0.6%p, 무시 가능)** / `ko-edu-en` **66.9/33.1(16.9%p 이탈)** — `korean-webtext-edu` 문서가 약 2배 길다. **L1 은 버그가 아니라 인터페이스 결함**(`ratio` 가 문서 비율인데 문서들이 토큰 비율로 읽어 왔다). ★★**L4 신규 결함 = 소스 고갈**: `_stream()` 이 `StopIteration` 을 **조용히 `continue`** 해서 한국어 위키 고갈 후 fineweb 이 채운다 → `ko-en` 1200M 풀은 **25.0/75.0**. **L4 는 L1 보다 무겁다** — L1 은 상수 편향이라 런 간 상쇄되지만 **L4 는 풀마다 달라 비교 자체를 깨뜨린다**. 말미 val 분할(**L5**)과 결합해 **val 언어 구성까지** 바꾼다. 즉시 영향: 결과 009·011 §1 귀속 불가, 결과 006 §2-(1) 교락 | 2026-08-06 | [023](023_20260802100000_P037-단계2-필터는작동-검증이엉뚱한캐시를쟀다.md) §9 |
| 016++++++ | P034 | **단계3B 단독 재측정 — 부분 재현. 그리고 CUDA 를 처음 쟀다.** 메모리·게이트는 **소수점까지 재현**(978.0/451.5/523.5 → 154.8/86.9/96.2, `0.000e+00` 3/3). ⚠️**속도는 014 §11 정정값보다 2~9% 낮다**(mA 는 −8.6%). 동시 실행은 아니었고(둘 다 단독) **직전 12.5분 `prepare` 토큰화의 열/부하**가 유력. ★`mA` 만 +2.23ms/토큰 벌어져 열로 설명 안 됨 → **상주 278MB 가 L3 경계에서 가장 불리**하다는 가설(미확정). **정본은 014 §11 유지, 평균내지 않는다.** ★★**CUDA 신규**: int8 감속이 **−12~15%** = **CPU(−62~67%)의 1/5** → **"R8 은 융합 커널 아니면 3배 느려진다" 는 CPU 한정 진술**이다(배포 판정은 불변, 외부 인용 시 디바이스 명시 필수). ★★**GPU 가 CPU 1스레드 대비 1.18배뿐**(42.47 vs 36.09) → 배치1 디코드는 런치 바운드, **이 크기에서 CPU 배포는 타협이 아니라 정답**. ⚠️CUDA 에서 서열 역전(mA>mC>p6d)은 1회 측정이라 해석 보류. ★**규범 개정**: 단독 실행만으로 부족 — **직전 5분 유휴 + 같은 배치에 기준선 조건 포함(비율로 읽기)** | 2026-08-06 | [016](016_20260731110000_P034-실행시메모리-순위역전확인.md) §12 |
| 016+++++++ | P034 | **단계3C 1차 — 실패. 대조군이 구현 결함을 잡았다.** 게이트는 3/3 `0.000e+00` 통과(=정확성 문제 아님). ★**`p6d`(대조군)가 13.85 → 8.26 tok/s = −40%.** 배치 헤더가 미리 적은 규칙("p6d 가 움직이면 측정이 틀린 것")대로 판정 보류. 원인 2건 코드 확인: ①**공유 안 되는 모듈까지 캐시를 켰다** — dense 는 적중 0 인데 **fp32 472.5MB** 상주(단계2·3 이 없앤 사본을 되살림) ②**버퍼를 다음 forward 까지 미해제**. ★★**계측이 못 봤다** — `tensor_mb()` 가 파라미터·`_wq`/`_i8` 만 세고 **파이썬 속성 텐서를 안 센다** → 상주 회계가 신규 코드에 눈이 멀어 있었다(1순위 지표인데). `mC` +4.8% / `mA` +0.3% / **CUDA `mC` +18.5%**(오염 상태에서도 이득). ★**실패 데이터에서 환율을 역산**: 대가 = **0.1035 ms/tok per MB** → `mC` 의 언팩 절감분은 이미 **−24.8 ms/tok** 이 깔려 있었고 수정본 예측 **약 22.6 tok/s(1.56×)**. 수정 완료(공유 모듈만 무장·그룹 이탈 시 해제·`tensor_mb` 보강) → `run_P034_stage3c2_unpackcache.bat` | 2026-08-06 | [016](016_20260731110000_P034-실행시메모리-순위역전확인.md) §13 |
| 006++ | P007B | **단계2 공통 원문 bpb — 부호가 뒤집혔다. 그리고 그것이 답이다.** `p12d` **1.3196** vs `p6d` 1.3342 = **−0.0146 bpb**(분해능 1.8배) = nats **−0.0441(3.7σ)**. ★§5 의 자기 val 비교는 **+0.0614(p12d 나쁨)** 였다 → **부호 반대인데 원인은 하나, 언어 구성**이다(val 은 한국어 0% / 학습은 영어 75%). **같은 결함이 양쪽 끝에서 반대로 나타났다.** → **"1200M 이 더 좋다" 가 아니라 "우리 데이터로는 풀 포화점을 측정할 수 없다" 가 답.** ★**P007 종결 판정**(결과 006 §6.5): 같은 풀 안의 결론(타잉 격차·KD 역스케일링)은 유효, **적정 데이터 스케일은 자원 제약으로 미측정** — 한국어 코퍼스가 약 300M 토큰에서 고갈되어 풀·언어비율·val 이 한 노브에 묶인다. 후속 **P041** 신설(착수 조건 4개 전부 미충족). ⚠️ §2-(1) "풀 2× −0.12" 는 **교락 병기 유지** — P037 단계5(공통 val) 후 기존 체크포인트 재평가로 **사후 분리 가능**하므로 그때까지 철회도 확정도 하지 않는다 | 2026-08-06 | [006](006_20260730114800_P007-토큰스윕-클린판.md) §6 |
| 011++ | P028 | **★단계3 — 필터로 학습하니 모델이 실제로 좋아졌다. −0.296 nats(24.7σ).** 공통 원문 bpb `densef` **1.4124** vs `dense` 1.5182 = **−0.1058 bpb = 분해능의 13배**. 결과 023 §8 이 "필터가 val 을 −1.61 바꿨다(모델은 그대로)" 였다면 이번은 **모델 자체가 좋아진 첫 측정**이다. ★**판정을 val_loss 가 아니라 공통 원문 bpb 로 했기 때문에 유효**하다(필터가 문서를 지우면 스트림이 밀려 val 도 밀린다 = L5. `densef` 4.2019 vs `dense` 6.00125 는 **다른 시험지**라 비교 불가). ★★**§1.1 판정이 바뀐다**: `ko-en` 1.3497 대비 격차가 **+0.1685 → +0.0627** — **63%가 스팸이었다.** 남은 격차는 **영어 예산 교락**(49.4% vs 42.0%). → **결과 009 "큐레이션 한국어 열세" 부분 철회** — 열세의 다수는 큐레이션이 아니라 **코퍼스 오염**이었다. 부수: 신규 구현 2건 작동 확인(`--doc-filter` 학습 전달 ✅ / `bytes_per_token_val` +2.4% = §1.8 예측과 일치 ✅), `ko-edu-en` 믹스도 풀 의존(100M 66.9/33.1 → 300M 58.0/42.0) | 2026-08-06 | [011](011_20260730135000_P028-단계0-캐시진단.md) §2 |
| 016++++++++ | P034 | **단계3C 2차 — 타잉이 처음으로 속도를 벌었다. 그리고 게이트가 잔여 결함을 잡았다.** `mC` 16.06→**18.76(+17.1%)** / `mA` 15.45→**17.19(+11.3%)**. ⚠️**`p6d` 대조군이 또 움직였다**(15.24→11.76, **0.77×**). ★**이번엔 메모리 게이트가 이유를 짚었다** — 캐시 ON 상주 **267.3/188.2/197.5** vs OFF 154.8/86.9/96.2. ★★**잉여가 어텐션 TLinear 캐시와 바이트까지 일치**(p6d 112.5 MiB = 29.49M×4B, mC·mA 101.2 = 26.54M×4B). 원인: `enable_unpack_cache` 는 공유 MLP 만 무장했는데 **`forward()` 가 전 TLinear 에 세대를 심어 어텐션을 되살렸다**(층당 1회 호출 = 적중 0, 순수 대가). **같은 계열 실수 2회**(1차 무장범위, 2차 세대범위) → **적용 대상 집합을 `_armed_tlinears` 한 곳에서만** 정하도록 3차 수정. ★**역산**: 대가 **0.1726 ms/tok per MiB** → `mC` 언팩 절감 **−26.44 ms/tok** → **3차 예측 27.9 tok/s(1.74×)**. 내부 정합: mC 14회 절감 1.89ms/회 vs mA 12회 2.00ms/회(**6% 안**), 결과 014 §11.4 추정 1.7ms 와 동일 자릿수 = **독립 3경로 일치**. CUDA 는 평평(0.94~1.01×) | 2026-08-06 | [016](016_20260731110000_P034-실행시메모리-순위역전확인.md) §14 |
| **025** | P039 | **어휘 활용도 — 축소는 레버가 아니다. 확정.** `ko-en` 597M 전량 스캔 **빈도0 53/32,768 = 0.16%**(그중 다수가 바이트 폴백·특수토큰), `ko-edu-en` 0.11%, 필터 캐시 0.17%. 본문 토큰(한글·라틴·숫자)은 **빈도0 0.0%** — 죽은 것은 기호(7.2%)·CJK기타뿐. → **P034 단계5 선택지 (c) 폐기**(0.16% × 32.0MB = **0.05MB**). ★★**예상 밖 발견: 임베딩 노름이 거꾸로다** — 미사용 중위 **0.8315** vs 사용 **0.5201**(비 **1.599**), 미사용 최소가 사용 중위보다 크다. 설명: **헤드가 임베딩과 tie** 돼 있어 미학습 토큰도 매 스텝 softmax 분모에서 **"이 토큰이 아니다" gradient 만 일관되게** 받아 노름이 단조 증가한다 → **Magikarp 의 "노름이 작으면 미학습" 휴리스틱은 untied 헤드 전제이고 타잉 모델에서는 부호가 반대**다. ⚠️도구 자동 안내문이 이걸 "차이가 작다"로 잘못 서술 → 결과문서에 옮기지 않았다(함정 11). 부수: `ko-edu-en` 토크나이저가 어휘 **62.8%를 한국어**에 배분 → 결과 011 §1.3 의 영문 8.1% 압축 열세가 **설명됨** | 2026-08-06 | [025](025_20260806_P039-어휘활용도-축소는레버가아니다.md) |
| 016+++++++++ | P034 | **★단계3C 3차 — 확정. 타잉이 속도 레버가 됐다.** `mC` 13.36→**19.07(+42.7%)** / `mA` 13.61→**18.19(+33.7%)** / **대조군 `p6d` 12.86→12.54 = 0.975×(±3% 안, 버텼다)**. 로짓 게이트 3/3 `0.000e+00` + **상주 MB ON/OFF 완전 일치**(154.8/86.9/96.2) → §14 의 어텐션 캐시 잉여가 사라졌다. ★**예측 대조**: `p6d` 불변 적중 / `mC` 1.74× 예측 vs **1.427× 실제**(−18% 과대) — 역산이 "대가 ∝ 버퍼 MiB" 를 선형 가정하고 계수를 표본 1점에서 뽑은 탓. ★**그러나 층당 절대값은 또 맞았다**: mC **1.60 ms/언팩**(22.41ms÷14회) vs mA **1.54**(18.50÷12) = **4% 안**, 결과 014 §11.4 추정 1.7ms 와 동일 자릿수 → **교훈: 예측은 비율이 아니라 절대량으로 세운다**. ⚠️기준선이 3C2 대비 **12~17% 흔들렸다**(열·부하) — **같은 런에 기준선을 넣은 설계라 비율이 살아남았다**. CUDA 는 이득 0(0.97~1.03×). 세 번 걸린 원인은 둘 다 **"적용 대상 집합을 두 곳에서 정의"** | 2026-08-06 | [016](016_20260731110000_P034-실행시메모리-순위역전확인.md) §15 |
| **026** | P008 | **★단계0 게이트 실패 — 어닐 LoRA 는 메모리를 안 돌려준다. 6시간을 수 분에 살렸다.** `s(t)→0` 으로 어닐해도 `report()` 가 LoRA 를 계상: 저장 **1.0 vs 0.9MB(+11%)**, 상주 **29.1 vs 26.8MB(+8.6%)**, 파라미터 4.9M vs 4.6M. **P008 의 핵심 주장("배포 메모리 대가 0")이 구현 상태로는 거짓**이다. 원인: `LoRA._scale=0` 은 **forward 기여**를 0 으로 만들지만 `nn.Parameter` 인 D·U 는 그대로 있고 `mem_breakdown()` 이 센다 → **"계산에 기여하지 않는다" ≠ "메모리에 없다"**(저장≠상주, 파서에 있다≠전달된다 와 같은 계열). ★**계획서 §7 이 이 리스크를 미리 적어 두고 그것만 검사하는 값싼 게이트를 단계1 앞에 세운 것**이 그대로 작동했다 — 막은 비용 **6 GPU시간**. 조치 = **(a) `export_deploy.py` 로 LoRA 키를 실제로 제거**(회계만 고치는 (b)는 거짓말). → P043 의 선결 C1 | 2026-08-06 | [026](026_20260806_P008-단계0게이트-어닐LoRA는-메모리를-못돌려준다.md) |
| **027** | P044 | **FiLM 은 KD 조건에서 무효. 종결.** paired **+0.0003**(SE 0.0004, t=0.65, 승률 49.2%) — "못 읽었다" 가 아니라 **거의 정확히 0**(|Δ| = σ의 1/40). full-val `mC_film` 3.6981 vs `mC_wsd` 3.6984. ★**예측 4개 중 P1(−0.005~−0.015 이득)이 빗나갔다.** ✅P3 적중 — 메모리 대가가 **정직하게 계상**됐다(packed 13.05→13.18MB, 기타 항 65,536 파라미터) → **결과 026 의 어닐 LoRA 와 정반대**(거긴 s=0 인데도 계상돼 주장이 거짓이었다). 🚫**P4 빗나갔다** — `grad_max` 0.847 → **1.855(2.2배)**. `h·(1+γ)+β` 의 γ·β 가 학습되며 은닉 스케일이 층마다 움직인다. `t_lora32` 6.20 과 같은 계열 = **층별 조건화 장치는 gradient 를 키운다**. ★★**세 측정이 한 방향**: KD 없음 LoRA −0.0338/FiLM −0.0138 → KD 있음 LoRA+FiLM −0.0065 → **KD+g8+paired FiLM 단독 +0.0003**. **KD 가 층별 조건화의 자리를 이미 차지했다.** ⚠️벽시계 130.6 vs 205.8분은 **다른 세션이라 인용 금지** | 2026-08-06 | [027](027_20260806_P044-FiLM은-KD조건에서-무효.md) |
| 023++++ | P037 | **단계4 — L4 경고가 실제로 떴다. 그리고 부산물이 목적보다 크다.** 경고 3종 전부 발화(`소스 고갈 wikimedia/wikipedia 280.6M 지점` · `요청 400.0M 인데 280.6M 만` · `mix_exhausted`) + 전 소스 고갈 시 **무한루프 대신 정상 종료**(종전 코드는 무한루프). 예측 4/4 적중. ★★**부산물: 우리 한국어 코퍼스는 280.6M 토큰(ko 전용 토크나이저) / 300.2M(ko-en)** — 7% 차이는 압축률. **P041 의 착수 게이트 C4 는 한국어 1.2B+ 였고 우리는 그 23~25%다.** → **"부족하다" 가 추정에서 측정으로 바뀌었다.** 리뷰 부록 R8 의 "적절한 한국어 데이터셋을 마련하지 못해 유보" 에 구체 수치가 붙었다. 부수: `bytes_per_token_val` +1.9% 정상 작동 / ⚠️`ko` 단독인데 **한글이 56.5%뿐**(위키에도 라틴·숫자가 많다) — "한글 비율 ≠ 한국어 비율" | 2026-08-06 | [023](023_20260802100000_P037-단계2-필터는작동-검증이엉뚱한캐시를쟀다.md) §10 |
| **028** | P014C | **단계0 — per-row α 는 사실상 공짜, per-tensor 는 못 쓴다. 게이트 통과.** per-row Δbpb **+0.0050~+0.0063**(4모델 전부 **분해능 0.008 미만**, 여유 21%) → **★커널을 새로 짜지 않아도 될 길이 열렸다**(단계1 로). per-tensor **+0.0465~+0.0862 = nats 0.141~0.261(최대 21.7σ)** → **bitnet.cpp 규약이므로 P014B 중단 권고에 품질 근거가 붙었다** — 빌드가 됐어도 **우리가 아키텍처로 벌어 온 것과 같은 자릿수를 한 번에 잃는다**(스팸필터 −0.296 / KD −0.15 급과 비교). ★**도구 자체검증 4/4**: g128 행 상대오차 **0.00000**(원래 α 복원) — 이게 아니면 나머지 행이 무의미했다. ★**g256 은 완전 공짜**(+0.0010~0.0015 = 분해능 1/5) → `micro_group` 은 **한 번도 스윕 안 한 축**. ★★**부수 소득: per-row 는 저장 레버이기도 하다** — α 는 fp16 `16/g` bpw 이므로 1.7100→**1.6036(−6.2%)**, packed 13.05→**12.25MB(2.21×)**. ⚠️**처음 −13% 로 계산했다가 정정**(α 를 fp32 로 가정 — 정본 1.7100 을 식으로 재현해 보니 안 맞아서 잡혔다, 함정 7). ★**품질↑ → per-tensor 민감도↑**: `mC_wsd`(1.3138) +0.0862 vs `mC_g8_k4`(1.3258) +0.0694 — **아키텍처 동일, 학습만 다르다**(가설, 미검증). `mA` 의 3:4 는 판정과 **직교**(per-row 완전 동일). ⚠️결함 2건 수정 — **g512 행 전부 스킵**(비나눔 텐서 하나가 granularity 전체를 버렸다) · `requires_grad` 경고. **재실행·stage0B 불필요.** ★★**야간큐가 여기서 밤새 멈춘 것은 실험이 아니라 배치 결함** — `run_P014C` 만 **무방비 `pause`**(P045·P046 엔 가드 있음) → `lint_bat.py` **규칙 9b 신설**(무방비 pause = 에러) + 저장소 전체 **18건 가드 추가** | 2026-08-07 | [028](028_20260807_P014C-단계0-per-row는-공짜고-per-tensor는-못쓴다.md) |
| **029** | P045 | **g16 — 분해능 미만. 타잉 축 종결, 채택 가능.** paired **+0.0161**(SE 0.0005, t −33.88, 승률 80.9%) = **분해능 0.024 미만**. packed 13.050→**12.088MB**(감축비 2.07→**2.24×**), fp32 상주 451.5→**415.5MB(−8.0%)**, 삼진 54.85→50.14M. 헤더 검산 `MLP g=16`·`1그룹 16회 재사용` ✅. ★예측 P1(+0.01~0.03) **적중**. ★**예측 밖: `grad_max` 가 오히려 내려갔다**(0.847→**0.738**) — 타잉을 강화하면 gradient 가 더 안정. `n_middle=16` 이라 g16 이 **구조적 상한** → **타잉 강도 축 종결**. ⚠️부모초기화가 16층 평균이 되어 g 효과에 섞이나 Δ 가 분해능 미만이라 귀속 무관. ⚠️트레이너 인쇄 최종 3.6628 은 대조군 full-val 3.6984 와 **다른 추정량**이라 비교 안 했다(함정 4) | 2026-08-07 | [029](029_20260807_P045-g16-타잉축-종결-채택가능.md) |
| **030** | P046 | **E=128 — 🚫판정 불가. `grad_max` 19.32 가 잡았다.** paired +0.1768(SE 0.0011, t −156.31, 승률 **100%**) = 임계 0.08 의 2.2배 — **그런데 아키텍처 판정이 아니다.** ★★세 증거가 초기화를 가리킨다: ①**`grad_max` 19.32**(기준선 0.847의 23배) → `CLAUDE.md` 판정기준 *"Δ>+0.15 는 grad_max 먼저, 10 이상이면 학습 문제"* 에 정확히 걸린다 ②**step 0 `ce` 10.4051 = ln(32768) = 완전 무작위**(같은 밤 `mC_g16` 은 7.7742) ③`[init]` 경고 3줄이 *"임베딩은 난수로 시작"* 을 이미 찍었다. **변수 2개**(E 256→128 **및** 임베딩 부모초기화 O→X)이고 헤드가 tie 라 입·출력이 동시에 무작위였다. ★★**함정 4 가 처음으로 판정을 뒤집었다** — 인쇄 `\|g\|` 는 0.43 으로 **셋 중 가장 안전**해 보였다. ⚠️**자기비판: 배치 WHAT TO RECORD 에 grad_max 를 적어 두고도 `[init]` 경고를 비교유효성 게이트로 세우지 않았다**(결과 026 은 게이트를 세워 6시간을 살렸는데 여기선 2.1h 를 썼다). ✅회계는 적중(⚙int8 상주 −18.8% 예측 70.7 → 실측 **70.56MB**). **조치: SVD 절단 이식 구현**(`W=emb@emb_up` 의 Eckart–Young 최적 랭크근사, numpy 검산 — 상대오차가 하한과 소수점까지 일치 0.405722, 난수는 1.0136) → **재측정하면 판정 가능**. ⚠️단 재측정 우선순위는 낮다(결과 032 §4: packed 기준 최악 레버) | 2026-08-07 | [030](030_20260807_P046-E128은-판정불가-grad_max19가-잡았다.md) |
| **031** | P047 | **측정 0건 — `runlog.py` 가 명령을 조용히 버렸다. 로그 84바이트.** `--note` 와 실행 명령을 **한 줄에** 줘서 `if a.note: ... return 0` 이 **명령을 버리고 종료코드 0** 을 돌려줬다 → 두 단계가 **"성공"** 으로 끝나고 큐가 넘어갔다. **P045·P046·P048 은 두 줄로 나눠 써서 정상 — 4개 중 1개만 틀렸다.** ★★**기존 게이트 4개 전부 통과**(lint·check_batch_flags·check_call_kwargs·`if errorlevel 1`) — **함정 10 의 다른 얼굴**(*"파서에 있는데 전달이 안 된다"*, 2026-08-06 `--doc-filter` 와 동형). ⚠️**이번이 더 나쁘다** — 그때는 죽어서 알았고 **이번은 성공으로 끝났다. 조용한 무동작이 가장 나쁜 실패**다. `runlog._verify()` 도 **0바이트만** 경고하게 만들어 84바이트를 통과시켰다. **조치 3중 방어**: ①`runlog.py` 가 **종료코드 2 로 거절**(회귀검증 통과) ②`lint_bat.py` **규칙 9c**(한 줄 병용 = 에러) ③**규칙 9d + runlog 경고 — `--name` 에 계획번호 필수**(사용자 지적: 야간큐 v2 로그가 `log_..._mC_g16.txt` 라 손수 고쳐야 했다. 배치 4개 24곳 수정, 규약 = `P{번호}_{태그}`). ★**야간큐의 "경고하고 계속" 규약이 손실을 막았다** — P047 이 죽어도 나머지 **10.5시간이 그대로 돌았다**. GPU 손실 0 | 2026-08-07 | [031](031_20260807_P047-측정0건-runlog가-명령을-조용히-버렸다.md) |
| **032** | P048 | **prelude/coda 1+1 — 효율이 g16 과 같다. 그리고 2.6배 크다.** paired **+0.0493**(SE 0.0006, t −77.03, 승률 97.7%) = 예측 대역(+0.02~0.06) **안**. **packed 13.050→10.574MB(−19.0%)**, **fp32 상주 451.5→359.2MB(−20.4%)** = **둘 다 저장소 최대**. 삼진 54.85→**42.76M(−22.0%)**, FLOPs −10.1%, 벽시계 **−7.9%**, KV −9.3%. **G-init 통과**(`[init]` 구조불일치 4줄 + `layers=1+16+1` + 삼진 42.8M). ⚙**계산이 소수점까지 적중**(예측 42.76M/10.47MB/−20.4%). ★★**이 밤의 최대 산출물 = 세 레버 효율 비교**(§4): **g16 0.0167 vs P048 0.0199 nats/MB(packed)** — packed·fp32상주·삼진 **세 지표 전부 1.19~1.20배**로 일치 → **국소 법칙 가설: 0.0035~0.0041 nats per 유니크삼진 100만**. ⚠️**법칙 아니다** — `mC` vs `p6d`(69.0M 차)에 적용하면 +0.255 여야 하는데 실제로는 **mC 가 이긴다**(KD·부모초기화가 메운다). 적용범위 4.7~12.1M. ★★**내 우선순위가 틀렸다** — 야간큐에서 P046 을 앞에 둔 근거가 ⚙**int8** 상주 −18.8% 였는데, **fp32 경로에서는 −3.6% 뿐**이고 결과 016 §10 은 *"int8 은 마지막 수단이지 기본 경로 아님"* 이라 적어 두었다 → **함정 1 의 3번째 얼굴: 상주 안에서 fp32/int8 이 갈린다. 레버 인용 시 배포 경로까지 명시.** 예측 P4(grad_max 불변) **절반 빗나감**(0.847→**1.253**, +48%. 10 미만이라 무해) — FiLM 1.855 와 기준선 사이 → 구분선은 "장치냐" 가 아니라 **"층별 자유도를 건드리냐"**(가설). ⚠️**깊이 20→18 교락 잔존** → "prelude/coda 를 줄인 대가" 로 **귀속 불가**, 배포 판정에는 충분. **A-6 축 닫지 않는다**(임계 0.10 의 절반). **다음 = 단계2 g16+p1c1**(유니크 3, ⚙packed −26.3%/상주 −28.4%, **국소선형 예측 +0.062 검정**) | 2026-08-07 | [032](032_20260807_P048-preludecoda-1+1-효율은-g16과-같다.md) |
| 028+ | P014C | **단계1 — 연산이 있다. G1 통과.** `torch._weight_int8pack_mm` **존재**(torch 2.10.0), **CPU·CUDA 양쪽**, 우리 shape 3종(8×768×2048 / 8×2048×768 / 1×768×768) 전부 동작. ★**이름만 보지 않고 실제 호출**해 규약 확인 — 참조 대비 상대오차 **1.7e-3 = bf16 누산 자릿수**(어긋났으면 0.5급). `_int_mm`·`_scaled_mm`·`mkldnn` 도 가용, **torchao 불필요**. → **커널을 짤 필요가 없다. P014 안 C 보류.** ⚠️"있다"≠"빠르다" — **속도는 단계2**(게이트 = 언팩 캐시 **19.07 tok/s** 초과, 못 넘으면 접는다). ⚠️채택하려면 **per-row 규약으로 재학습**(3.5h) 필요 — 결과 028 표는 비관적 상한 | 2026-08-07 | [028 §11](028_20260807_P014C-단계0-per-row는-공짜고-per-tensor는-못쓴다.md) |
| **031** | P047 | **★`ko-en` 은 깨끗하다. 기준선이 안전하다는 첫 확인.** 스팸 **6 / 366,819 문서(0.00164%)**, **문자 0.06229%**. 양성 대조군 `ko-edu-en` **18.75654%** = **301배 차이**(도구 정상). ★**소스별 귀속**: 한국어 위키 **0.00000%** / fineweb-edu 0.082% — 그리고 `ko-edu-en` 오염의 정체가 **`korean-webtext-edu` 문자 34.4%** 로 확정(결과 011 §2 의 "격차 63%가 스팸" 의 직접 물증). **선형 외삽 기대 이득 −0.00098 nats = 분해능의 1/24** → **단계1·2 안 한다. 종결.** ★★**부정 결과가 이번엔 가장 값어치 있다** — GPU 0·5.7분에 **REVIEW2 착수를 막던 유일한 위험을 제거**했다(양성이었으면 리뷰 수정이 아니라 **전 기준선 재작성**). ⚠️**예측 P5 정반대** — 대형 1,030개 중 줄바꿈조건 통과 **6개** / 고유율조건 통과 **594개** → **서명에서 실제로 일하는 건 "줄바꿈 ~0%" 하나**이고 "세 조건이 함께" 라는 설명은 **실효 과장**. 1차 시도(측정 0건, runlog 사고)는 §6 에 보존 | 2026-08-07 | [031](031_20260807_P047-ko-en은-깨끗하다-기준선이-안전하다.md) |
| 032+ | P048 | **단계2 — 예측이 20% 빗나갔고 그 빗나감이 답이다.** `m100R1q`(유니크 MLP **3**) paired vs `mC_wsd` **+0.0745**(예측 +0.062, 배치가 적어 둔 "+0.070 초과 = 상호작용" 갈래). ⚙메모리는 **또 소수점까지 적중**(삼진 38.0M / packed 9.612MB / fp32 상주 323.21MB). ★★**보조 판정이 기전을 밝혔다** — vs `mC_p1c1` **+0.0252** 인데 **같은 g16 이 `mC` 위에서는 +0.0161** = **1.57배**. 파라미터 수는 같은데 **가격이 다르다** → **대가 함수는 선형이 아니라 볼록(convex)**, 용량을 덜어낼수록 한계비용이 오른다(0.00341 → 0.00534 nats/M). 단순합 대비 **+0.0091 = 13.9% 초가법**. → **결과 032 §4 의 "0.0037 nats/M" 은 평균가격이고 예측에 쓰면 안 된다.** 적용범위가 "4.7~12.1M" 에서 **"+ 유니크 MLP 5~6개 기반"** 으로 좁아졌다. `grad_max` 0.970(건강). 분해능 안은 여전히 **`mC_g16` 하나뿐** — `p1c1g16` 은 메모리 최대(packed −26.3%)지만 **분해능의 3.1배** → 기본 후보 승격 안 함 | 2026-08-07 | [032 §8](032_20260807_P048-preludecoda-1+1-효율은-g16과-같다.md) |
| **033** | P040 | **타잉은 학습시간을 조금 벌고 VRAM 을 많이 번다.** 정상상태 ms/step **2,970.0 → 2,805.8 = −5.53%**(누적평균 환산). ★★**그런데 교락을 찾았다** — `--arch dense` = `dense_baseline()` 은 **`tie_mlp=False` 와 `cla_group=1` 을 동시에** 끈다. KV 소유 **20/20 vs 10/20**. k/v 는 층당 MAC 의 4.76% 이므로 CLA2 몫 **≈2.38%p** → **타잉 단독 ⚙약 −3.15%**. **기준표 §2.1 의 −7.8% 는 과대 → 정정.** ★★**그리고 배치가 세운 게이트 자체가 틀렸다** — 헤더가 *"FLOPs 가 both 0.248 이어야 한다, that is the point"* 라 적었는데 `report()` FLOPs 식에 **`cla_group` 이 없다**(`mlp_group` 이 없는 것과 같은 계열, 결과 014 §11.3). **같은 값이 나온 건 "연산이 같다" 가 아니라 "이 지표가 CLA 를 못 본다"** → 함정 25 신설. ★**진짜 상금 = VRAM reserved 7.82 → 5.06 GB(−35.3%)** — 스필벽 대비 여유 **8~9GB** 확보 = **P042(`--no-ckpt` 개방)의 전제 확인**. AdamW 스텝은 0.13% 라 3.15% 를 설명 못 함 → **캐시 지역성 vs 옵티마이저 대역폭** 미분리 | 2026-08-07 | [033](033_20260807_P040-타잉은-학습시간을-조금-벌고-VRAM을-많이-번다.md) |
| **034** | P042 | **단계0 — 비트 동일. G0 통과. 25초.** `--kd-teacher-infer` on/off 두 런의 **전 인쇄 지표가 마지막 자리까지 일치**(ce·혼합손실·`|g|`·val_loss·ppl·best·final). `[kd] 교사 추론 모드` 두 줄이 ON 런에만 있어 **플래그가 실제로 걸린 것**도 확인 → `freeze_quant()`+`drop_latent()` 는 교사 출력을 안 바꾼다. ✅**G1(초기화 정합)도 함께 통과** — 학생 step0 `ce 9.9432` 가 부모 최종 9.9175 근방이라 **빈 텐서를 복사하지 않았다**(`--init-from` 뒤에 해제하는 순서가 맞다). ⚠️**속도는 판정하지 않았다** — 배치 꼬리말이 *"DO NOT read speed from this"* 라 미리 적어 뒀고 그 지시를 따랐다. 참고값 124.7→99.0 ms/step(−20.6%)이지만 교락 셋(**OFF 가 먼저 돌았다**·tiny 라 교사 비중이 다르다·30스텝). ★**그래도 적어 둔 이유**: 계획 P1 예측이 **−0~5%** 인데 참고값이 그 **밖**이라 **단계1 이 P1 을 기각할 수 있다**. 예측을 미리 세워 두는 것이 목적. VRAM 0.71→0.70GB(약 10MB) vs tiny 교사 latent **12.6 MiB** = 같은 자릿수(P2 방향 일치, 인쇄 분해능 0.01GB 라 그 이상은 못 말한다). ⚠️`[commit] eb2274f +dirty 7개` — **그 커밋으로 검증한 것이 아니다**. 검증 대상(`--kd-teacher-infer` 구현)이 미커밋분에 있었으므로 **검증 자체는 유효**. 단계1(3런 OFF-ON-OFF, 60분)·단계2(`--no-ckpt`, 20분) 배치 작성 완료 | 2026-08-07 | [034](034_20260807_P042-단계0-비트동일성-게이트-통과.md) |
| 034+ | P042 | **단계1·2 — 속도는 측정 불가, VRAM 은 예측대로. `--no-ckpt` 는 "조건부" 로만 열린다.** 정상상태 OFF_A **3474.5** / ON **3566.5** / OFF_B **3521.9** ms → `drift +47.4(+1.37%)` vs `effect +68.3(+1.95%)`, **|drift|/|effect| = 0.69** → 배치가 미리 정한 *"노이즈 이하"* 갈래. **P1 적중.** ★★**그리고 이것이 결과 034 §3.1 의 tiny −20.6% 를 폐기했다** — 순서 교락 하나였고, **3런 설계가 아니었으면 이번에도 +2.6%(OFF_A만) 또는 +1.3%(OFF_B만) 로 다르게 읽었을 것**이다. **VRAM alloc −490.6 MiB**(이론 472.5, 오차 +3.8%) / reserved −1.03 GiB → **P2 적중**. ★**품질: ON final 4.08031 이 두 OFF(4.08000·4.08078) 사이**에 있다 = 처치 효과 < **동일 조건 두 런의 비결정성**(compile+cudnn.benchmark+TF32 라 이 스케일에서 비트 동일 원리적 불가 → 단계0 을 tiny·비compile 로 돌린 것이 옳았다). **단계2**: 스필 금지에서 **OOM 35초**(`kl_div` 가 **1024.00 MiB** 요구 = `8192×32768×4B` 정확히, 단편화 **1.71 GiB**), 스필 허용에서 완주(reserved **15.37 GiB**, 여유 0.62, 정상상태 **2836.1 = −20.5%**, 순간 ms/step **±0.5% = 스필 없음**). → **G3 조건부 통과, 표준 승격 불가.** ★**기준표 §5 의 15.7GB 는 낡지 않았다**(실측 15.37) — 직전 핸드오프 §4-B 의 의심 **철회**. ★★**사용자 질문 답(§11): −35.3%(결과 033)는 "KD 없는 학생 단독" 축이고 여전히 유효하다. 그 위에 KD 교사가 +5.48 GiB(alloc), `--no-ckpt` 가 +4.45 를 얹는다. 그리고 교사 파라미터는 945 MiB 뿐 — 나머지 4.5 GiB 는 "어휘 전체 위의 KD 손실 계산"이다**(회계가 실측과 일치) → `docs/methods/09_training_memory.md` 신설 | 2026-08-08 | [034 §9~11](034_20260807_P042-단계0-비트동일성-게이트-통과.md) |
| **035** | P022B | **★per-tensor 로도 FP8 은 거의 공짜다. 예측 3개가 빗나갔고 그게 결론을 바꿨다.** [A] sm_89/torch 2.10 은 **per-tensor 만** 받는다(rowwise `Rowwise scaling is not currently supported`, blockwise `Invalid scaling configuration` — **3형상 전부**) → **P5 적중**. [B] **g128 자기검증 0.00000** ✅. Δbpb: g256 +0.0002 / per-row **+0.0001** / **per-tensor +0.0005**. [C] 활성 1×128 +0.0002 / per-token +0.0003 / **per-tensor +0.0003**. **[D] 최선 +0.0002, 최악 +0.0005** = 게이트선 0.008 의 **1/40~1/16** → **G0 전부 통과**. ★★★**P2·P3 가 크게 빗나갔다** — per-tensor **E4M3 왕복** +0.0005 vs 결과 028 의 per-tensor **α 재추정** +0.0862 = **173배**. **두 오차원이 완전히 다르다**: E4M3 는 α 를 **보존**하고(α 비가 가수 3비트에 실린다) α 재추정은 α 를 **버린다**(428,544개 → 1개). ★**교훈: 같은 단어 'per-tensor' 가 다른 규약을 가리키고 있었고, 나는 그걸 예측 근거로 섞어 썼다.** P4 도 빗나감(활성이 가중치보다 아플 것이라 했는데 오히려 낫다 — RMSNorm·QK-norm 이 활성 범위를 통제하는 것으로 추정). ★★**결론 전환: 커널이 필요 없다.** DeepGEMM 도 CUTLASS 도 불필요, `torch._scaled_mm(per-tensor)` 만으로 성립하고 그건 결과 010 이 이미 1.63~2.08× 로 쟀다. **단, P6(지금 구현 안 함)은 유지** — 이유가 품질이 아니라 값어치다(메모리 0, −15%, 그리고 **같은 밤 `--no-ckpt` 가 −20.5% 를 구현 0 으로 줬다**). ⚠️누산 오차·backward 경로는 **미측정**(양방향 미지) | 2026-08-07 | [035](035_20260807_P022B-단계0-per-tensor로도-FP8은-거의-공짜다.md) |
| **036** | P051 | **α 그룹 256 은 구분조차 안 된다. 완전히 공짜고 축 종결.** paired **−0.0007**(SE 0.0004, **t −1.65**, 승률 52.5%) = *"구분 불가(체크포인트 수준에서도)"* — **부호가 오히려 음수**이고 유의하지도 않다. packed **13.0503 → 12.5777 MB(−3.62%)**, 감축비 2.07 → **2.15×**, **fp32 상주 451.487 → 451.487 소수점까지 불변**. ★**예측 4/5 적중** — P1(대역 아래끝 밖), **P2 오차 0.04%**, P3, P5(같은 밤 `mC_ct` 134.9분 vs 135.2분 = +0.2%). **P4 만 빗나감**(`grad_max` 0.847→**1.621**, 예측 0.5~1.5) — ⚠️**그러나 귀속 불가**: 같은 밤의 `mC_ct`(micro_group 무관)도 **1.580** 으로 함께 올랐다. *"α 그룹을 키우면 gradient 가 커진다" 고 쓰지 않는다.* ★**직교 레버 확정** — 다른 셋은 유니크 삼진 **개수**를 줄이는데 이건 **파라미터당 비트**를 줄인다 → 결과 032 §8.3 볼록성 **바깥**, `m100R1q` 위에 얹으면 계산상 9.612 → **약 9.26MB**. ⚠️**packed 전용**(상주식에 bpw 없음) — *"메모리 −3.6%" 가 아니라 "저장 −3.6%"*. `768 % 512 ≠ 0` 이라 **g256 이 구조적 상한** = P045 가 g16 에서 닫힌 것과 같은 형태 | 2026-08-07 | [036](036_20260807_P051-알파그룹256은-완전히-공짜다-축-종결.md) |
| **037** | P018 | **★★우리가 써 온 KD 교사가 그 교사로 만든 학생보다 0.11 nats 나빴다.** [1/5] `dense` full-val **3.8080** vs `mC_wsd` **3.6984** → paired **+0.1096**(SE 0.0014, t 78.04, dense 승률 **1.8%**) = 분해능의 **4.6배**. **한 번도 안 재 본 비교였다** — 풀이 달라(300M vs 600M) 함정 2 때문에 피해 왔고, `paired_eval` 이 **같은 크롭 위에서** 재므로 비로소 성립했다. ⚠️**풀 핸디캡이 섞여 있다**(결과 006: 풀 2× = −0.12) → *"dense 아키텍처가 나쁘다"* 로 읽지 말 것. 말할 수 있는 건 **"우리가 매 KD 런에서 교사로 쓰는 그 파일이 우리가 가진 다른 파일보다 나쁘다"** 뿐. [4/5] 교사를 `mC_wsd` 로 바꾼 `mC_ct` = **3.6764**, paired **−0.0220**(SE 0.0005, t 48.46) = **분해능 미만 동급, 방향은 압축 교사 쪽**(R-P2 적중). **VRAM reserved 12.47 → 10.59 = −1.88 GiB**(alloc −0.51) → **R-P3 을 3.7배 과소예측** — 예측은 교사 파라미터(−526 MiB)만 셌는데 실제로는 **유니크 MLP 20→6 으로 할당 churn 이 줄어 단편화까지 줄었다**. ★**속도 무변**(3521.2 vs 3474.5/3521.9) → **R-P4 적중 + 원문 X1("압축 교사면 forward 0.55×")을 실측으로 반박**. `--no-ckpt`: 완주는 했으나(14.95 GiB) **순간 ms/step p90/p10 +48.5% = 스필**, ckpt ON 대비 **+26.4% 느림**. ★★**14.95 GiB 가 스필하고 15.37 GiB 가 안 했다 — 총량으로 스필을 예측할 수 없다.** ⚠️배치 안 [3/5] 은 **`CUBLAS_STATUS_EXECUTION_FAILED`**(§7: cuBLAS workspace 할당 실패는 `torch.OutOfMemoryError` 가 아니라 이 이름으로 나온다 / 직전 런 종료 **1초** 뒤 시작 + 여유 1 GiB / **대기를 넣은 배치는 안 죽고 안 넣은 배치가 죽었다**) → **RG0~RG3 전부 통과. 압축 교사는 드롭인이고 P042 §7 안 B 는 '대안' 이 아니라 '기본' 이 됐다** | 2026-08-08 | [037](037_20260808_P018-교사가-학생보다-나빴다-압축교사가-더-좋다.md) |
