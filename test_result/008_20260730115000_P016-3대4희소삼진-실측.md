> ## ⚠️ 2026-07-31 정정 — **측정치는 유효, 메모리 서술과 기전 해석은 무효** (결과 016 §7)
>
> | 항목 | 상태 |
> |---|---|
> | **val_loss · bpb · 시간 · `grad_max` · VRAM** | ✅ **전부 유효.** 재학습 불필요 |
> | 저장 메모리 **11.7MB / 10.3MB**, 감축비 **2.64× / 3.00×** | 🚫 **회계 규약 불일치.** 1.95(코드+스케일+컨테이너) 와 1.25(순수 코드) 를 나눴다. 동일 회계로는 `mA` **14.6MB / 2.11×**, `mC` 14.7MB / 2.10× — **거의 동률** |
> | "희소 단독비용 +0.035~0.056" | ⚠️ 값은 유효하나 **기전 해석이 틀렸다.** 3:4 는 0 을 **늘리는 게 아니라 줄인다**(비영 75% vs TWN 54~57%) |
> | 실험 조건 | ⚠️ 논문의 **Arenas(weight trapping 방지)를 빼고** 측정했다 → **비용이 과대평가**됐을 수 있다(P036) |
>
> **"3.00× 감축" 을 인용하지 말 것.** 상세는 결과 016 §7.4·§7.5.

# 008 — P016 3:4 희소 삼진(1.25bpw) 실측 : 3.0× 감축에 +0.06, 희소 자체 비용 +0.056

- **계획**: [P016](../test_plan/P016_3대4희소삼진-1.25bit-done.md)
- **로그**: `008_log_20260727_P016-train-logs.txt` (사용자 대리 실행, 추가분석 2건 포함)
- **배치**: `run100m_P016.bat` (완료 후 삭제 — 명령은 §5 에 보존)
- **설정**: m100 / ko-en / 300M(300M 풀) / 2289스텝 / `--micro-bs 8 --accum 16` / lr 1e-3 /
  `--kd --init-from`(full KD, 교사=정본 dense) / `--sparse34` / `--compile`
- **비교 대상**: 정본 `dense`(3.8241) · `t_kd_g8`(3.8292) · `t_kdinit`(g4, 3.8253) 재사용

## 1. 측정 결과

| 태그 | g | 파라미터 | 배포 메모리(report) | val | best | bpb | 시간 | \|g\|max |
|---|---|---|---|---|---|---|---|---|
| `dense` (기준) | — | 132.5M | 30.9MB | 3.8241 | 3.7797 | — | 97.5분 | 1.2 |
| `t_kd_g8` | 8 | 63.5M | 14.9MB | 3.8292 | 3.7906 | 1.2337 | 165.2분 | 0.5 |
| `t_kdinit` | 4 | 72.9M | 17.1MB | 3.8253 | 3.7758 | — | 195.0분 | 0.5 |
| **`t_kd_g8_s34`** | 8 | 63.5M | **10.3MB** | **3.8847** | 3.8487@2250 | 1.2516 | 166.2분 | 0.6 |
| **`t_kd_g4_s34`** | 4 | 72.9M | **11.7MB** | **3.8605** | 3.8234@2250 | 1.2438 | 167.5분 | 0.6 |

격차 분해(모두 동일 토큰·동일 시드):

| 비교 | 격차 | 의미 |
|---|---|---|
| dense → `t_kd_g8_s34` | **+0.0606** | 타잉 g8 + 3:4 희소를 **모두** 합친 비용 |
| dense → `t_kd_g4_s34` | **+0.0364** | 타잉 g4 + 3:4 희소를 모두 합친 비용 |
| `t_kd_g8` → `t_kd_g8_s34` | **+0.0555** | **3:4 희소 단독 비용**(g8 고정) |
| `t_kdinit`(g4) → `t_kd_g4_s34` | **+0.0352** | **3:4 희소 단독 비용**(g4 고정) |

## 2. ★핵심 발견

### (1) 1.25bpw 는 작동한다 — 판정기준 통과
두 구성 모두 dense 대비 **`≤+0.07` 성립**. `|g|max 0.6`(dense 1.2보다 낮음), skip 0, 발산 없음.
"4당 정확히 1개 강제-0"(비영 75%)이 학습 안정성을 해치지 않는다는 것이 300M 실측으로 확인.
tiny 스모크(`s34_smoke`)에 이어 실스케일에서도 통과.

### (2) 희소의 순수 비용은 **+0.035 ~ +0.056**, 메모리 이득은 **1.45×**
g 를 고정하고 희소만 켰을 때: g8에서 +0.0555, g4에서 +0.0352.
같은 조건의 메모리: g8 14.9MB → 10.3MB (**1.45×**), g4 17.1MB → 11.7MB (**1.46×**).
→ **손실 0.035~0.056을 지불해 메모리 1.45× 를 산다.** 우리 프로젝트의 1순위(메모리)에서는
매우 좋은 교환비다. 참고로 g 를 4→8 로 올려 얻는 이득은 17.1→14.9MB(1.15×)에 +0.004 정도로,
**희소가 타잉보다 메모리 대비 효율이 높은 레버**임을 시사한다.

### (3) g4_s34 가 g8_s34 보다 우수 (희소와 타잉은 부분적으로 경합)
g4_s34 는 g8_s34 보다 **-0.0242** 좋고 메모리는 1.4MB만 더 쓴다(11.7 vs 10.3).
희소 단독 비용도 g4(+0.0352)가 g8(+0.0555)보다 작다 → **타잉이 이미 용량을 깎은 상태에서
희소로 또 깎으면 손실이 초선형으로 늘어난다.** 두 레버를 동시에 최대로 밀지 말 것.
- 메모리 최우선이면 g8_s34(10.3MB, +0.0606)
- 품질 여유를 두려면 **g4_s34(11.7MB, +0.0364)** 가 균형점

### (4) 학습 시간은 희소로 늘지 않는다
166.2 / 167.5분 vs t_kd_g8 165.2분 → **+0.6~1.4%**. argmin+마스킹은 STE forward의 elementwise
비용이라 GEMM 대비 무시 가능. **메모리 레버를 벽시계 비용 없이 얻는다.**
(단 dense 97.5분 대비 1.7× 는 full KD 교사 forward 때문 — 결과 007의 k4+압축교사 조합으로 축소 가능)

### (5) best 시점이 2250 = 마지막 eval — 아직 undertrained
두 s34 런 모두 best 가 2250스텝(마지막 직전 eval)에서 나왔고 그 뒤 최종값이 되레 올랐다
(3.8487→3.8847, 3.8234→3.8605). 이는 cosine 꼬리에서의 통상적 요동이며 **조기 수렴은 아니다** —
s34 는 더 긴 예산에서 격차가 더 줄어들 여지가 있다(반대로 결과 006 §2-(3)에 따르면 KD 이득은
예산에서 줄어들므로, **600M 예산 s34 재측정**이 필요).

### (6) ⚠️ `compare.py` 배포메모리 계산 버그 (발견 → ✅ 2026-07-30 수정 완료)
구 `compare.py:44` 는 `MB = params × bpw / 8` 로 **전체 파라미터에 단일 bpw**를 적용했다.
그런데 `report()`(`transformer.py:161-171`)는 옳게 **삼진 가중치만 1.25bpw**, 임베딩은 1.95bpw 로
계산한다. 임베딩은 3:4 희소 대상이 아니기 때문이다.

| | compare 출력 | report(정확) | 오차 |
|---|---|---|---|
| `t_kd_g8_s34` | 9.5MB / **3.26×** | **10.3MB / 3.00×** | -0.8MB, 비율 과대 |
| `t_kd_g4_s34` | 10.9MB / **2.83×** | **11.7MB / 2.64×** | -0.8MB, 비율 과대 |
| `t_kd_g8` → `s34` | 1.56× | **1.45×** | 과대 |

비희소 런에서는 임베딩도 1.95bpw 라 우연히 일치했지만, **sparse34 런에서만 어긋났다.**
→ 본 문서의 표는 전부 **report 기준(정확값)** 으로 기록했다.

**수정(2026-07-30)**: `transformer.mem_breakdown()` 신설로 `report()` 와 단일 소스화하고,
`trainer` 가 json 에 `deploy_mb` 를 기록하게 했다. `compare` 는 그 값을 쓴다.
**단 본 실험의 두 s34 로그는 수정 이전 것이라 `deploy_mb` 가 없다** → compare 가 `~(근사·과소)`
표시와 경고를 낸다. 정확값을 json 에 넣으려면 재학습이 필요하다(REVIEW1 후보 A·B 가 그 역할).

## 3. 판정

| 항목 | 판정 |
|---|---|
| `t_kd_g8_s34` (10.3MB, 3.00×) | +0.0606 → **성립**(`≤+0.07`) |
| `t_kd_g4_s34` (11.7MB, 2.64×) | +0.0364 → **성립**, 여유 큼 |
| 학습 안정성 | `|g|max 0.6`, skip 0 → 문제 없음 |
| 구현 정합성 | 4당 정확히 3 nonzero(75%) — numpy 검증(이전 세션) + 실스케일 안정 확인 |
| `compare.py` bpw 회계 | ❌ 버그 발견 → ✅ **수정 완료**(§2-(6)). 단 본 실험 로그는 구 포맷 |

## 4. 후속

1. **[중] 600M 예산 s34** — 결과 006이 보인 "KD 이득의 예산 역스케일링"이 s34에도 오는지.
   `--pool-tokens 600M --exact-cache` 로 공정 풀에서.
2. **[중] s34 + k4 조합** — 결과 007이 확인한 `--kd-every 4 --kd-teacher-tag t_kd_g8 --no-ckpt` 를
   s34 에 얹으면 품질 유지·시간 -30% 기대. 리뷰문서 최적안 B/C 의 근거런.
3. ~~`compare.py` bpw 분리 수정~~ → ✅ 완료. 단 **구 로그에는 `deploy_mb` 가 없어** 재compare 로는
   정확값이 안 나온다(근사+경고 표시). 정확값은 REVIEW1 재학습분부터 자동 기록된다.
4. **[저] CPU LUT/AVX2 패킹 실증** — 1.25bpw 의 실제 이득은 배포 커널에서 5비트/4가중치 무낭비
   패킹이 되어야 실현된다. 현재는 이론 메모리만 측정된 상태.

## 5. 재현 명령

```
python run100m.py train --arch tied --data ko-en --tokens 300M --steps 2289 --micro-bs 8 --accum 16 --lr 1e-3 --compile --kd --init-from --mlp-group 8 --sparse34 --tag t_kd_g8_s34
python run100m.py train --arch tied --data ko-en --tokens 300M --steps 2289 --micro-bs 8 --accum 16 --lr 1e-3 --compile --kd --init-from --mlp-group 4 --sparse34 --tag t_kd_g4_s34
python run100m.py compare --tag t_kd_g8_s34
python run100m.py compare --tag t_kd_g8 --vs t_kd_g8_s34
python run100m.py compare --tag t_kdinit --vs t_kd_g4_s34
```
