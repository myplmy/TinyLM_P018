# 014 — P030 **단계 2(1차, 무효)** CPU 추론 실측 : **측정된 것은 추론이 아니라 매 forward 재양자화 비용이었다**

> **★단계 이름 정리(2026-07-31)** — "단계 2" 라는 이름이 세 번 쓰여 혼동이 있었다. 앞으로 이렇게 부른다.
>
> | 이름 | 시점 | 상태 | 이 문서의 절 |
> |---|---|---|---|
> | **단계 2 (1차)** | 07-31 이른 시각 | **무효** — 추론이 아니라 재양자화를 쟀다 | §1~§7 |
> | **단계 2 재시도** | 07-31 | **중단** — [0/4] 게이트가 막았고, 그 게이트가 **오경보**였다 | §8 |
> | **단계 2B** | **미실행** | 캐시 정확성 증명(§9) 후의 **첫 유효 측정**. 배치 `run_P030_stage2B_infer.bat` | — |
>
> **§1 의 표를 단계 2B 결과와 같은 표에 섞지 말 것.**

> ## 🚧 재측정 시도 실패 → **원인 규명·해소 완료 (2026-07-31)**
> 수정 후 `run_P030_infer.bat` 을 재실행했으나 **[0/4] 게이트가 `mA` 를 2/5 불일치로 판정**해
> [1/4]~[4/4] 가 전부 실행되지 않았다(§8).
> **§9 에서 원인이 확정됐다: 게이트 오경보.** fp32 teacher-forced 로짓 동등성 `max|Δ| = 1.1~1.4e-05`,
> argmax 100% 일치 → **KV 캐시는 정확하다.** bf16 ULP(0.125)가 그리디 24스텝에서 증폭된 것이었다.
> **단계 2 재측정 진행 가능.** 다만 **본 문서 §1 의 무효 수치는 아직 대체되지 않았다.**

- 계획: [P030](../test_plan/P030_추론속도-KV캐시-CPU배포.md) 단계 2 · 배치 `run_P030_infer.bat`
- 로그: `014_log_20260731_P030-2-test-logs.txt`(1차, 무효) ·
  **`014_log_20260731_P030-2rerun-infer-test-logs.txt`(재실행, 게이트 중단)** (2026-07-31)
- 환경: **AMD Ryzen 7 9700X** 8코어/16스레드, L2 8MB, **L3 32MB**, RTX 4080급 GPU
- 조건: `max_new=32`, reps 3 중위값, 프롬프트 `대한민국의 수도 서울은`, batch 1

> ## ⚠ 결론 먼저 — **이 표의 절대값도 모델 간 순위도 배포 성능이 아니다**
> **`transformer.forward()` 가 매 호출마다 `refresh_quant()` 를 무조건 부른다.**
> 학습 중에는 latent 가 스텝마다 바뀌므로 **그게 옳다.** 그러나 추론에서는 가중치가 고정이라
> 전부 낭비다 — **매 토큰·매 층마다 삼진 양자화를 처음부터 다시 계산**하고 있었다.
> 측정된 시간의 대부분이 **GEMM 이 아니라 양자화**다(§2 에서 수치로 확인).
> **인공물이며, 수정 전 숫자로 아키텍처를 판단하면 안 된다.**
>
> **★2026-07-31 정정**: 본 문서 초판은 원인을 "`load_model()` 이 `refresh_quant()` 를
> 호출하지 않아 `_wq` 가 None 으로 남는다"로 적었다. **그건 틀렸다.**
> `forward()` 가 무조건 부르므로 `_wq` 는 매번 **새로 채워진다** — 즉 `evaluate()` 가
> 사전에 `refresh_quant()` 를 부르는 것도 **효과가 없었다.** 결론(양자화 재계산이 지배)은
> 같지만 **경로가 다르고, eval 도 같은 낭비를 하고 있었다.**
> 수정: `freeze_quant()` 로 1회만 계산 + forward 가 건너뛰기(`clear_quant` 가 되돌림).
> **val_loss 값 자체는 영향 없다**(같은 값을 비싸게 계산했을 뿐) — 품질 결론은 전부 무사하다.

## 1. 측정 결과

### GPU (참고)

| 모델 | 배포MB | tok/s | TTFT ms |
|---|---|---|---|
| `p6d` dense | 30.9 | 22.12 | 40.8 |
| `mC_g8_k4` | 14.9 | **31.00** | 28.2 |
| `mA_g4s34_k4` | 11.7 | 30.43 | 30.3 |

### CPU — 스레드 스윕

| 스레드 | `p6d` (30.9MB) | `mC_g8_k4` (14.9MB) | `mA_g4s34_k4` (11.7MB) |
|---|---|---|---|
| 1 | 1.89 | **3.77** | **1.39** |
| 2 | 2.57 | 5.18 | 2.13 |
| 4 | 3.26 | 7.01 | 3.57 |
| 8 | 4.33 | **8.90** | 5.06 |

TTFT(1스레드): dense 476ms / mC **210ms** / mA **680ms**

**두 가지가 이상하다.**

1. **`mC` 가 dense 보다 2.0배 빠르다.** 그런데 **타잉은 FLOPs 를 줄이지 않는다** —
   층 수가 같고 공유 가중치를 그대로 다시 적용하므로 GEMM 연산량은 dense 와 **동일**하다.
   연산이 같은데 2배 빠르다면 **시간을 지배하는 것이 GEMM 이 아니다.**
2. **`mA` 가 dense 보다 26% 느리다.** 메모리는 가장 작고(11.7MB) 유니크 가중치도 dense 의
   절반인데 가장 느리다. **메모리→속도 전이의 반례**처럼 보이지만, 실제 원인은 다르다.

## 2. ★원인 규명 — 양자화 비용이 유니크 가중치 수에 비례한다

### 2.1 코드 경로

`tinylm/model/ternary.py` 의 `TLinear.forward`:

```python
wq = self._wq if self._wq is not None else ternary(self._w(), self.cfg)
```

`_wq` 는 `refresh_quant()` 가 채운다. 그런데 **`transformer.forward()` 가 매 호출 `self.refresh_quant()` 를 부른다**(수정 전 L93).
따라서 `_wq` 는 항상 채워져 있지만 **매번 새로 계산**된다.

| 경로 | 수정 전 동작 | 낭비 |
|---|---|---|
| `train/trainer.py` | 스텝마다 재계산 | **없음 — latent 가 실제로 바뀐다** |
| `eval/evaluate.py` | 사전 `refresh_quant()` 후에도 forward 가 다시 계산 | **`iters` 배**(100배치면 100회) |
| `infer/generate.py` | 토큰마다 전 층 재계산 | **생성 토큰 수 배** |

→ **추론만의 문제가 아니었다.** eval 도 같은 낭비를 하고 있었고 아무도 재지 않았을 뿐이다.
KV 캐시가 없어 매 토큰 전체 시퀀스를 재계산하므로 `max_new=32` 기준
**약 700회의 층-forward × 층수** 만큼 양자화가 반복된다.

**수정**: `freeze_quant()`(1회 계산 + 플래그) / forward 는 `if not self._quant_frozen` 일 때만
재계산 / `clear_quant()` 가 플래그를 내려 학습 재개를 안전하게 한다.

### 2.2 수치 검증 — 예측과 실측 대조

양자화 비용이 **삼진 유니크 파라미터 수**에 비례한다고 보고 계산했다(임베딩·norm 제외):

| 모델 | 삼진 유니크 파라미터 | dense 대비 | **예측 속도비** | **실측(1스레드)** | 판정 |
|---|---|---|---|---|---|
| `p6d` dense | 120.9M | 1.00× | 1.00× | 1.00× | 기준 |
| `mC_g8_k4` | 54.9M | 0.45× | **2.20×** | **1.99×** | ✅ **일치** |
| `mA_g4s34_k4` | 64.3M | 0.53× | **1.88×** | **0.73×** | ❌ **2.6배 미달** |

**`mC` 의 미스터리는 풀렸다.** g8 타잉은 유니크 가중치를 dense 의 45% 로 줄이고,
양자화는 **유니크 가중치당 1회**면 되므로 양자화 비용도 45% 가 된다.
GEMM 은 그대로인데 2배 빨라진 것은 **시간의 대부분이 양자화였다**는 직접 증거다.

시간을 `GEMM + 양자화` 로 분해하면 (1스레드, dense 0.529s/token 기준):
**GEMM ≈ 0.11s(21%) + 양자화 ≈ 0.42s(79%)**. mC 예측 = 0.11 + 0.42×0.45 = **0.30s**
(실측 0.265s). 자릿수가 맞는다.

### 2.3 ★`mA` 의 추가 손실 = **sparse34 양자화 경로가 훨씬 비싸다**

예측 1.88× 대비 실측 0.73× → **2.6배의 추가 비용**이 있다. 코드가 그대로 설명한다:

```python
if sparse34:
    b = aw.reshape(O, G, group // 4, 4)
    keep = torch.ones_like(b)                                   # 텐서 신규 할당
    keep.scatter_(3, b.argmin(dim=3, keepdim=True), 0.0)        # argmin + scatter
    mask = keep.reshape(O, G, group)
else:
    mask = (aw >= thr_ratio * aw.mean(dim=2, keepdim=True))     # mean + 비교, 1패스
```

- **일반 경로**: `mean` 1회 + 비교 1회 = 약 2패스
- **sparse34 경로**: `ones_like` 할당 + `argmin`(4원소 축) + `scatter_` + reshape 2회

`argmin` 과 `scatter_` 는 CPU 에서 **메모리 접근 패턴이 나쁜 연산**이고, 매 forward
재실행된다. 역산한 배율 **≈ 2.7배**가 이 차이와 부합한다.

> **★따라서 "s34 희소 모델이 CPU 에서 dense 와 비슷하다" 는 관측은 희소성의 성질이 아니다.**
> **희소 마스크를 매번 다시 만드느라 생긴 비용**이며, 마스크는 가중치가 고정된 추론에서
> **단 한 번만 계산하면 되는 것**이다. 배포에서는 애초에 존재하지 않아야 할 비용이다.

## 3. 사용자 질문에 대한 확인 — eos 정지 / KV 캐시 / `past_kv`

| 항목 | 측정 당시 | **현재(2026-07-31)** | 비고 |
|---|---|---|---|
| **eos 정지** | ❌ 미구현 | ✅ **구현** | `sample(stop_at_eos=True)`. `<eos>`(id 2)에서 중단 |
| **KV 캐시** | ❌ 미구현 | ✅ **구현** | `sample(use_cache=True)` 기본. `--no-cache` 로 옛 경로 대조 |
| **`past_kv` 경로** | ❌ 미구현 | ✅ **구현** | `forward(past_kv=, use_cache=)`. **캐시 키는 CLA owner 층** |
| **계획 문서의 해당 내용** | ✅ **온전했다 — 압축으로 유실되지 않았다** | ✅ | [P030 §단계 1](../test_plan/P030_추론속도-KV캐시-CPU배포.md) 에 `past_kv`, **CLA 캐시 공유 주의**, eos, `--no-cache` 폴백, **캐시 유/무 일치 검증**이 모두 남아 있었다 |

**측정 시점에는 계획만 있고 구현이 없었다.** 단계 2 를 단계 1 보다 먼저 돌린 것은
"오늘 있는 코드로 기준선을 만든다"는 의도된 순서였다(배치 헤더에 명시).
그 기준선이 §2 의 인공물에 오염됐으므로 **재측정이 필요**하고, 단계 1 은 그 뒤 구현됐다.

**★캐시 구현에서 조용히 틀리기 쉬운 두 곳**(그래서 배치에 게이트를 넣었다):
1. **RoPE 절대위치** — 캐시 사용 시 `rope[:T]` 가 아니라 `rope[past_len:past_len+T]`.
2. **SDPA 마스크** — `q_len != kv_len` 인데 `is_causal=True` 를 쓰면 마스크가 **좌상단 정렬**돼
   완전히 틀린 위치를 가린다. `tril(kv_len - q_len)` 을 명시해야 한다.
둘 다 **유창한 텍스트를 계속 만들어내므로 눈으로는 안 잡힌다.** 그리디 출력 일치로만 잡힌다.

## 4. 판정

| 질문 | 답 |
|---|---|
| 메모리를 줄이면 CPU 추론이 빨라지는가? | **이 측정으로는 답할 수 없다.** 상관은 보였으나(mC 2.0×) 원인이 메모리 대역폭이 아니라 **양자화 연산량**이었다 |
| s34 가 CPU 에서 느린 것이 희소성의 한계인가? | **아니다.** 재양자화 인공물이다 |
| 1스레드 tok/s < 1 인가? (배치의 위험 임계) | 아니다(1.39~3.77). 단 **KV 캐시 없이도 이 정도**이므로 캐시 후에는 크게 개선될 여지가 크다 |
| 지금까지의 품질·val 결론이 영향받는가? | **아니다.** eval 도 같은 재계산을 했지만 **결과값은 동일**하다(비싸게 계산했을 뿐). 단 **eval 소요시간**은 그만큼 과대였다 |

**본 결과는 "부정적 결과"가 아니라 "무효 측정"이다.** 계측 결함을 잡은 것이 성과다.

## 5. 후속 (우선순위)

1. ✅ **[완료 2026-07-31] `freeze_quant()` 도입 + forward 가드 + KV 캐시 + eos 정지.**
   → **재측정 필요**: `run_P030_infer.bat`(재측정판, 캐시 정확성 게이트 포함).
   세 모델의 순위가 바뀔 가능성이 높다.
2. **[필수] 실행시 메모리 별도 측정** — `_wq` 는 **dequant 된 fp32 사본**이라
   latent `weight` 와 **동시에 상주**한다. 저장 11.7MB 와 실행시 메모리는 별개다.
   → **P034 신설**(§6).
3. **[중] P030 단계 1**(KV 캐시 + eos + `past_kv`) 구현 후 전 항목 재측정.
4. **[참고] GPU 수치도 같은 인공물을 포함**한다(dense 22 vs mC 31 tok/s). 재측정 대상.

## 6. 재현 명령

```
run_P030_infer.bat
REM 또는 개별 실행
python scripts/bench_infer.py --device cuda --max-new 32 --reps 3
python scripts/bench_infer.py --device cpu --threads 1 --max-new 32 --reps 3
python scripts/bench_infer.py --device cpu --threads 2 4 8 --max-new 32 --reps 3
```
학습 없음. `runs/` 에 아무것도 쓰지 않는다.

## 7. 한계

- **`max_new=32`, batch 1, 프롬프트 1종**. 서버 처리량과는 다른 주제다.
- Windows CPU 계측은 노이즈가 크다(중위값 3회). **1~2% 차이는 읽지 않는다.**
- §2.2 의 파라미터 수는 **손계산 근사**다(CLA2 로 KV 절반 가정). 자릿수 판정에는 충분하나
  정확값은 `mem_breakdown()["params"]["ternary"]` 로 확인해야 한다.
- §2.3 의 "2.7배"는 **역산값**이지 프로파일러 측정이 아니다. 원인 지목은 코드 구조로
  뒷받침되지만, **정확한 배율은 별도 프로파일링이 필요**하다.

---

# 8. 재실행(2026-07-31) — [0/4] 캐시 게이트 실패, 측정 0건

로그: `014_log_20260731_P030-2rerun-infer-test-logs.txt`
환경: 동일(AMD Ryzen 7 9700X, L2 8MB, **L3 32MB**, 8코어/16스레드)

## 8.1 관측

```
--- mA_g4s34_k4 (tied) ---   3/5 일치, 2/5 불일치
  ✅ 대한민국의 수도 서울은
  ❌ 조선 시대의 과학 기술은 세종대왕 때   -> '...14세기 후반...' vs '...14세기 중기(世宗)...'
  ✅ 물이 끓는 온도는 섭씨
  ❌ The Industrial Revolution began in    -> '...was the first industrial...' vs '...was the first of the first...'
  ✅ 인공지능 기술의 발전은 사회에
--- p6d (dense) ---          5/5 일치
```

배치는 설계대로 `:CACHEBAD` 로 분기해 **[1/4]~[4/4] 를 실행하지 않았다**(게이트가 의도대로 동작).
**`runs/` 에 아무것도 쓰이지 않았다.**

## 8.2 ★배치가 지목한 두 곳은 범인이 아니다

배치의 실패 안내문은 ① RoPE 슬라이스 ② SDPA 마스크를 지목한다. 그런데 **그 두 코드는
dense 와 tied 가 완전히 공유**한다. 둘 중 하나가 틀렸다면 `p6d` 도 틀려야 하는데 **5/5 통과**했다.
코드 재검토 결과도 둘 다 정합이다:

| 위치 | 코드 | 판정 |
|---|---|---|
| `transformer.forward:121` | `rope_cos[past_len : past_len+T]` | ✅ 절대위치 보정됨 |
| `modules.Attention.forward:90-97` | `q_len != kv_len` 에서 `torch.ones(q,kv).tril(kv-q)` (bool, True=attend) | ✅ 조건 `j ≤ i + past_len` 성립 |

→ **안내문이 다음 사람을 틀린 곳으로 보낸다.** 수정 대상(§8.5).

## 8.3 mA 만 다른 것은 둘뿐 — 가설 2개

| 가설 | 근거 | 반증 방법 |
|---|---|---|
| **H1 · 수치정밀 (유력)** | `sample()` 이 CUDA 에서 **bf16 autocast**(`generate.py:77`). 캐시 없음 경로는 `is_causal=True`(flash 커널, T×T), 캐시 경로는 `attn_mask=`(efficient/math 커널, 1×kv_len) — **서로 다른 커널·다른 누산 순서**. bf16 가수 8비트 → 로짓 오차 ~1e-2. 그리디 24스텝은 **한 번의 argmax 뒤집힘이 이후 전부를 바꾸는 혼돈 증폭기** | **fp32 에서 재실행**(CPU 는 autocast 가 꺼진다) |
| **H2 · CLA 캐시 공유** | mA 는 `cla_group=2`, dense 는 `dense_baseline()` 이 `cla_group=1` 로 강제 → **owner 키 캐시 경로를 mA 만 통과**한다 | 같은 fp32 실행에서 mA 가 여전히 실패하면 H2 |

**H1 을 지지하는 정황**: 불일치가 난 두 지점이 정확히 **near-tie 지점**이다
(`the first of the first` vs `the first`, `14세기 후반` vs `14세기 중기`). 그 전까지 두 경로는
완전히 일치하다가 확률이 거의 같은 갈림길에서만 갈라진다. mA 는 반복 붕괴가 심한 모델(결과 013)이라
near-tie 가 훨씬 잦다. **3/5 통과·2/5 실패는 결정론적 버그가 아니라 확률적 뒤집힘의 패턴**이다.

## 8.4 ★근본 원인 — 게이트가 계획의 불변량을 재고 있지 않다

P030 계획 §단계1 의 요구는 **"캐시 유/무의 로짓이 일치"** 다.
구현(`check_cache_equivalence`)은 그 대리로 **디코딩된 문자열의 완전 일치**를 쓴다.
이 대리는 ① bf16 ② 자기회귀 24스텝의 증폭 ③ 토크나이저 decode 를 거치면서
**원래 불변량과 다른 양**이 된다.

> **결과 014 에 이어 두 번째로 걸린 "계측이 재려는 양을 재고 있는가" 함정이다.**
> 014 는 추론 대신 재양자화를 쟀고, 여기서는 로짓 동등성 대신 **그리디 문자열의 우연 일치**를 쟀다.

## 8.5 후속 (승인 후 수행)

| 순 | 작업 | 비용 | 내용 |
|---|---|---|---|
| **A** | 판별 실험 | 코드 소량 + CPU 수 분 | `probe_prompts.py` 에 **`--device` 추가(현재 미구현)** → `--check-cache --device cpu`(fp32). 통과=H1 / 실패=H2 |
| **B** | 게이트 재설계 | 반나절 미만 | **teacher-forced 로짓 비교**로 교체. 하드 게이트 = fp32 `max\|Δlogit\| < 1e-3`. bf16 그리디 불일치는 **최초 분기점 top-2 로짓 간격**과 함께 보고만 |
| **C** | 안내문 교체 | 분 | `run_P030_infer.bat` `:CACHEBAD` 가 RoPE/마스크를 지목하지 않게 |

**H1 이 맞더라도 "캐시가 옳다"가 증명되는 것은 아니다** — 증명은 B 의 fp32 로짓 동등성이 한다.
A 는 어느 쪽을 먼저 볼지 정하는 값싼 분기일 뿐이다.

---

# 9. 단계 1.5 (2026-07-31) — **캐시 정확성 증명 완료. §8 의 게이트 실패는 오경보였다**

로그: `014_log_20260731_P030-1point5A-cachecheck-test-logs.txt`(A) ·
`014_log_20260731_P030-1point5B-cachegate-test-logs.txt`(B)
배치: `run_P030_cachecheck.bat` → `run_P030_cachegate.bat`. **GPU 학습 없음.**

## 9.1 [1.5-A] 판별 — H1(수치정밀) 확정

동일 게이트를 **cuda(bf16)** 와 **cpu(fp32)** 에서 각각 돌렸다.

| 모델 | cuda / bf16 | **cpu / fp32** |
|---|---|---|
| `mA_g4s34_k4` (tied, cla2, s34) | 2/5 불일치 | **5/5 일치 ✅** |
| `mC_g8_k4` (tied, cla2, **s34 아님**) | **2/5 불일치** | **5/5 일치 ✅** |
| `p6d` (dense, cla1) | 5/5 일치 | 5/5 일치 ✅ |

> **`mC` 도 bf16 에서 실패했다** — §8 이 남겨둔 H2(CLA 캐시) 후보를 좁히는 대신,
> **sparse34 특이성이 없다**는 것이 먼저 드러났다. 그리고 **fp32 에서는 15/15 전부 일치**한다.
> **H1 확정, H2 기각.**

## 9.2 [1.5-B] ★증명 — teacher-forced 로짓 동등성

자기회귀 피드백을 끊고 같은 토큰열을 두 경로로 흘려 **위치별 로짓**을 비교했다.

| 모델 | fp32 `max\|Δlogit\|` | argmax 불일치 | 판정 |
|---|---|---|---|
| `mA_g4s34_k4` | **1.431e-05** | **0** / 28~32 | 통과 |
| `mC_g8_k4` | **1.335e-05** | **0** | 통과 |
| `p6d` | **1.144e-05** | **0** | 통과 |

**오차가 평탄하다** — prefill(5.7~1.05e-05) ≈ decode(9.5~1.43e-05) ≈ 전반 ≈ 후반.
위치에 따라 증가하지 않고(→ RoPE 정상), prefill 경계 이후 도약하지 않는다(→ 마스크 정상).
**허용오차 1e-3 의 70분의 1** 수준이고 argmax 는 15개 프롬프트 전부 100% 일치.

> **★KV 캐시 구현은 정확하다. P030 단계 1 이 요구한 불변량이 처음으로 실제 검정됐다.**
> 남은 1e-5 는 fp32 누산 순서 차이(두 경로가 다른 SDPA 커널을 탄다)로 설명된다.

## 9.3 bf16 오차의 크기 = **bf16 ULP 그 자체**

| 모델 | bf16 `max\|Δlogit\|` |
|---|---|
| `mA_g4s34_k4` | 1.875e-01 |
| `mC_g8_k4` | 1.875e-01 |
| **`p6d`** | **2.188e-01** ← **dense 가 가장 크다** |

관측값이 전부 **0.015625(=1/64)의 정수배**다(0.125 / 0.14375 / 0.15625 / 0.171875 / 0.1875 / 0.21875).
로짓 크기 16~32 구간에서 bf16 의 ULP 는 0.125 이므로 **이 오차는 1~2 ULP, 즉 표현 가능한 최소
단위 그 자체**다. "오차가 있다"가 아니라 **"더 작을 수 없다"** 는 뜻이다.

**그런데 그리디 게이트는 dense 가 5/5 통과했다.** 오차가 가장 큰 모델이 통과하고 작은 모델이
실패했다 → **게이트 실패를 결정한 것은 오차 크기가 아니라 near-tie 빈도**다. §8 의 가설 그대로다.

## 9.4 타이브레이크 확인 — 분기점의 top-2 간격

| 모델 | 프롬프트 | 분기 스텝 | **top-2 로짓 간격** | 판정 |
|---|---|---|---|---|
| `mA` | 조선 시대의 과학 기술은… | 3 | **0.0625** | 간격 < 오차(0.188) → 타이브레이크 |
| `mA` | The Industrial Revolution… | 13 | **0.0625** | 타이브레이크 |
| `mC` | 대한민국의 수도 서울은 | 2 | **0.0000** | **완전 동점** |
| `mC` | 물이 끓는 온도는 섭씨 | 11 | **0.0000** | **완전 동점** |

`mC` 의 두 사례는 간격이 **정확히 0** 이다 — bf16 에서 두 토큰의 로짓이 **같은 값으로 반올림**됐고,
`argmax` 가 인덱스 순서로 임의 선택한 것이다. **수치 오차조차 아니고 진짜 동점**이다.

## 9.5 판정 · 조치

| 항목 | 결론 |
|---|---|
| KV 캐시 구현 | **정확함(증명됨).** fp32 로짓 동등 1.1~1.4e-05, argmax 100% 일치 |
| §8 의 [0/4] 게이트 실패 | **오경보.** 계측이 불변량이 아닌 대리지표를 쟀다 |
| 원인 | bf16 ULP × 자기회귀 24스텝 증폭 × 모델별 near-tie 빈도 차 |
| P030 단계 2 | **진행 가능.** 선결 조건 해소 |

**조치 ✅ 완료(2026-07-31)**: **`run_P030_stage2B_infer.bat` 신설** — `[0/4]` 가
`scripts\diag_kvcache.py --device cpu --tol 1e-3` 를 호출한다.
`check_cache_equivalence()` 는 삭제하지 않는다 — 정성 확인용으로는 쓸모가 있고,
**왜 이것이 게이트가 될 수 없는지**가 기록으로 남아야 한다.

> **⚠️ 초판의 조치 기록이 불완전했다.** "게이트를 교체한다"고 적고 `run_P030_infer.bat` 의
> **`:CACHEBAD` 안내문만** 고쳤을 뿐 **게이트 명령(`probe_prompts.py --check-cache`)은 그대로 뒀다.**
> 그래서 그 배치를 다시 돌리면 **여전히 bf16/cuda 로 판정해 같은 오경보로 멈춘다.**
> 구 배치 `run_P030_infer.bat` 는 **폐기**하고 `run_P030_stage2B_infer.bat` 를 쓴다.

## 9.6 한계

- 프롬프트 5개 · `max_new=24` · batch 1. **긴 컨텍스트(수백~수천 토큰)에서의 캐시 정확성은
  이 측정 범위 밖**이다. `max_seq_len` 초과 시 캐시를 버리고 재구축하는 경로(`generate.py`)도 미검정.
- fp32 1e-5 는 **CPU** 값이다. cuda fp32(TF32 허용 시)는 다를 수 있다.
- "argmax 100% 일치"는 **teacher-forced** 조건이다. 자유 생성에서 bf16 이 다른 문자열을 내는 것은
  여전히 정상이며, **재현 가능한 생성을 원하면 fp32 또는 고정 시드가 필요**하다.

---

# §10. **단계 2B — 첫 유효 CPU 추론 측정** (2026-07-31, 완료)

- 배치: `run_P030_stage2B_infer.bat` · 로그: `014_log_20260731_P030-stage2B.txt`
- 환경: **AMD Ryzen 7 9700X** 8코어/16스레드, L2 8MB, **L3 32MB**
- 조건: `max_new=128`, reps 3 **중위값**, 프롬프트 `대한민국의 수도 서울은`, batch 1
- 선행 게이트 `[0/4]`: **통과** — fp32 teacher-forced `max|Δlogit|` 1.14~1.43e-05, argmax 100% 일치

> **§1 의 무효 표는 이것으로 대체된다.** 두 표를 같은 표에 섞지 말 것.

## 10.1 ★프로젝트 헤드라인 수치 — CPU 단일 스레드

| 모델 | 캐시 | **tok/s** | TTFT ms |
|---|---|---|---|
| `p6d`(dense) | on | **31.29** | 52.8 |
| `mC_g8_k4` | on | **34.67** | 47.1 |
| `mA_g4s34_k4` | on | **32.10** | 46.6 |
| p6d | off | 4.86 | 53.3 |
| mC | off | 5.00 | 45.6 |
| mA | off | 4.96 | 50.3 |

**132M 삼진 모델이 데스크톱 CPU 1스레드에서 31~35 tok/s.** 이것이 `CLAUDE.md` 첫 줄의
"저사양 CPU·엣지" 주장에 대한 **최초의 실측**이다.

## 10.2 ★핵심 질문 — 메모리를 줄이면 빨라지는가 : **거의 아니다**

| 모델 | 저장 감축 | **상주** 감축(결과 016) | 속도비(CPU t1) | 전이율(상주 기준) |
|---|---|---|---|---|
| mC_g8_k4 | 2.07× | 2.17× | **1.11×** | **9.4%** |
| mA_g4s34_k4 | 2.64× | 1.87× | **1.03×** | **3.4%** |

**메모리→속도 전이는 사실상 없다.** 이유는 이미 알고 있다 — 삼진 가중치는 GEMM 직전에
**fp32 로 dequant** 되므로 GEMM 이 읽는 바이트 수가 줄지 않는다. **음성 결과이지 실패가 아니고**,
P034 단계2~4(특히 **R8 = 5비트 패킹 커널**)가 존재하는 이유가 정확히 이것이다.

**단 순위는 상주를 따라간다**: 속도 순 `mC > mA > p6d` 는 **상주** 순위(mC 451.5 < mA 523.5 < p6d 978.0)와
일치하고 **저장** 순위(mA < mC < p6d)와는 어긋난다. 결과 016 의 예측이 **방향으로는 맞았다.**

## 10.3 mA 가 mC 보다 느리다 — **예상된 결과이고, 014 §1 의 인공물은 사라졌다**

`mA` 32.10 vs `mC` 34.67 = **−7.4%**. mA 는 유니크 삼진 파라미터가 **17% 많다**(64.29M vs 54.85M).
결과 014 §1 의 **−26%** 는 `freeze_quant` 이전의 **마스크 재계산 인공물**이었고,
수정 후 **−7.4% 로 줄어 파라미터 수 차이와 정합**한다. → **§1 의 진단이 옳았음이 확인됐다.**

## 10.4 ★KV 캐시 이득은 **디바이스에 따라 부호가 바뀐다**

| 디바이스 | p6d | mC | mA |
|---|---|---|---|
| **CPU t1** | **6.44×** | **6.94×** | **6.47×** |
| **CUDA** | 0.93× | 0.88× | 0.95× |

**GPU 에서는 캐시가 오히려 손해다(−5~12%).** 이 크기·이 길이에서 GPU 는 **런치 오버헤드 바운드**라
O(T²) 재계산이 거의 공짜인 반면 캐시는 부기 비용만 추가한다. CPU 는 연산 바운드라 6.4~6.9× 이득.

> **인용 시 반드시 디바이스를 함께 적을 것.** "KV 캐시 6.9× 이득"만 떼어 쓰면 GPU 에서 거짓이 된다.

## 10.5 ★스레드는 **디코드를 가속하지 않는다** (TTFT 만 개선)

| 스레드 | p6d tok/s | p6d TTFT | mC tok/s | mA tok/s |
|---|---|---|---|---|
| 1 | 31.29 | **52.8** | 34.67 | 32.10 |
| 2 | 30.07 | 49.5 | 33.51 | 31.37 |
| 4 | 30.34 | **31.1** | 34.19 | 32.46 |
| 8 | 30.03 | **30.8** | 34.75 | 31.05 |

**tok/s 는 1→8 스레드에서 전혀 늘지 않는다**(오히려 −4%). 반면 **TTFT 는 52.8 → 30.8ms 로 −42%**.

- **prefill** 은 (T×d) 행렬 연산이라 병렬화된다 → TTFT 개선
- **decode** 는 토큰당 (1×d) GEMV 라 코어를 못 채운다 → 스레드 무의미

**엣지 배포 함의**: 코어를 늘려도 생성 속도는 안 오른다. **배치·투기적 디코딩·패킹 커널**만이 남은 축이다.

## 10.6 [4/4] eos 정지 — **확인 실패(생성이 멈추지 않았다)**

정성 프로브 5건 모두 **문서 끝에서 멈추지 않고 max_new 까지 이어 썼다**. 반복 붕괴도
결과 013 과 동일하게 재현(`온도는 온도에서 가장 낮은 온도…`). eos 정지 자체는 `generate.py` 에
있으나 **이 모델들이 eos 를 잘 내지 않는다** — 300M 토큰·파라미터당 2.3토큰에서는 예상 범위다.

> 다만 `bench_infer.py` 는 속도 측정에서 `stop_at_eos=False` 를 쓰므로 **이 관찰과 속도 수치는 무관**하다.

## 10.7 ★계측 결함 발견 — 표의 `배포MB` 가 **하드코딩이었다**

같은 배치 로그 안에서 **같은 모델의 크기가 두 값**으로 찍혔다.

| 모델 | `bench_infer.py` 가 찍은 `배포MB` | `mem_runtime.py` 가 찍은 `저장` |
|---|---|---|
| p6d | 30.9 | **27.1** |
| mC_g8_k4 | 14.9 | **13.1** |
| mA_g4s34_k4 | 11.7 | **12.4** |

원인: `bench_infer.py` 의 `DEFAULT_MODELS` 에 **30.9 / 14.9 / 11.7 이 리터럴로 박혀 있었다.**
그 값들은 **2026-07-31 이전 구 규약**이고, `--models` 를 주면 `mb=0.0` 이 되어 비율 절이 통째로
사라지기까지 했다. `CLAUDE.md` 의 **파라미터 단일 소스** 규약 위반이다.

**수정 완료**: 로드한 모델의 `mem_report_all()` 에서 계산하고 **저장·상주를 함께** 찍는다.
`probe_prompts.py` 설명문의 `11.7MB(2.64x)` · `30.9MB` 도 제거했다.

> **§10.2 의 비율은 결과 016 의 상주 실측값으로 다시 계산한 것**이며,
> 로그 표에 인쇄된 저장 비율(2.07×/2.64×)은 **구 규약이므로 인용하지 말 것.**

## 10.8 다음

| 항목 | 배치 | 근거 |
|---|---|---|
| P034 단계2(latent 해제) | **`run_P034_stage2_latent.bat`** | 상주만 절반으로 움직이는 **유일한 단일축 실험** — §10.2 의 전이 가설을 깨끗하게 재검정 |
| R8 5비트 패킹 커널 | 미작성(설계만) | §10.2 가 그 필요성의 **실측 근거**다 |

## 10.9 한계

- batch 1 단일 요청. **서버 처리량은 다른 주제다.**
- Windows CPU 타이밍은 노이즈가 크다(중위값 3회, **1~2% 차이는 읽지 않는다**).
- 저장 MB ≠ 상주 MB(결과 016). §10.2 의 전이율은 **상주 기준**으로 계산했다.
- 이 측정 중 GPU/CPU 를 다른 작업이 점유하지 않았는지는 로그로 확인 불가
  (결과 015 부수 발견: 동시 실행 시 **+14% 느림**).

## 10.10 재현 명령 (배치 삭제 대비 보존)

```bat
run_P030_stage2B_infer.bat
:: [0/4] 캐시 정본 게이트
python scripts\diag_kvcache.py --models mA_g4s34_k4 mC_g8_k4 p6d --device cpu --tol 1e-3 --max-new 24
:: [1/4] GPU 기준선, 캐시 on/off
python scripts\bench_infer.py --device cuda --max-new 128 --reps 3 --both-cache
:: [2/4] ★헤드라인 — CPU 1스레드
python scripts\bench_infer.py --device cpu --threads 1 --max-new 128 --reps 3 --both-cache
:: [3/4] CPU 스레드 스윕(캐시 on)
python scripts\bench_infer.py --device cpu --threads 2 4 8 --max-new 128 --reps 3
:: [4/4] eos 정지·정성
python scripts\probe_prompts.py --models mA_g4s34_k4 --temps 0.7
```

---

# §11. **단계 4 — 디코드 속도는 파라미터가 아니라 층 수를 따라간다** (2026-08-06, 완료)

- 배치: `run_P030_stage4_layerscaling.bat` · 로그: `014_log_20260806_P030-stage4.txt`
- **04:49:54~04:55:09 단독 실행.** 다음 작업(P037 2C)은 04:56:02 시작 — **겹치지 않았다.**
  → **이 로그가 현재 가장 신뢰할 수 있는 속도 기준선이다**(§11.4 참조).

## 11.1 측정 (CPU 1스레드, 캐시 on, `--drop-latent`)

| R | 층 통과 | `p6d` | `mC_g8_k4` | `mA_g4s34_k4` |
|---|---|---|---|---|
| 0.5 | 12 | 53.82 | **64.59** | 63.12 |
| **1.0** | **20** | 36.78 | **45.01** | 42.48 |
| 1.5 | 28 | 27.57 | **34.25** | 31.87 |
| 2.0 | 36 | 21.48 | **26.66** | 24.70 |
| 1.0 + int8 | 20 | 14.20 | 14.89 | **15.08** |

## 11.2 ★선형 적합 — **시간/토큰 = a + b × 층통과**

| 모델 | **a**(고정, ms/토큰) | **b**(층당, ms) | R=1.0 에서 a 비중 |
|---|---|---|---|
| `p6d` | 4.25 | **1.163** | 15.4% |
| `mC_g8_k4` | 4.18 | **0.913** | 18.6% |
| `mA_g4s34_k4` | 3.28 | **1.022** | 13.8% |

**네 점이 깨끗한 직선 위에 있다.** 디코드 시간은 **층 통과 수에 선형**이고,
**층과 무관한 고정비 `a ≈ 3.3~4.3 ms/토큰`** 이 있다.

## 11.3 ★★가설 수정 — **§10.4 의 프레이밍이 틀렸다**

§10.4 는 이렇게 적었다:

> "파라미터 수는 2.26배 차이인데 tok/s 는 5% 이내다 → 런치 오버헤드 바운드"

**전제가 틀렸다.** 타잉은 **저장을 줄이지 컴퓨트를 줄이지 않는다.**
`transformer.forward()` 는 타잉이든 아니든 **매 층마다 MLP 전체를 실행**한다
(공유 객체를 g번 호출할 뿐이다). `report()` 의 FLOPs 계산에도 타잉 항이 없다:

```python
per_l = (dim*dim*2 + kv_dim*dim*2) + 3*dim*ffn_dim
flops = 2 * n_layers * per_l          # ← mlp_group 이 안 들어간다
```

**즉 `p6d` 와 `mC` 는 층당 FLOPs 가 정확히 같다.** 속도가 비슷한 것은 **이상현상이 아니라 당연**하다.
`CLAUDE.md` 첫 줄이 이미 그렇게 적고 있다 — "**연산이 아니라 메모리 최적화**(연산 증가는 감수)".

> **내가 놓친 것**: "파라미터 2.26배" 는 **유니크 파라미터** 수이고, 그건 **메모리** 축이다.
> 속도 축에 그걸 기대한 것이 오류였다. §10.4 의 "런치 오버헤드 바운드" 는
> **필요 없는 가설**이었다 — 설명할 이상현상이 애초에 없었다.

### 그럼 남는 진짜 관측은 무엇인가

**`mC` 가 `p6d` 보다 층당 21.5% 싸다**(b 0.913 vs 1.163). FLOPs 가 같은데 왜?

가장 그럴듯한 설명은 **캐시 지역성**이다. `mC`(g=8)는 **연속 8개 층이 같은 MLP 가중치**를 쓰므로
그 가중치가 L2/L3 에 머문다. `p6d` 는 20개 층이 전부 다른 MLP 를 만진다.
`mA`(g=4)가 중간(1.022)인 것도 이 설명과 맞다 — **g 가 클수록 재사용이 길다.**

> **이건 가설이다.** 확정하려면 캐시 미스 카운터가 필요하고 우리에겐 없다.
> 다만 **b 가 g 순서(g8 < g4 < g1)로 단조**라는 것은 기록할 값어치가 있다.
> **타잉은 메모리뿐 아니라 속도에도 (작지만) 이득이 있다** — 종전에 없던 관측이다.

## 11.4 ★★int8 의 비용을 층당으로 분해하면 — **R8 의 설계가 결정된다**

| 모델 | b(단계2) | b(단계2+3) | **int8 이 얹는 층당 비용** |
|---|---|---|---|
| `p6d` | 1.163 | 3.309 | **+2.146 ms** |
| `mC` | 0.913 | 3.149 | **+2.236 ms** |
| `mA` | 1.022 | 3.152 | **+2.129 ms** |

**세 모델이 거의 같은 +2.1~2.2 ms/층** 을 낸다. 파라미터는 2.26배 차이인데도 그렇다.
→ **분리된 unpack 연산 자체의 고정비**가 지배한다(별도 커널·별도 메모리 왕복).

> ### **R8(5비트 패킹)에 대한 결론 — 착수 조건이 정해졌다**
> 1.375bpw 패킹은 int8 보다 **더 복잡한 unpack** 이 필요하다. 그걸 **분리된 연산으로 하면
> +2.1ms 보다 나빠진다** — 층당 비용이 0.9~1.2ms 인데 거기에 2.1ms 이상을 더하는 것이다.
> **R8 은 unpack 을 GEMM 에 융합해야만 의미가 있다.** 융합 없이는 메모리만 벌고
> 속도는 **3배 이상 잃는다**(int8 이 이미 보여줬다).
>
> **그리고 융합해도 얻는 것은 메모리뿐이다** — b 의 하한은 FLOPs 이고, 패킹은 FLOPs 를 안 줄인다.
> 잘해야 캐시 지역성 개선(§11.3 의 21.5% 급)이다.
>
> **→ R8 의 값어치 = 상주 96.2 → 43.5MB, 속도는 잘해야 본전.**
> 그 메모리가 필요한 배포 목표가 있을 때만 착수한다(`08_paper_review` §3 의 ESP32 표 참조).

## 11.5 고정비 `a ≈ 3.3~4.3 ms/토큰` — **모델 밖의 비용**

R=1.0 에서 **전체의 14~19%** 다. 층과 무관하므로 모델 forward 밖이다. 후보:

- **LM 헤드**: `768→256→32768`. 토큰당 약 **8.6M MAC** — **층 하나(6.2M)보다 크다.**
- **top-k 샘플링**: 32,768 vocab 에 대한 `topk(40)` + softmax + multinomial.
- `torch.cat([x, nxt])` 가 매 토큰 전체 시퀀스를 재할당(O(T), 128토큰이면 작다).

**버그가 아니라 실제 비용**이다. 줄이려면 vocab 축소나 샘플링 경로 최적화가 필요하고,
그건 아키텍처 실험이 아니라 배포 최적화다. **지금은 기록만 한다.**

## 11.6 계획서 예측 대조

| 예측 | 관측 | 판정 |
|---|---|---|
| 런치 바운드면 tok/s ∝ 1/층통과 | **성립**(선형 적합 깨끗) | 단 §11.3 이 해석을 바꿨다 |
| 고정 R 에서 모델 간 격차가 몇 % 이내면 런치 바운드 | 격차 **1.15~1.24×로 안정** | **FLOPs 가 같으니 당연** — 이 검정은 무효였다 |
| tok/s 가 R 에 무관 | ❌ | 기각 |

## 11.7 한계

- **캐시 지역성은 가설이다.** 하드웨어 카운터로 확인하지 않았다.
- 선형 적합은 4점이다. `a`·`b` 는 추정이고 신뢰구간을 계산하지 않았다.
- batch 1 · CPU 1스레드 · Windows. 서버 배치에서는 균형이 컴퓨트 쪽으로 이동한다.
- R≠1.0 은 **모델 품질을 바꾼다**(결과 020). 여기서는 **시간만** 읽었다.
