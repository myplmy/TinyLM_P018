# 029 — P045 g16: **분해능 미만. 타잉 축 종결, 채택 가능**

- **계획**: [P045](../test_plan/P045_타잉상한-g16-종결.md) · **로그**: [`029_log_20260807_P045_mC_g16.txt`](029_log_20260807_P045_mC_g16.txt)
- **배치**: `run_P045_g16_ceiling.bat` (야간큐 v2 [4/4], 05:11~07:14, **123.1분**)
- **대조군**: `mC_wsd` **재학습 안 함**(결과 024) — 조건이 `--mlp-group` 하나만 다르다

---

## 0. 한 줄

> **paired Δ = +0.0161** (SE 0.0005, t=−33.88, `mC_wsd` 승률 80.9%) — **분해능 0.024 미만 = 실무상 동급.**
> packed **13.050 → 12.088MB**(감축비 2.07× → **2.24×**), fp32 상주 **451.5 → 415.5MB(−8.0%)**.
> → **게이트 G1 통과(Δ < 0.024). 채택 가능하고 타잉 축은 여기서 닫힌다**(`n_middle=16` 이라 g16 이 구조적 상한).

---

## 1. 측정값

| | `mC_wsd`(대조) | **`mC_g16`** | Δ |
|---|---:|---:|---:|
| **paired per-crop** | — | — | **+0.0161** (SE 0.0005, t −33.88, 승률 80.9%) |
| 결정적 full-val | 3.6984 | 3.7145 | +0.0161 |
| 삼진 파라미터 | 54.85M | **50.14M** | −8.6% |
| **packed(안 B)** | **13.050MB** | **12.088MB** | **−7.4%** |
| fp32 상주 | 451.487MB | **415.487MB** | **−8.0%** |
| ⚙ int8 상주 | 86.94MB | 82.29MB | −5.3% |
| `grad_max`(json) | 0.847 | **0.738** | −13% |
| ms/step | — | 3,224 | — |
| 유니크 MLP | 6 | **5** | −1 |

- 헤더 검산: `layers=2+16+2  MLP g=16` ✅ / `타이드 MLP 1그룹 1.0MB (16회 연속 재사용)` ✅
  → **오버라이드가 실제로 걸렸다**(배치가 "g8 이면 이 런은 무가치" 라고 미리 적어 둔 항목)
- 조건 검산: `sched=wsd` / `anneal_end=0.80` / `decay_frac=0.20` / `kd_every=4`(573/2289 ≈ 25%)
  / `seed=1337` / 풀 600M exact / skip 0 — **대조군과 전부 일치** ✅

⚠️ **트레이너 인쇄 "최종 val_loss 3.6628" 을 대조군과 비교하지 않았다.** 그건 `evaluate(...,100)`
추정량이고 대조군의 3.6984 는 `paired_eval` 의 **결정적 full-val** 이다 — **다른 시험지**다(함정 4·결과 015).
**판정은 paired 열 하나로만** 했다.

---

## 2. 예측 대조

| # | 예측 | 결과 |
|---|---|---|
| P1 | **Δ = +0.01 ~ +0.03** | ✅ **적중 (+0.0161).** 구 측정 `t_kd_g16 − t_kd_g8 = +0.0128` 과도 같은 대역 |
| P2 | 분해능 경계라 `paired_eval` 필수 | ✅ 적중 — SE **0.0005 = σ(0.012)의 1/24**. 주기 eval 로는 절대 못 봤다 |
| P3 | 부모초기화가 16층 평균이 되어 g 효과에 섞인다 | ⚠️ **확인만 됨**(`중간 MLP는 16층 평균`) — **분리는 못 했다** |
| — | (예측 없었음) | ★**`grad_max` 가 오히려 내려갔다**(0.847 → 0.738). 타잉을 강화하면 gradient 가 **더** 안정적 |

★ P3 의 교락은 남아 있다: g16 은 **아키텍처**와 **부모초기화 평균 범위**를 동시에 바꾼다.
다만 Δ 가 분해능 미만이므로 **어느 쪽으로 귀속하든 결론이 같다** — 그래서 이번엔 무해하다.

---

## 3. 판정 — **채택 가능 + 축 종결**

`n_middle = 16` 이고 `assert n_middle % mlp_group == 0` 이므로 **g ∈ {1,2,4,8,16}** 뿐이다.
g16 이 **구조적 상한**이고, 그 상한에서 대가가 **분해능 미만**이다.

> **★타잉 강도 축은 여기서 닫힌다.** g 를 더 키우려면 `n_middle` 을 늘려야 하고
> 그건 깊이 변경이라 **다른 실험**(P048 계열)이다.

⚠️ **"채택" 은 REVIEW2 의 권고이지 확정이 아니다.** 시드 1개이고, `paired_eval` 의 t 값은
*"이 두 **체크포인트**가 다르다"* 만 말한다(도구 자체 경고). 아키텍처 서열 주장에는 시드가 여러 개 필요하다.

---

## 4. 다른 레버와의 비교는 결과 032 §4 에 있다

같은 밤에 P046(E=128)·P048(prelude/coda)이 같은 조건으로 돌았다.
**세 레버의 "품질 대가 / 메모리 이득" 효율 비교**가 이 실험들의 가장 큰 산출물이고,
데이터가 가장 많이 모인 **[결과 032 §4](032_20260807_P048-preludecoda-1+1-효율은-g16과-같다.md)** 에 정리했다.

미리 한 줄만: **g16 = 0.0167 nats/MB(packed)** 이고 **prelude/coda 1+1 이 0.0199 로 거의 같다** —
같은 효율선 위에 있고 **P048 이 2.6배 큰 용량을 산다.**

---

## 5. 재현 명령

```
python scripts\runlog.py --name P045_mC_g16 -- python run100m.py train --preset m100R1c --arch tied ^
  --mlp-group 16 --data ko-en --tokens 300M --pool-tokens 600M --exact-cache --steps 2289 ^
  --micro-bs 8 --accum 16 --seq 1024 --lr 1e-3 --sched wsd --anneal-end 0.80 --decay-frac 0.2 ^
  --seed 1337 --eval-every 100 --compile --kd --kd-every 4 --init-from --tag mC_g16

python scripts\runlog.py --name P045_mC_g16 -- python scripts\paired_eval.py ^
  --preset m100R1c --data ko-en --tokens 300M --models mC_wsd mC_g16
```

⚠️ 배치 원문은 `--name mC_g16` 이었다 — 로그 파일명에 계획번호가 없어 사용자가 손수 고쳤다.
**위 명령은 수정본**(`--name P045_mC_g16`)이고 배치도 그렇게 고쳤다(결과 031 §3).
