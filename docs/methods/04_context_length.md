# 4. 컨텍스트 길이

긴 문맥 처리 능력과 그 메모리(KV 캐시) 비용.

| 기법 | 상태 | 버전 | 작동원리(개선 기여) | 트레이드오프 | 특기 |
|---|---|---|---|---|---|
| **RoPE** (theta 10000, max_seq 2048) | ✅ | v4~ | 회전 위치 임베딩 → 상대위치 일반화 | — | 현재 학습 seq 1024 |
| **CLA2** | ✅ | v4~ | KV 2층 공유 → KV 캐시 절반(15→7.5 KB/token) | 표현력 소폭 | 같은 VRAM으로 2배 긴 컨텍스트 |
| **CLA4** | 💡 | — | 4층 공유 → KV 1/4 | 표현력↓ | 메모리 더 필요 시 |
| **KDA 3:1 하이브리드 + NoPE** | 🔜 | — | 채널게이팅 선형 어텐션 3 : softmax 1 → KV 75%↓, 긴 문맥 디코딩 배속 | **GPU 전용 chunkwise 커널(CPU 없음)** | KDA 층에서 NoPE가 RoPE보다 좋음. RULER 128k 우수 |
| **4bit KV** | 🔜 | — | KV 양자화 → 캐시 추가 4배↓ | V가 K보다 비트 더 필요 | TurboQuant: RoPE와 직교변환 충돌(RoPE-first) |
| **InfiniteHiP** | 🔜 | — | 학습 불필요 드롭인, 1장에서 3M 토큰 | 배포 단계 도구 | 계층적 프루닝 |

## KV 캐시 로드맵 (28층, 1.7B 구성 가정)

| 구성 | KB/token | 32K 컨텍스트 |
|---|---|---|
| 층별 독립 | 112 | 3.50GB |
| +CLA2 | 56 | 1.75GB |
| KDA3:1 + CLA2 + 4bit | 3.5 | 0.11GB |
| **KV 캐시 int8/4bit**(신규) | 💡 | — (A-1) | CLA2 로 이미 7.5 KB/token 인 KV 를 더 낮은 비트로 | 정확도 | ~1024ctx 7.5→5.6MB(int8)/**2.8MB(4bit)**. ⚠️**상주 86.9MB 대비 작다** — 긴 컨텍스트를 목표로 할 때만 값어치가 있다 |
| **★멀티스레드는 디코드를 가속하지 않는다** | ✅**측정 완료** | — (C-7) | 1→8 스레드 | — | ★결과 014 §10: 디코드 tok/s **−4%**, 단 **TTFT −42%**. **prefill 만 병렬화된다.** 다시 재지 않는다 |
