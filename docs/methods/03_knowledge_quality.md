# 3. 지식 품질 향상

압축(타잉·삼진)으로 잃는 지식/성능을 되찾거나, 절대 품질을 끌어올리는 기법.
현재 지표는 val cross-entropy(perplexity)뿐이며, 다운스트림 평가는 미도입(한계).

| 기법 | 상태 | 버전 | 작동원리(개선 기여) | 트레이드오프 | 특기 |
|---|---|---|---|---|---|
| **KD (dense 교사 증류)** | ✅**측정·상시** | v6 (`--kd`) | dense 로짓을 KL로 따라가 타잉 손실 복원 | 교사 forward(k4+압축교사로 +19%까지 축소). **어휘 일치 필수** | **실측(P002): +부모초기화로 격차 +0.132→+0.001 소멸(1.82× 유지)**. ★**단, 이득은 예산 의존**(결과 006): 공정풀에서 100M **-0.110** → 300M **+0.062** → 600M **+0.098**. 세 데이터점(005·006·009)이 **"dense 가 약할 때만 교사를 능가"** 로 일관. 교사=자체 dense. 근거 RRT |
| **부모초기화 (average-init)** | 🧪 | v6 (`--init-from`) | 공유 MLP를 dense 그룹 g층 평균으로 초기화 → 수렴·격차 개선 | dense 체크포인트 필요 | **실측(P002): KD와 결합해 격차 소멸의 핵심**. undertrained에서 특히 효과 |
| **더 많은 토큰 / 더 긴 학습** | ✅**측정(부분 기각)** | — | undertraining 해소 → 절대 품질↑ | GPU 시간(선형) | ★결과 006: 절대품질은 개선되나 **수확체감 급격**(100→300M **-0.66**, 300→600M **-0.18**). **"타잉 격차↓" 부분은 기각** — 격차가 6배 토큰에서 +0.149→+0.168→+0.166 로 평평. 300M은 파라미터당 ~4토큰(Chinchilla ~20) |
| **★데이터 풀 다양성** | ✅**측정·채택** | v6 (`--pool-tokens`) | 학습토큰 고정, 샘플 풀만 확대 → 반복 노출 감소 | 토큰화 1회·디스크. 학습시간 증가 **0** | ★결과 006: 풀 300M→600M 만으로 dense **-0.12**(타잉 격차의 70%에 해당). **가장 값싼 품질 레버.** 신규 기준선은 풀 ≥ 2× 학습토큰. 포화점 미측정. 풀이 다른 런끼리 **직접 비교 금지** |
| **ternary-LoRA / FiLM 층별 특화** | 🧪 | v6 | 공유층을 층별로 특화 → 표현력 복원 | 메모리(1번·2번 참조) | 격차 겨냥 최상위 아키텍처 레버 |
| **EMA / 체크포인트 병합** | 🧪 | v6 (`--ema`) | 최근 가중치 평균 → 무료 품질 향상 | decay 스케일 주의(6번) | 근거 WSM/EMA(2025) |
| **다운스트림 평가 도입** | 💡 | — | val CE만으론 품질 판정 불충분 → 벤치마크 필요 | 구현·평가 비용 | 손실↔품질 상관은 과제 의존(Chinchilla 후속). **선행 최소 확인 = P029 정성 프로브**(프롬프트 5개, 수 분) — 정식 벤치마크의 대체가 아니라 "숫자 뒤에 뭐가 있는지" 감각용 |
| **더 큰 외부 교사 증류** | 🔜 | — | 1.7B 확장 시 7B+ 교사로 절대 품질↑ | 어휘 일치/시퀀스·은닉 증류 필요 | "아키텍처 검증"과는 다른 목표(자체 dense와 비교 원칙 유지) |
| **고품질/curated 데이터** | ⚠️**판정 불가** | v4~(부분) | 교과서·QA·FineWeb-Edu 비율↑ → 적은 토큰으로 목표 loss | 절대품질용(타잉 격차엔 무관) | **이미 부분 적용**: en=FineWeb-Edu. ★결과 009(P012): ko-edu-en(`eliceai/korean-webtext-edu`) 관측은 열세(bpb 1.614 vs 1.234)지만 **토크나이저·val셋이 달라 판정 불가**. 게다가 그 캐시는 train 3.03/val 6.17 = **3.14 nats 괴리**(중복 또는 val 분포이동 의심) → ★**결과 011(P028 단계0): 원인 확정** — val 분포이동(JS 9.9× vs 대조군 1.26×)이 주범, 문서중복(5.69%)이 보조. **P012 무효 확정·캐시 폐기 대상.** P028 |
| **bits-per-byte 지표** | ⚠️**적용범위 제한** | v6 | 토크나이저 무관 손실 지표 → 이종 모델 비교 | ★**코퍼스 불변이 아니다** — 현재 bpb 는 각 캐시의 **자기 val.bin** 에 대해 계산된다 | ★결과 009: **같은 데이터셋 안에서만 유효.** 데이터셋이 다르면 평가셋 난이도 차이가 섞여 무효(교차평가는 토크나이저가 달라 원천 불가). 해결 = 공통 원문 eval 셋 + `eval --bpb-text`(P028 미구현). prepare가 bytes/token 저장, evaluate가 bpb 산출 |
| **★★SEO 스팸 필터로 학습** | ✅**측정** | v6 (`--doc-filter`, P028 단계3) | 결과 018 이 실측한 서명(대형 ∧ 줄바꿈~0% ∧ 줄 고유율 100%)으로 문서 제외 후 **그 데이터로 학습** | 학습 예산 고정 시 **유니크 텍스트 13% 감소** | ★**−0.296 nats(24.7σ)** — 우리가 측정한 **가장 큰 단일 품질 개선**이고 **아키텍처가 아니라 데이터**에서 나왔다. `ko-edu-en` 열세의 **63%가 스팸**이었다(결과 011 §2) |
| **★★`ko-en` 에 필터 적용**(신규) | 💡**최우선** | — (D-1) | 표준 코퍼스의 train 쪽 스팸률은 **미측정**이다(결과 018 은 val 만 봤다) | ⚠️양성이면 **전 기준선 재학습** | ~**−0.1~0.3 nats**. ★**우리가 가진 가장 큰 잠재 레버.** 측정만이라도 즉시(GPU 0) |
| **★어휘 재학습(필터 데이터로)**(신규) | 💡 | — (D-7) | ★결과 018: 토크나이저 표본 20만 문서에 **스팸이 있었다** → 어휘가 오염됐을 수 있다 | **전 모델 재학습** | ~압축률 개선 → 같은 예산에 더 많은 정보. bpb −0.01~0.05 추정 |
| ~~FiLM 층별 조건화~~ | 🚫**기각** | v6 (`--mlp-film`, P044) | 공유 MLP 은닉을 층별 scale/shift 로 | 0.13MB(정직하게 계상됨) | 🚫★결과 027: paired **+0.0003**(SE 0.0004, t=0.65). **KD 가 그 자리를 이미 차지했다.** 부수: `grad_max` 0.847→**1.855** |

## 논거 요약

- 손실(CE/perplexity)은 다운스트림과 **집계적으로** 상관(스케일링 법칙)하나, 과제 의존이고
  사전학습 손실이 파인튜닝 후 성능을 깔끔히 예측하진 못함 → val_loss·격차는 **필요하나 불충분**.
- 타잉 페널티는 규모·학습량↑에서 감소(Tying the Loop 7B에서 g=4 동률).
