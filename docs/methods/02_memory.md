# 2. 메모리 감축

배포 메모리(가중치·KV 캐시) 축소. 이 프로젝트의 1차 목표.

| 기법 | 상태 | 버전 | 작동원리(개선 기여) | 트레이드오프 | 특기 |
|---|---|---|---|---|---|
| **g128 삼진 양자화** | ✅ | v4~ | 가중치를 {-α,0,+α} 3진 + g128 그룹 스케일 → 약 1.95bpw | 품질 소폭(어닐로 완화) | llama.cpp Q2_0_g128 / T-MAC 호환. TWN 임계+L2 alpha |
| **MLP 타잉 (g=4)** | ✅ | v4~ | 중간 MLP를 4층 1그룹 공유 → 파라미터 132.5M→72.9M | 손실 격차 | 감축 **1.82×**. 메모리 축의 최대 기여 |
| **CLA2** | ✅ | v4~ | KV를 2층 공유 → KV 캐시 15→7.5 KB/token | 표현력 소폭 | 긴 컨텍스트에도 직접 기여(4번) |
| **Factorized embedding** | ✅ | v4~ | V×E+E×d 저랭크 → 임베딩 메모리 바닥 제거 | 로짓 랭크 E 제한 | (a)64k+(b)E=512 조합 시 9.2× |
| **g 스윕 (g=6, 8, 16)** | ✅**측정** | v6 | 공유 강도↑ → 감축↑ | 손실↑ | g=8 → **2.07×**(63.5M, KD 후 +0.005), g=16 → **2.26×**(58.8M, +0.018). **g8 을 권장 상한**(g4→g8 이득 1.15×뿐). ★결과 006: **격차는 토큰 6배에도 불변(+0.15~0.17)** → 용량 상한이며 학습 부족 아님 |
| **Ternary-LoRA (저비트)** | 🧪 | v6 | 격차 복원을 저비트 LoRA로 → 품질 복원하며 메모리 거의 유지 | r·bits 의존 | r=32 ternary → 1.71× (fp16은 1.46×) |
| **3:4 희소 삼진(1.25bpw)** | ✅**측정** | v6 (`--sparse34`) | 4-블록마다 \|w\|최소 1개 0강제 → 3개만 ±1, 5비트 무낭비 = 1.25bpw. 타잉과 곱셈누적 | **희소 단독 비용 +0.035(g4) / +0.056(g8) — 타잉과 경합해 초선형 악화**. 학습시간 영향 0(+1%) | ★결과 008: **g8_s34 10.3MB(3.00×)/+0.061**, **g4_s34 11.7MB(2.64×)/+0.036** (둘 다 `≤+0.07` 성립, \|g\|max 0.6). **300M 풀 기준이라 공정풀 재검증 필요**(REVIEW1). 실이득은 CPU 5비트 패킹 전제. Sherry/Sparse-BitNet 정합. P016 |
| **어텐션 g=2** | 💡 | — | 어텐션까지 부분 공유 | 층 차별화↓(비싼 영역) | 메모리가 더 필요할 때만 |
| **vocab 64k 축소 / 1-bit 임베딩** | 🔜 | — | 임베딩·lm_head 추가 축소 | 한국어 토큰 증가/토크나이저 재학습 | 1.7B 확장 시 |
| **4bit KV** | 🔜 | — | KV 캐시 추가 압축 | 정확도(2bit V→cos 0.94) | TurboQuant: RoPE-first 필요 |
| **★LUT 기반 mpGEMM(T-MAC/bitnet.cpp)** | 💡**최우선** | — (P014) | **역양자화 없이** 삼진 가중치를 룩업테이블로 곱셈누적. 곱셈을 테이블 조회로 치환 | 커널 구현·플랫폼별 SIMD | ⚠️**2026-08-07 코드 실사**: 라이선스 MIT ✅ · TL 활성값 F32 ✅ · **그러나 스케일이 텐서당 1개**(우리 g128 = 428K개)라 **재양자화 필수·로짓 동등성 불가**. TL 은 shape 하드코딩(3200/8640), **I2_S 는 shape 무관**. ★**우리가 필요한 물건이 이미 오픈소스로 존재한다.** T-MAC(arXiv:2407.00088)은 mpGEMM 을 **dequant 없이** 수행, bitnet.cpp(arXiv:2502.11880)의 **TL**(Ternary Lookup Table)·**I2_S**(Int2+Scale, **무손실**)가 x86 **2.37~6.17×**·에너지 **−71.9~82.2%**, ARM 1.37~5.07×. **우리 `_wq_from_i8()` 는 정확히 그 반대(매번 fp32 복원)를 하고 있다** |
| **★임베딩·헤드 fp32(미해결)** | 🚫**설계 미완** | v6 | 상주 33.0MB 중 **32.0MB 가 `emb.weight`**. `quantize_embedding=True` 는 **회계 플래그일 뿐** — 임베딩은 한 번도 양자화된 적이 없다 | 헤드가 임베딩과 **tie** 돼 있어 int8 저장 시 **forward 마다 32.0MB 복원**(+3.3ms/토큰) | ★2026-08-06 발견. `packed_mb` 27.1MB 는 임베딩 쪽에서도 **없는 포맷을 가정**한다. 어휘 축소는 P039 가 닫음(빈도0 0.28%). 남은 길 = **LUT 커널 / 스트리밍 matvec**(위 행과 같은 물건) |
| **int8 언팩 캐시(타잉 전용)** | ✅**확정(3차)** | v6 (`--unpack-cache`) | 타이된 층이 **같은 MLP 객체**를 g번 부르는데 언팩은 호출당 1회 → **유니크 모듈당 1회**로 | 동시 fp32 버퍼 1벌(≈18.9MB) | 1차 실패(016 §13, 무장 범위) → **3차 확정: mC **+42.7%** / mA **+33.7%**, 대조군 p6d **0.975×**(016 §15). 게이트 로짓 0 + **상주 MB ON/OFF 일치**. 층당 절감 **1.54~1.60 ms/언팩**(두 모델 4% 안). int8 감속이 −67% → **약 −53%** 로 완화. ⚠️**LUT 커널(P014)이 오면 무의미해지는 임시 방편** — 거긴 언팩 자체가 없다 |
| **어휘 축소** | 🚫**폐기** | — | 32,768 → 24,000 으로 임베딩 26.7% 감축 | 토크나이저 + 전 모델 재학습 | 🚫**결과 025 가 닫았다** — `ko-en` 597M 전량 스캔에서 **빈도0 이 53개(0.16%)** 뿐이고 본문 토큰은 0.0%. 잘라낼 것이 **0.05MB** 다. 부수: **타잉 헤드에서 Magikarp 노름 휴리스틱의 부호가 역전**(미사용 노름이 오히려 크다, 비 1.599) |

## 감축 비율 요약 (m100, dense 30.9MB 기준)

| 구성 | 파라미터 | 메모리 | 감축 |
|---|---|---|---|
| 현재 tied (g4, cla2) | 72.9M | 17.1MB | **1.81×** |
| + ternary-LoRA r=32 | — | 18.1MB | 1.71× |
| + FiLM | ~73.0M | ~17.2MB | ~1.80× |
| prelude/coda 3+3 | 85.0M | 19.9MB | 1.55× |
| g=2 | 91.8M | 21.4MB | 1.44× |
| g=8 | 63.5M | 14.9MB | 2.07× |
| g=16 | 58.8M | 13.7MB | 2.26× |
| m100d (깊은, g6) | ~83.6M | ~19.6MB | ~2.27× (자기 dense 대비) |
| **g=4 + 3:4 희소(1.25bpw)** | 72.9M | **11.7MB** | **2.64×** (결과 008 실측, +0.036) |
| **g=8 + 3:4 희소(1.25bpw)** | 63.5M | **10.3MB** | **3.00×** (결과 008 실측, +0.061) |

> **⚠️ 메모리 수치는 `report()` 기준으로 인용한다.** 구 `compare.py` 는 전체 파라미터에 단일
> bpw 를 적용해 **sparse34 런에서만** 0.8MB 과소·비율 과대(9.5MB/3.26×, 10.9MB/2.83×)로 출력했다.
> 임베딩은 3:4 대상이 아니기 때문이다. **✅ 2026-07-30 수정**: `transformer.mem_breakdown()`
> 단일 소스 + json `deploy_mb` 기록. **단 그 이전 로그에는 `deploy_mb` 가 없어** compare 가
> `~(근사·과소)` 로 표시한다 — 그 경우 학습 로그의 `report()` 블록이 정본.
>
> **g16+s34(≈9.9MB, 3.11×)는 권하지 않는다** — 희소 비용이 g4→g8 에서 1.6배로 초선형 악화했으므로
> g16 에서는 `+0.15` 초과가 예상되고, 메모리 이득은 10.3→9.9MB(4%)에 불과하다.

### 3:4 준정형 삼진 (Sherry) — **★2026-07-31 전면 개정**(결과 016 §7)

> **이전 판의 서술 3건이 틀렸다.** 논문 원문(arXiv:2601.07892)과 실측(`diag_sparse34.py`)으로
> 정정한다. **이름의 "희소"부터 오해를 부른다.**

**논문이 실제로 말하는 것** — *Sherry: Hardware-Efficient 1.25-Bit Ternary Quantization via
Fine-grained Sparsification* (arXiv:2601.07892, Tencent AngelSlim).
연속 4-가중치 블록마다 **정확히 3개를 비영(±1), 1개를 0** 으로 고정 →
`N_perm = C(4,3)·2³ = 32 = 2⁵` 로 **5비트를 빈틈없이 포화**시켜 **power-of-two 정렬을 회복**한다.
- **문제의식은 메모리가 아니라 정렬·속도다.** 기존 삼진은 ① 2비트 정렬 패킹(비트 낭비) 또는
  ② 1.67비트 비정형 패킹(**정렬이 깨져 추론이 느려짐**) 중 하나를 골라야 했다.
- **보고 이득: 비트 25% 절약 + 속도 10%**(LLaMA-3.2 1B, Intel i7-14700HX, 정확도 손실 0).
  **25% 는 1.67bpw 대비**다(1.67 × 0.75 ≈ 1.25).
- **Arenas = annealing residual synapse.** 3:4 를 그냥 씌우면 **weight trapping**
  (gradient 균질화 → 가중치 국소 축적 → representational collapse)이 난다고 논문이 명시하고,
  그 대응으로 도입한 **별도 메커니즘**이다.

**우리 문서가 틀렸던 곳**

| # | 이전 서술 | 실제 | 근거 |
|---|---|---|---|
| 1 | "현 **1.95** 대비 **×1.56** 추가절감" | 1.95 는 **논문에 없는 숫자**(Bonsai/PrismML 출처를 섞었다). 동일 회계 실측 절감은 **15~18%** | 016 §7.4 |
| 2 | "**25% 강제-0 의 용량손실**" | **방향이 반대.** 우리 표준 TWN 경로는 이미 **43~46% 가 0** 이고, 3:4 는 0 을 **25% 로 줄인다** | 016 §7.2 |
| 3 | "Arenas = **우리 삼진 어닐의 특수형**(이미 보유)" | **근거 없는 동일시.** 우리 `quant_anneal` 은 STE 위 어닐 계수이고 residual synapse 가 아니다. **우리는 대응책 없이 3:4 를 씌웠다** | 016 §7.5 |

**실측(결과 016 §7)**

| 항목 | 값 |
|---|---|
| 표준 TWN 비영 | 54.4 ~ 56.8% (0 이 43~46%) |
| **3:4 비영** | **75.00%** — 표준보다 **18~21%p 조밀** |
| TWN 유지 → 3:4 제거 | **2.5 ~ 3.3%** ← 블록 규칙이 죽인 **큰 가중치**. 품질 비용의 기전 후보 |
| 상대 재구성오차 | 3:4 가 TWN 의 **1.15 ~ 1.29×** |
| 파라미터 개수 변화 | **0** — 상주 메모리 이득도 **0**(결과 016 §7.1) |
| **★임베딩 랭크 축소(E 256→128)** | 🧪**준비완료** | v6 (`--emb-rank`, P046) | `emb = V×E + E×dim` 이라 E 에 **선형**. int8 이후 상주 87.1MB 중 **32.0MB 가 emb.weight** | **로짓 랭크가 E 로 제한**된다(헤드 tie) | ⚙**상주 −18.8%**(→70.7MB), packed 2.07→**2.22×**. 같은 3.5h 를 쓰는 g16(−5.3%)의 **3.5배**. ⚠️`--init-from` 이 임베딩에 안 걸린다(shape 불일치 → 건너뛰고 경고) = **교락** |
| **★타잉 상한 g16** | 🧪**준비완료** | v6 (`--mlp-group 16`, P045) | `n_middle=16` 이라 **구조적 상한**. 중간 유니크 MLP 2→1 | 품질(구 측정 +0.0128, 분해능 안) | ⚙상주 **−5.3%**, packed **2.24×**. **타잉 축을 종결**하는 것이 본 목적 |
| **★prelude/coda 1+1**(신규 아이디어) | 💡 | — (A-6) | 유니크 MLP 6→4(g8). 결과 002 의 "2+2 가 최대 이득" 은 **전층 타잉 대비**였다 | ⚠️**품질 리스크가 g16 보다 클 것으로 추정** — prelude/coda 는 임베딩 입출력 조정 | ⚙삼진 54.85→**45.4M**, 상주 **−14%**, packed **2.38×**. **구현 0**(프리셋 필드) |
| **KV 캐시 int8/4bit**(신규) | 💡 | — (A-1) | KV 7.5 KB/token → int8/4bit | 정확도 | ~상주 −5.6MB(int8). **긴 컨텍스트에서 선형 증가**. 📄TurboQuant 계열은 RoPE-first 요구 |
| **삼진 α 를 fp16 으로**(신규) | 💡 | — (A-5) | α 가 `(O, I//128)` fp32 = mC 428K×4B = **1.7MB** | 정밀도 소폭 | ⚙**−0.85MB**. ★본 이득은 **packed 회계의 `SCALE_BITS=16` 과 정합**해지는 것 |
| ~~어휘 축소~~ | 🚫**폐기** | — | 32,768 → 24,000 | — | 🚫**결과 025**: `ko-en` 597M 전량 스캔 빈도0 **53/32,768 = 0.16%** → 잘라낼 것이 **0.05MB** |
| ~~헤드 untie 로 임베딩 int8~~ | 🚫**계산상 역효과** | — (A-3) | tie 를 풀면 임베딩만 int8 가능 | 헤드가 **+8.4M 파라미터** | 🚫⚙임베딩 −23.8MB 이나 헤드 **+33.6MB** → **순 +10MB 손해**. 계산해 보고 접었다 |

**동일 회계 저장 MB**(016 §7.4): `mA` 14.6 vs `mC` 14.7 → **0.7% 차이, 사실상 동률.**
현행 회계의 11.6 vs 14.7(21%)은 **분자·분모 규약 불일치에서 온 착시**다.
`mA` 감축비도 2.66× → **2.11×**, `mC` 는 2.10× 로 **불변**.

**상태**: 구현완료(`--sparse34`, 2026-07-25) — `_TernarySTE` 3:4 경로 + report/compare 1.25bpw 분기.
**단 ① Arenas 미구현 ② 5비트 패킹 커널 미구현** — 이 둘이 없으면 논문의 이득(25%/10%)은
**어느 것도 실현되지 않는다.** 실행 명령은 결과 008 §5. 감사 재현은 `scripts/batch/run_P034_sparse34.bat`.
Sparse-BitNet(arXiv:2603.05168)은 "삼진 LLM은 준정형 희소에 친화" 근거로 유지.
