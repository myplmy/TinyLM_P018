# P014B — LUT 커널(T-MAC / bitnet.cpp) 실사 및 속도 게이트

> **신설 2026-08-07.** [P014](P014_커스텀삼진커널-검증-done.md) 의 **실사 단계**를 분리한다.
> P014 본체는 **커널 구현·검증**을 소유하고, 이 문서는 **"착수해도 되는가" 를 값싸게 판정**한다.
>
> **근거**: [`docs/20260807_T-MAC와-bitnetcpp-실사-우리구조적용가능성분석.md`](../docs/20260807_T-MAC와-bitnetcpp-실사-우리구조적용가능성분석.md)

---

## 1. 왜 — 우리가 반복해 온 실수를 여기서 안 하려고

이번 세션에만 **같은 실수를 두 번** 했다(결과 016 §13·§14): **설계를 덜 마치고 구현부터 했고,
대조군이 아니었으면 둘 다 이득으로 기록될 뻔했다.**

P014 는 그보다 훨씬 비싸다 — 외부 C++ 의존, 포맷 변환, 플랫폼 빌드. 그래서 **먼저 잰다.**

### 1.1 아직 모르는 것 세 가지 (이게 게이트다)

| # | 미지 | 왜 치명적인가 |
|---|---|---|
| **U1** | **우리 모델 크기(dim 768)에서 LUT 가 이득인가** | 논문들은 **3B급(dim 2560+)** 에서 잰다. LUT 는 **표 구축 비용을 재사용으로 상각**하는데, 작은 행렬은 재사용 기회가 적다. **이득이 0 이거나 음수일 수 있다** |
| **U2** | **`micro_group=128` 그룹 스케일을 커널이 받는가** | BitNet b1.58 원형은 **텐서 단위 absmean** 스케일이다. 커널이 그룹 스케일을 모르면 α 를 **밖에서** 곱해야 하고, 그게 또 하나의 fp32 순회가 된다 |
| **U3** | **활성값 int8 을 요구하는가** | 요구하면 **정확도 영향**이 생긴다. 지금 우리 int8 경로는 로짓 게이트 `0.000e+00` 인 **무손실**인데 그 성질을 잃는다 |

**셋 중 하나라도 부정이면 P014 의 비용-이득이 뒤집힌다.**

---

## 2. 질문

| # | 질문 |
|---|---|
| Q1 | T-MAC / bitnet.cpp 커널이 **우리 가중치 포맷을 받는가** (삼진 코드 + g128 α) |
| Q2 | **우리 크기에서** 역양자화 제거가 실제로 몇 배인가 |
| Q3 | 라이선스·빌드가 우리 환경(Windows·x86)에서 성립하는가 |
| Q4 | `mA`(sparse34)를 포기해야 하는가 |

---

## 3. ★예측 — 정직하게

| # | 예측 | 근거 | 틀리면 |
|---|---|---|---|
| P1 | **`mC` int8 16.1 → 25~40 tok/s (1.5~2.5×)** | 결과 014 §11 분해: 언팩이 층당 약 2ms × 20층 = 40ms 를 62.3ms 중에서 차지. 고정비 4.2ms 는 안 줄어든다 | **6.17× 가 나오면 우리 계측을 의심한다** — 그건 3B·전정밀 기준 숫자다 |
| P2 | **U2 는 부정일 가능성이 높다** | BitNet 원형이 텐서 스케일이다 | 그룹 스케일을 받으면 통합이 훨씬 쉬워진다 |
| P3 | **U1 이 가장 위험하다** | 표 구축은 활성값당 고정비, 재사용은 행렬 폭에 비례. dim 768 은 3B 의 1/3.3 | 이득이 크면 P014 즉시 진행 |
| P4 | **`mA` 는 이 경로에서 배제된다** | 5비트/4가중치 코드북을 두 구현 다 모른다 | 확장 가능하면 mA 가 되살아난다 |

> **★P1 을 계획서와 배치 양쪽에 적어 둔다.** 그래야 결과가 나왔을 때
> "예상대로" 로 뭉개지지 않는다. **그리고 "몇 배" 를 인용할 때는 항상 기준선을 함께 적는다.**

---

## 4. 단계 설계

### 단계 0 — **문헌·코드 실사** ⏸ **착수(2026-08-07)** · GPU 0 · 배치 없음

**이건 실험이 아니라 조사다. 배치를 만들지 않는다.**
그리고 **AI 가 혼자 못 한다** — 논문 본문 PDF·코드 저장소는 AI 의 웹 접근으로는
**초록 수준까지만** 닿는다(그게 `08_paper_review` §5 가 "본문 미독" 이라고 적은 이유다).

---

#### 0.A ★역할 분담 — 사용자가 할 것 / AI 가 할 것

| # | 항목 | **사용자** | **AI** | 왜 이렇게 나누나 |
|---|---|---|---|---|
| **U1** | **bitnet.cpp 논문 PDF 확보**([arXiv:2502.11880](https://arxiv.org/abs/2502.11880) 또는 [ACL 2025 pdf](https://aclanthology.org/2025.acl-long.457.pdf)) → `article/` 에 저장 | ⭕ 다운로드 | ⭕ 저장되면 **읽고 §5.2 의 U2·U3 판정** | AI 의 웹 도구는 대용량 PDF 본문을 못 읽는다. **파일로 주면 읽는다** |
| **U2** | **T-MAC 논문 PDF**([arXiv:2407.00088](https://arxiv.org/abs/2407.00088)) → `article/` | ⭕ | ⭕ 읽고 커널 인터페이스·타일링 조건 정리 | 위와 같다 |
| **U3** | **저장소 클론**(작업폴더 **밖**에) — `microsoft/BitNet`, `microsoft/T-MAC` | ⭕ `git clone` | 🚫 | AI 는 네트워크로 코드를 못 가져온다 |
| **U4** | 클론한 저장소에서 **다음 파일만** 작업폴더로 복사 → `article/lut_ref/` | ⭕ 복사 | ⭕ **읽고 분석** | 저장소 전체는 크고 gitignore 를 오염시킨다. **필요한 것만** |
| | └ `LICENSE` (양쪽) | | | 라이선스 판정 |
| | └ `README.md` (양쪽) | | | 빌드 요구·지원 플랫폼 |
| | └ BitNet: `include/*.h`, `src/ggml-bitnet*.{c,cpp,h}` 계열 | | | **커널 API·스케일 규약** |
| | └ T-MAC: `include/`·`python/t_mac/` 의 인터페이스 파일 | | | 같은 목적 |
| **U5** | **빌드 시도**(선택, 시간 여유 있을 때) — BitNet README 절차대로 | ⭕ | ⭕ 실패 로그를 받으면 진단 | Windows/x86 빌드 가능 여부 = **G0 의 핵심** |
| **U6** | 위 진행 중 **백그라운드 실험 실행**(§0.C) | ⭕ | ⭕ 배치는 미리 만들어 둔다 | **연산자원을 놀리지 않는다** |

> **★사용자 작업이 U1~U4 로 끝나면 AI 가 나머지를 전부 한다.**
> U5(빌드)는 오래 걸리므로 **U6 과 병행**한다.

#### 0.B AI 가 U1~U4 를 받으면 하는 일

1. **U2 판정** — I2_S 의 "Scale" 이 **텐서 단위인가 그룹 단위인가**.
   우리는 `micro_group=128` 이다. 다르면 **α 를 밖에서 곱해야 하고 이득이 깎인다**
2. **U3 판정** — TL/I2_S 가 **활성값 int8 을 요구**하는가. 요구하면 우리 무손실 성질을 잃는다
3. **가중치 포맷** — GGUF 변형인가 자체인가 → 단계1 변환기의 난이도가 정해진다
4. **라이선스** — 우리 저장소에 넣을 수 있는가(넣을 필요가 없을 수도 있다 — 안 A 는 외부 실행)
5. **커널 호출 인터페이스** — C API 로 노출되는가, llama.cpp 내부 전용인가
   → **안 B(PyTorch custom op)의 가능 여부**가 여기서 갈린다
6. 결과를 **`docs/methods/08_paper_review_202608.md` §5 에 근거 링크와 함께** 갱신하고
   **§5.2 의 "확인 필요" 표를 확정 표로** 바꾼다

#### 0.C ★U5 를 기다리는 동안 — **연산자원을 놀리지 않는다**

단계0 은 **사람 시간**을 쓰고 **GPU 를 안 쓴다.** 그동안 GPU 가 놀면 손해다.

| 우선 | 배치 | GPU | 시간 | 왜 지금 병행해도 되나 |
|---|---|---|---|---|
| **1** | `run_P044_film_recheck.bat` | 예 | **3.5h** | **완전 독립.** 태그 `mC_film`, 대조군 재사용 |
| 2 | `run_P042_*`(구현 후) | 예 | 1h | 독립 |
| 3 | `run_P043_stage1_*`(G0 통과 후) | 예 | 4.5h | 독립 |

> ⚠️ **속도 측정 배치와는 병행 금지**(결과 016 §12.5). P044·P042·P043 은 **학습**이라
> 서로/실사와 겹쳐도 무방하지만, **`run_P040`·`run_P034_*` 는 단독**이어야 한다.

**게이트 G0**: 라이선스가 허용되지 않거나 Windows 빌드가 불가하면 **여기서 종료**.

### ✅ 단계 0 결과 (2026-08-07) — **U2 부정 · U3 양호 · U1 은 길이 갈렸다**

사용자가 U1~U4 를 완료해 논문 2건과 코드를 `article/lut_ref/` 에 넣었다.
**추정이 아니라 코드로 확정**했다. 전문은
[`docs/20260807_...분석.md`](../docs/20260807_T-MAC와-bitnetcpp-실사-우리구조적용가능성분석.md) §13.

| 미지 | 판정 | 근거(코드) |
|---|---|---|
| **U2** 그룹 스케일 | 🚫 **부정. 우회 불가** | `quantize_i2_s` 가 텐서 전체에서 `max` 1개 → `scale_ptr[0]`. TL 도 `aligned_malloc(sizeof(bitnet_float_type))` = **1개**. 우리는 `(O, I//128)` 약 **428K개** |
| **U3** 활성값 | ✅ **TL 은 F32** / ⚠️ I2_S 는 int8(W2A8) | `ggml_bitnet_can_mul_mat` 이 `src1->type == GGML_TYPE_F32` 를 요구. 그리고 **`src1->ne[1] <= 1` = 배치 1 전용** — 우리 벤치와 일치 |
| **U1** 우리 크기 | ⚠️ **갈렸다** | `kernel_config.ini` 는 **3200/8640 하드코딩**(BitNet-3B) → **TL 은 codegen 필요**. 반면 **I2_S 는 `gemm-config.h` 의 일반 블록 GEMM 이라 shape 무관** |
| 라이선스 | ✅ **MIT**(양쪽) | `LICENSE` |
| 예측 대조 | **P2 적중**(텐서 스케일) · P4 적중(mA 배제) · **P1·P3 미검정** | |

**★U2 가 G1 을 무너뜨린다.** 우리 α 는 `(O, I//g)` 라 활성값에도 출력에도 **접을 수 없다.**
텐서당 1개로 재양자화하면 **가중치가 바뀐다** → **로짓 동등성은 원리적으로 불가**.
→ **단계1 의 게이트를 "동등성" 에서 "허용 오차 + 품질 재측정" 으로 바꿔야 한다.**

### ★단계 0.5 — **더미 벤치**(신설·최우선) ⏸ · GPU 0 · 수 분

**실사가 지름길을 찾았다.** `README.md:375`:

```
python utils/generate-dummy-bitnet-model.py models/bitnet_b1_58-large        --outfile models/dummy-bitnet-125m.tl1.gguf --outtype tl1 --model-size 125M
python utils/e2e_benchmark.py -m models/dummy-bitnet-125m.tl1.gguf -p 512 -n 128
```

**임의 크기 더미 모델로 벤치할 수 있고 `--model-size 125M` 이 우리 132M 과 거의 같다.**

> **★그래서 U1 을 변환기 없이·우리 체크포인트 없이·품질 논의 없이 먼저 답한다.**
> **이득이 없으면 여기서 P014 가 끝난다.** 종전 설계(단계1 변환기 반나절 → 단계2)보다
> **훨씬 값싸고 순서가 옳다.**

- **선결**: 빌드(U5). §단계0 의 G0 중 라이선스는 통과, 빌드는 미결
- **판정**: 같은 기계에서 **우리 `mC` int8 (16.06 tok/s)** 와 나란히 놓는다
- ⚠️ **더미는 무작위 가중치**라 **속도만** 유효하다. 층 구성도 우리와 다르다
  (우리는 MLP 타잉 + CLA) → **절대값 직접 비교 금지, 자릿수만 읽는다**

### 단계 1 — **포맷 변환기** ⏸ **단계0.5 통과 후**(순서 변경) · GPU 0 · 구현 소~중

우리 체크포인트 → 그쪽 포맷. **읽기 전용 내보내기이고 `tinylm/` 을 건드리지 않는다.**

- `scripts/export_ternary.py`(신설 예정): `TLinear._i8`(int8 코드) + `_alpha`(g128) 를 내보낸다
- **U2 가 부정이면** 여기서 **α 를 텐서 단위로 재계산**해야 하고, 그건 **가중치를 바꾸는 것**이다
  → **로짓 동등성이 깨진다** → 그 경우 **품질 재검증이 별도로 필요**하다(비용 증가)
- **★게이트 G1 개정(단계0 결과 반영)**: U2 가 **부정으로 확정**됐으므로 **로짓 동등성은 목표가 아니다.**
  대신 **두 가지를 잰다**:
  1. 텐서당 α 로 재양자화한 모델의 **공통 원문 bpb**(`common_bpb.py`) vs 원본
  2. 그 차이가 **분해능(0.008 bpb) 안인가**
  ⚠️ **넘으면 P014 는 "우리가 g128 을 지원하는 커널을 직접 만든다"(안 C)로 되돌아간다** —
  그게 원래 P014 의 정의였고, 외부 구현 재사용이라는 지름길이 닫히는 것뿐이다

### 단계 2 — **속도 게이트** ⏸ 단계1 통과 후 · GPU 0 · 수 분

**같은 기계·같은 프롬프트·같은 max_new 로** 세 조건을 나란히:

| 조건 | 값 |
|---|---|
| 우리 PyTorch int8 (기준선) | `mC` **16.06 tok/s** (결과 016 §14 [3/5]) |
| 우리 PyTorch int8 + 언팩 캐시 | `mC` 18.76 (3차 수정 후 재측정 예정) |
| **LUT 커널** | ? |

- **측정 위생**(결과 016 §12.5): 단독 실행 · 직전 5분 유휴 · **같은 세션에 기준선 포함해 비율로**
- **게이트 G2**: **1.3× 미만이면 P014 를 접는다.** 그 아래면 언팩 캐시(순수 PyTorch, 이미 완료
  직전)로 충분하고 외부 의존을 질 이유가 없다

### 단계 3 — **P014 로 인계** ⏸ G2 통과 후

여기서부터는 P014 본체(커널 통합·정확도 검증·메모리 실측)가 소유한다.

---

## 5. 판정 기준

| 게이트 | 통과 조건 | 실패 시 |
|---|---|---|
| **G0** 실사 | 라이선스 허용 + Windows/x86 빌드 가능 | **종료.** 안 C(직접 구현)만 남고 그건 별도 판단 |
| **G1** 정합 | 변환 결과가 우리 출력과 일치(또는 정해 둔 허용 오차 안) | 포맷 불일치 원인을 기록하고 종료 |
| **G2** 속도 | **≥ 1.3× vs 우리 int8 기준선** | **P014 접는다.** 언팩 캐시로 만족 |

> **★G2 를 1.3× 로 잡은 이유**: 언팩 캐시가 이미 `mC` 에서 약 1.17×(대가 포함) 를 냈고
> 3차 수정 후 1.7× 추정이다. **외부 의존을 지려면 그보다 확실히 나아야 한다.**

---

## 6. 비용

| 단계 | GPU | 사람 시간 | 되돌리기 |
|---|---|---|---|
| 0 실사 | 0 | 수 시간 | 쉽다(문서만) |
| 1 변환 | 0 | 반나절~ | 쉽다(신규 스크립트, 본체 무변) |
| 2 측정 | 0 | 수 분 | 쉽다 |
| (3 인계) | — | 큼 | 어렵다 → **그래서 G2 가 있다** |

---

## 7. 한계

- **U1(모델 크기 효과)은 단계2 에서만 답이 나온다.** 단계0·1 을 다 해야 알 수 있다는 뜻이고,
  그게 이 계획의 **매몰 비용 리스크**다. 대신 단계0·1 이 GPU 0 이고 본체를 안 건드린다.
- 단계2 는 **두 런타임(PyTorch vs C++)** 을 비교한다. 프레임워크 오버헤드가 섞이므로
  **"커널만의 이득" 이 아니다.** 그래도 **배포 관점에서는 그게 맞는 비교**다.
- `mA` 는 이 경로에서 배제된다(§P4). **아키텍처 판정이 아니라 도구 생태계 판정**이다.
- 우리 측정은 Windows·x86·1스레드다. 논문은 M2-Ultra·Raspberry Pi 다. **배수를 옮겨 쓰지 않는다.**

---

## 8. preflight

- [ ] `tinylm/` 을 **수정하지 않는다**(단계0~2 전부 읽기 전용 + 신규 스크립트)
- [ ] 체크포인트를 **읽기만** 한다
- [ ] 외부 저장소는 **작업 폴더 밖**에 클론한다(gitignore 오염 방지)
- [ ] 속도 비교는 **같은 로그 안에 기준선 포함**(결과 016 §12.5)
- [ ] 인용하는 모든 배수에 **기준선을 함께 적는다**(결과 016 §7.4 의 2.64×→2.11× 재발 방지)
