# P026 — cooldown-QAT 융합 스케줄 점검 (저비용·즉효)

> **✅ 전 단계 완료 2026-07-31 — 결과: [015](../test_result/015_20260731102500_P026-cooldownQAT-스케줄정렬.md) (§1~§7)**
>
> **최종 결론 — 가설 기각, 대조군이 이겼다.**
>
> | | 전이 0.60 | 전이 0.80 | 정렬 효과 |
> |---|---|---|---|
> | 2289 steps | 3.6275 | 3.6147 | −0.0128 (**1.07σ**) |
> | 1946 steps | **3.7086** | 3.7080 | **−0.0006 (0.05σ)** |
>
> **정렬은 두 예산 모두에서 검출되지 않았다**(짧은 예산에서 오히려 더 작다) → **불필요 확정.**
> 실제 이득은 **`--sched wsd` −0.0755(6.3σ)**.
> **★"15% 절감이 공짜"는 cosine 을 기준선에 둘 때만 성립한다** — wsd 기준선 대비로는 **+0.081 의 비용**이다.
> **P026 종결. 추가 실험 없음.** "왜 정렬이 안 통했나"는 **[P035](P035_어닐형태-QAT전이스케줄.md)** 가 이어받는다.

근거: *Compute-Optimal Quantization-Aware Training* (arXiv:2509.22935) ·
*Mapping the Schedule × Bit-Width Boundary in Sub-100M QAT* (arXiv:2605.25966, 우리 스케일 직결).

## 왜
연산 절감 중 **가장 저비용**: 새 커널·의존성 없이 **학습 스케줄만 조정**. 논문 요지 = LR cooldown(감쇠)
구간과 QAT(양자화 인식 학습)를 **동시 수행**하면 "full-precision로 먼저 학습 후 별도 QAT" 대비 중복
업데이트를 제거해 총연산↓. 우리는 이미 삼진 어닐 + WSD/cosine 감쇠를 쓰므로, **어닐-감쇠 스케줄 정렬만으로
흡수 가능**한지 점검한다.

## 현재 상태 점검 포인트 (trainer.py)
- 어닐: `anneal = (s/steps - a0)/(0.60 - a0)` → **0.60 지점에서 완전삼진 도달**.
- LR: WSD면 마지막 `decay_frac`(기본 0.2)에서 감쇠, cosine이면 전 구간.
- 즉 **완전삼진(0.60~)과 LR 감쇠(0.80~, WSD) 구간이 어긋남**. 논문은 감쇠=QAT 강화 구간을 겹치라 제안.

## 가설 (검증 대상)
어닐 완료 지점을 LR 감쇠 시작과 **정렬**(예: 어닐 종료 0.60→0.80, decay 0.80 시작)하거나, 감쇠 구간에서만
완전삼진을 강제하면, 동일 최종 val을 **더 적은 스텝**(짧은 총 steps)으로 달성 가능한지.

## 구현 (완료 — 2026-07-30)

플래그 2개만 추가. **기본값이 종전 동작과 동일**하므로 기존 명령·로그는 무영향.

| 플래그 | 기본 | 의미 |
|---|---|---|
| `--anneal-end F` | **0.60** | 완전삼진 도달 진행률. 종전 하드코딩 0.60 을 그대로 기본값화 |
| `--decay-frac F` | **0.2** | wsd 마지막 LR 감쇠 구간 비율(종전 `_lr_factor` 기본값을 노출) |

변경 파일:
- `trainer.py` — `train(..., anneal_end=0.60, decay_frac=0.2)`. 어닐식의 하드코딩 `0.60` →
  `anneal_end`, `_lr_factor(...)` 에 `decay_frac` 전달. `(0,1]` assert.
  `[sched] anneal_end=.. decay_frac=.. LR감쇠시작=.. 정렬됨/미정렬` 진단 출력(기본값이면 미출력).
  `res` 에 `anneal_end`·`decay_frac` 기록.
- `cli.py` — `--anneal-end`, `--decay-frac` + `train()` 배선.

**정렬 검증(오프라인 계산, 2289스텝·warm 100)**

| 설정 | 완전삼진 step | LR 감쇠시작 step | 정렬 |
|---|---|---|---|
| `cosine --anneal-end 0.60` (현행) | 1374 (0.60) | 147 (0.06) | — (cosine은 전구간 감쇠) |
| `wsd --anneal-end 0.60` | 1374 (0.60) | 1861 (0.81) | ❌ 487스텝 어긋남 |
| **`wsd --anneal-end 0.80`** | 1832 (0.80) | 1861 (0.81) | ✅ 29스텝(1.3%) |

> 1.3% 잔차는 어닐이 `s/steps`, LR이 `(s-warm)/(steps-warm)` 기준이라 생기는 warmup 오프셋이다.
> 완전 일치를 원하면 `--anneal-end 0.81`. 본 실험은 0.80 으로 충분.

## 실험 매트릭스 (m100, ko-en, 300M, dense, `--no-ckpt`)

배치: `run100m_P026.bat`. 전부 `qb_*` 태그(정본 미오염). 총 ~5.2h.

| # | 태그 | 스케줄 | steps | 확인 |
|---|---|---|---|---|
| 1 | `qb_cos` | cosine, 어닐0.60 | 2289 | **실험 내부 대조군**(+ 정본 dense 3.8241 대비 드리프트 = 노이즈 증거) |
| 2 | `qb_wsd60` | wsd, 어닐0.60 | 2289 | wsd-vs-cosine 성분 분리(정렬 효과와 구분) |
| 3 | `qb_wsd80` | **wsd, 어닐0.80(정렬)** | 2289 | 동일 스텝에서 정렬이 도움이 되는가 |
| 4 | `qb_wsd80_s85` | wsd, 어닐0.80(정렬) | **1946(-15%)** | ★같은 val 을 15% 적은 스텝으로? |

> `--no-ckpt` 를 전 런에 적용(결과 007: -17.3%, 13.5GB). 이 때문에 **정본 dense 와 직접 비교하지
> 않고 #1 을 대조군으로 쓴다**(결과 007 §2-(4): grad-ckpt 경로 차이가 수치 드리프트를 만든다).

## 판정

1. `#3 vs #1` — 동일 스텝 정렬 효과. **단, 결과 007에서 확인된 재현 드리프트보다 큰 차이만 인정.**
2. `#4 vs #1` — **핵심**. #4 가 #1 의 val 에 도달하면 정렬로 **15% 스텝 절감** 확정 → 전 실험 표준 갱신.
3. `#2 vs #1` — wsd 자체 효과를 빼야 정렬 기여분이 나온다.
4. `#1 vs 정본 dense(3.8241)` — 같은 설정 드리프트. **±0.07 판정기준의 첫 실측 근거**(부수 소득).

효과 없으면 현행 유지(음성결과 기록). 리스크 낮음(스케줄만, 기본값 무변).

## 우선순위

**착수 확정(2026-07-30, 사용자 승인)** — 저비용·즉효라 P023 Sophia 보다 먼저.
성공 시 모든 실험 총 steps 에 곱해지는 무료 이득. **P026 통과 후 P023 착수.**

---

## 실행 이력

| 일자 | 단계 | 배치 | 결과 | 계획과 달라진 점 |
|---|---|---|---|---|
| 2026-07-31 | **매트릭스 4런 ✅ 완료** | `run100m_P026.bat` | [015](../test_result/015_20260731102500_P026-cooldownQAT-스케줄정렬.md) | 없음(계획대로). 총 6.2h(예측 5.2h) |
| 2026-07-31 | **단계 5 ✅ 완료** | `run_P026_stage5.bat` | [015 §7](../test_result/015_20260731102500_P026-cooldownQAT-스케줄정렬.md) | 없음. 93.5분(예측 82분 — **P001B 동시 실행으로 GPU 경합**, val 은 무영향) |

## 예측 대조

| 계획의 예측 | 실측 | 판정 |
|---|---|---|
| ~~정렬(`anneal_end` 0.60→0.80)이 동일 스텝에서 val 을 낮춘다~~ | **−0.0128 (1.1σ)** — 2σ=0.024 미만 | ❌ **검출 실패**(효과가 없다는 증명은 아니다. 이 예산에서 분해능 아래일 뿐) |
| ~~★정렬로 15% 적은 스텝에 같은 val 도달~~ | **+0.0050 (0.4σ)** vs cosine 기준선. **단계5 에서 정렬 없이도 +0.0056 → 공로는 wsd** | ⚠️ **절반 성립.** 도달은 맞지만 **"정렬 덕분"은 기각**. 그리고 wsd 기준선 대비로는 **+0.081 의 비용**이라 "공짜"가 아니다 |
| (단계5 예측) 짧은 예산에서는 정렬이 필요할 수 있다 | **−0.0006 (0.05σ)** — 2289 의 1.07σ 보다 **더 작다** | ❌ **기각** |
| `#2`(wsd) 는 성분 분리용 대조군일 뿐 | **−0.0755 (6.3σ)** — 배치 최대 효과 | ⚠️ **예측 밖.** 대조군이 결론을 냈다 |
| `#1 vs 정본 dense 3.8241` 로 드리프트 측정 | **무효**(정본은 300M 풀). 대신 `p6d`(600M 풀) 대비 **−0.0016** | ⚠️ 비교 대상 교체. 얻은 값은 **grad-ckpt 드리프트**(스케줄 드리프트 아님) |

> **왜 빗나갔나(가설, 미검증)**: 우리 어닐은 `s/steps` 선형이고 완전삼진 도달 후에도 STE 로 계속
> 학습된다. 논문이 상정한 "FP 학습 종료 → 별도 QAT 단계"의 **중복 업데이트가 애초에 없었을**
> 가능성이 있다. 그렇다면 정렬로 제거할 낭비가 없는 것이 자연스럽다. **검정하려면 어닐 스케줄
> 자체(선형 vs 계단)를 바꿔야 하며 이 계획의 범위 밖이다.**
> → **[P035](P035_어닐형태-QAT전이스케줄.md) 로 분리 신설**(2026-07-31, 제안·미승인).
> 기존 `qb_wsd60`·`qb_wsd80` 을 대조군으로 재사용해 **신규 2런(3.2h)만으로 2×2 를 완성**한다.

## 단계 5 — 빠진 칸: `qb_wsd60_s85` ✅ **완료 2026-07-31 (결과 015 §7)**

> **결과: `qb_wsd60_s85` 3.7086 vs `qb_wsd80_s85` 3.7080 = +0.0006 (0.05σ) → 구분 불가.**
> 아래 표의 **첫 행**이 성립했다: **정렬 불필요, `anneal_end` 0.60 유지, `--sched wsd` 만 채택.**
> `docs/EXPERIMENT_BASELINES.md` §1 스케줄 행 **개정 완료**.

**정렬이 스텝 절감의 필요조건인가**를 가르는 유일한 실험. 1런 ≈ 82분.

```
python run100m.py train --arch dense --data ko-en --tokens 300M --steps 1946 ^
  --micro-bs 8 --accum 16 --seq 1024 --lr 1e-3 --eval-every 100 --compile --no-ckpt ^
  --pool-tokens 600M --exact-cache --sched wsd --anneal-end 0.60 --decay-frac 0.2 ^
  --tag qb_wsd60_s85
python run100m.py compare --tag qb_wsd80_s85 --vs qb_wsd60_s85
```

| 결과 | 결론 | 표준조건 조치 |
|---|---|---|
| `qb_wsd80_s85` 와 **구분 불가**(\|Δ\|<0.024) | 정렬 불필요. **wsd 만이 원인** | §1 스케줄 `cosine → wsd`, `anneal_end` 는 **0.60 유지** |
| `qb_wsd80_s85` 가 **유의하게 낮음** | 짧은 예산에서 정렬이 필요 | 스케줄 `wsd` + `anneal_end 0.80` 을 함께 표준화 |

**이 런 전에는 `docs/EXPERIMENT_BASELINES.md` §1 을 고치지 않는다** — 지금 고치면
"정렬을 채택했다"는 잘못된 근거가 문서에 박힌다.

## 한계 (결과 015 반영)

- **dense 전용 측정.** tied·KD·sparse34(REVIEW1 승자 mA)에서 wsd 이득이 재현되는지 미확인.
- σ=0.012 는 **cosine·2289스텝·표본 2개** 추정(결과 012). wsd·1946스텝의 σ 는 미측정.
- "15% 절감"은 2289→1946 **한 점**의 관측이다. 절감률–품질 곡선은 없다.
- `best` 값으로 판정하면 결론이 뒤집힌다(결과 015 §2-(6)) — **판정은 final(100 iters) 끼리만**.
