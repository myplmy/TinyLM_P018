# 실험 계획 목록

각 실험의 목적·아키텍처·실행 명령을 담은 계획서 색인. 실제 측정 결과는 `test_result/` 참조.
새 계획을 세우면 이 표에 한 줄 추가하고, 결과 파일에는 어떤 계획(P번호)을 따랐는지 명시한다.
완료된 계획은 제목 줄 끝에 `[-done(결과번호)]` 표기.

> **명령줄 표준(중요):** `--tokens` 는 데이터 캐시 크기일 뿐 학습 길이가 아니다.
> **실제 학습토큰 = steps × micro_bs × accum × seq.** 300M 기준선과 맞추려면 **`--micro-bs 8 --accum 16`**
> (유효배치 131K). 이를 빠뜨리면(기본 accum8) 150M만 학습된다(005 혼입 사례).
> **일괄 검토 필요:** P004~P013 계획은 v6 플래그 고도화 이전 작성분이라 명령줄이 낡았을 수 있음
> — 실행 전 `--accum 16` 및 현행 플래그명(`--mlp-group/--kd/--init-from/--kd-every/--kd-teacher-tag`) 대조 후 사용.

| 계획 | 요약 | 대응 결과 | 파일 |
|---|---|---|---|
| P001 | 파이프라인 동작확인 + 자동 LR 탐색 | — (결과문서 없음. `lr 1e-3` 확정만) | [P001](P001_파이프라인-동작확인-및-LR탐색.md) |
| **P001B** | **LR 재탐색**(현행 유효배치 accum16 기준) | **017 ✅완료** — **`lr 1e-3` 확정**(변경 없음). **P033 차단 해제**. 배치 의존성 실측(2배 배치 → LR 1.64~1.67배) → **P033 은 자체 lrfind 필수** | [P001](P001_파이프라인-동작확인-및-LR탐색.md) §P001B |
| P002 | dense/tied 300M 기준선 + 격차 축소 매트릭스 | 001 | [P002](P002_300M-기준선-및-격차축소-매트릭스-done.md) |
| P003 | KD + g 스윕(더 공격적 타잉으로 메모리 추가 감축) | 002 | [P003](P003_KD-g스윕-더-공격적-타잉-done.md) |
| P004 | Kimi Delta Attention 적용(롱컨텍스트 KV 절감) | — | [P004](P004_KDA-적용-롱컨텍스트.md) |
| P005 | muP 전이 + Muon 옵티마이저(스케일링 대비) | — | [P005](P005_muP전이-Muon옵티마이저.md) |
| P006 | MTP(학습 aux) + FastMTP(추론 가속) [별개] | — | [P006](P006_MTP-학습aux-및-FastMTP추론.md) |
| P007 | 토큰 스윕(100/300/600M) 격차·품질 곡선 | **006** (+P007B 추가실험 필요 → `-done` 보류) | [P007](P007_토큰스윕-격차품질곡선.md) |
| P007B | 풀 포화점(1200M) + 600M 예산 공정풀 k4 확인 | — **배치 준비됨**(`run100m_P007B.bat`) | 결과 006 §4 후속 |
| P008 | (C1) 점진적 타잉 — LoRA 스케일 어닐 | — | [P008](P008_점진적타잉-LoRA어닐.md) |
| P009 | (C2) 민감도 기반 혼합정밀 삼진 | — | [P009](P009_민감도-혼합정밀-삼진.md) |
| P010 | (C3) 하이퍼넷 층별 적응 | — | [P010](P010_하이퍼넷-층별적응.md) |
| P011 | (S1) 저토큰 교사 + KD 손익분기 | — | [P011](P011_저토큰교사-KD-손익분기.md) |
| P012 | (A) 한국어 데이터 효율(curated 교체) | **009**(판정불가→P028) | [P012](P012_한국어-데이터효율-done.md) |
| P013 | (S2) seq-length warmup 소규모 | — **설계조건 확보**(M=8192 유지, 이득상한 -5.7%) | [P013](P013_seq-length-warmup-소규모.md) |
| P014 | 커스텀 삼진 커널 검증(정확성→속도) | 003 | [P014](P014_커스텀삼진커널-검증-done.md) |
| P015 | 오프라인 KD 캐싱(교사 forward 제거) | 004 | [P015](P015_오프라인KD캐싱-done.md) |
| P016 | 3:4 희소 삼진(Sherry 1.25bpw) 메모리 레버 | **008** | [P016](P016_3대4희소삼진-1.25bit-done.md) |
| P017 | Skip-Forward/Dynamic KD(교사 forward 1/K) | 005 | [P017](P017_SkipForward-Dynamic-KD-done.md) |
| P018 | 압축 교사 distill(tied 교사로 KD 가속) | — | [P018](P018_압축교사-distill.md) |
| P019 | hidden(E=256) 정확 오프라인 KD(P015 대안) | — | [P019](P019_hidden정확-오프라인KD.md) |
| P020 | a4.8 경량: ReLU²GLU + 3-bit KV(추론측 이득) | — | [P020](P020_a4.8경량-ReLU2GLU-3bitKV.md) |
| P021 | GPU 최적화 학습속도 벤치(기존기능 조합, no-ckpt/mb/dyn4+압축교사) | **007**(+추가검증 B 완료) **[-done]** | [P021](P021_GPU최적화-학습속도벤치-done.md) |
| P021B | micro_bs 재스윕(seq512, 스필벽 아래) — 007의 교락 제거 | **007 §추가검증 B**(M 규칙 확립) | [P021](P021_GPU최적화-학습속도벤치-done.md) §추가검증 B |
| P022 | FP8 텐서코어 학습가속(Ada) [게이트] | **010**(0단계 통과) | [P022](P022_FP8-Ada-학습가속.md) |
| P023 | Sophia 옵티마이저 검증(스텝수↓·우리 스케일 정합) | — | [P023](P023_Sophia-옵티마이저-검증.md) |
| P024 | 점진적 스태킹(model growth)로 dense 교사 학습가속 | — | [P024](P024_점진적스태킹-교사학습가속.md) |
| P025 | 2:4 준정형 희소 GPU 학습가속(희소 텐서코어) [도전적·게이트] | — | [P025](P025_2대4희소-GPU학습가속.md) |
| P026 | cooldown-QAT 융합 스케줄 점검(저비용·즉효) | **015 (§1~§7) ✅종결** — 정렬은 **두 예산 모두 검출 실패**(1.07σ / **0.05σ**) → **불필요 확정**. 실제 이득 **`--sched wsd` −0.0755(6.3σ)** → **표준조건 개정**. "15% 절감 공짜"는 cosine 기준선일 때만(wsd 대비 +0.081 비용). **추가 실험 없음** — 후속 질문은 P035 | [P026](P026_cooldownQAT융합-스케줄점검.md) |
| P027 | CUDA Graphs + torch.compile(런치 오버헤드 제거) | — | [P027](P027_CUDAGraphs-compile-런치오버헤드.md) |
| P028 | 데이터효율 재측정: 공통 원문 bpb + 캐시 진단(P012 후속) | **011**(단계0+0.5 완료 — 미관측 2.198 > 분포이동 1.040, val 23.5% 단일문서) / **단계0.6 ✅완료(결과 011 §0.6) — 수정 범위 = `ko-edu-en` 만 확정**(ko-en val 은 건강, 전 데이터셋 재생성 회피). 단계0.5 문서통계는 eos 추정 버그로 **철회** / 단계1(공통 bpb) 대기 | [P028](P028_데이터효율-공통텍스트bpb-재측정.md) |
| P029 | 정성 프로브: 100M급이 학습 텍스트를 유추하는지 눈으로 확인(소규모) | **013**(1차 완료) | [P029](P029_정성프로브-100M급-능력확인.md) |
| P030 | 추론 속도: KV 캐시 + **CPU 배포 실측**(프로젝트 타깃 첫 측정) | **014**(단계2 — 계측결함으로 **무효**) / 단계1 ✅구현완료 / **재실행 [0/4] 게이트 실패 = 측정 0건**(결과 014 §8) / **단계1.5 ✅완료(결과 014 §9) — KV 캐시 정확성 증명**(fp32 로짓 1.1~1.4e-05, argmax 100%). 게이트 실패는 오경보 / **단계2B 대기**(`run_P030_stage2B_infer.bat` — 구 `run_P030_infer.bat` 는 **게이트 미수정으로 폐기**) | [P030](P030_추론속도-KV캐시-CPU배포.md) |
| P031 **[종결]** | 추론 시 중간블록 반복 확장/축소(깊이 외삽) — 메모리 0 레버 탐색 | **단계0 ✅구현 완료(2026-07-31)** — `TMTConfig.infer_repeat/repeat_where/repeat_kv_reuse` + `visit_schedule()` + eval/generate 플래그. **R=1.0 은 `range(n_layers)` 와 동일**(비트 동일성 보존), 학습에서 켜면 예외. 스케줄 계약 검증 완료(R 6종 × where 3종이 계획서 표와 일치). **✅실행 완료 = 결과 020. 종결** — 확장·축소 둘 다 배포 레버 아님. 얻은 것은 **타잉이 깊이 축소에 관대**(R=0.5 에서 dense 가 mC 보다 +0.83 나쁨)라는 **메커니즘 관측 1건**. ⚠️ where=front 만, kv-reuse 미측정 | [P031](P031_추론시-층반복확장-깊이외삽.md) |
| P032 | mA vs dense 정밀비교 — paired eval·결정적 full-val·반복률 정량화 | **✅완료 = 결과 021(R6)** — SE 0.0005 로 σ 의 1/20, **서열 mC ^< mA ^< p6d 확정**(체크포인트 수준). `scripts/paired_eval.py` + `run_P032_paired_eval.bat`. val_loss 평균비교로는 확정 불가(98런 필요) 판정은 유지 — 대신 **eval 샘플링 노이즈를 0 으로** 만들고 크롭 단위로 짝지어 뺀다. ⚠️**확정 범위는 '두 체크포인트'이지 아키텍처가 아니다** | [P032](P032_정밀비교-mA대dense-노이즈하한돌파.md) |
| P033 | **Chinchilla 최적 스케일업 윤곽** — 27.7h 예산·데이터 공급 병목·LR 선결 | — **윤곽만**(상세는 선결 실험 후) | [P033](P033_친칠라최적-스케일업-윤곽.md) |
| **P034** | **실행시 메모리 실측** + 패킹 추론경로 — 저장 11.7MB ≠ 실행시 메모리 | 단계0 ✅완료 / **단계1 ✅완료 = 결과 016**(순위 역전 확인, 저장 대비 상주 31~45배) / **단계1.5 ✅완료(결과 016 §7)** — 3:4 는 '희소'가 아니고(비영 75% vs TWN 54~57%) **bpw 회계 불일치 확인**(동일회계 mA 14.6 vs mC 14.7MB, 감축비 2.66×→2.11×), **논문 해석 오류 3건 정정** / **단계2 ✅완료 = 결과 016 §9(로드맵 R2)** — 게이트 `0.000e+00`, **상주 1.86~1.93× 감축**, 순위 불변, **속도 무변(−0.3~+4.9%)**. 단일축 실험이라 '메모리→속도 전이 없음' 을 확증 → **남은 축은 R8(5비트 패킹)뿐** / 단계3·4 미착수(R8=단계4, 설계만) | [P034](P034_실행시메모리-실측및패킹추론경로.md) |
| **P037** | **데이터 파이프라인 무결성 검증** — P028 의 `0.x` 계열 분리·승계 | — **✅승인(2026-07-31)**. **단계1 ✅완료 = 결과 018** — 경계는 정상(과분할 없음), **원인은 길이가 아니라 SEO 스팸 문서** → 단계2 설계가 **길이 상한에서 내용 필터로 변경**됨 / **단계2 실행됨 = 결과 023 — 재생성 성공(스팸 1,959개/388M자 제거), 검증 무효(옛 캐시를 읽음)**. `--doc-filter` 전달 수정 → **재검증 `run_P037_stage2b_verify.bat`**. **단계3·4 재정의**(로더 결함 L1 문서단위혼합·L2 셔플없음·L3 단일필드 — `docs/methods/07_corpus_selection.md` §4.3). 구현: `--doc-filter`/`--doc-min-chars`, 서명은 결과 018 실측값, 출력은 `{data}_{N}_filtered` 로 분리(기존 캐시 무변). 배치 **`run_P037_stage2_regen.bat`** 실행 가능 / 단계3 `ko-en` 누설 정책 / 단계4 `prepare()` 회귀 게이트 | [P037](P037_데이터파이프라인-무결성검증.md) |
| **P036** | **Arenas(annealing residual synapse)** — 논문이 필수라 한 부품이 우리에겐 없다 | — **✅승인(2026-07-31)**. 결과 016 §7.5·§8.6 에서 파생. `Y=XTα+λ_t·XW` 로 **backward 에 latent W 주입**(우리 어닐은 `.detach()` 라 경로 없음). **단계1B ✅완료 = 결과 019 §8(R4)** — dense 대조군 복구. shape 일치 (768,768) ER/차원 **p6d 50.31 ^> mC 45.20 ^> mA 33.20** → 타잉 설명 배제, **3:4 귀속 강화**. **단계2 근거 성립** / **✅종결 = 결과 019 §9(R5)** — Arenas **무효**(+0.0005=0.04σ, trapping 지표도 무변) → **3:4 비용은 구조 제약 확정**. 구현은 유지(기본 off). 단계0 — `--arenas`/`--arena-lambda`/`--arena-end`, λ_t 는 학습 끝에 0 이라 **추론 오버헤드 0**, off 면 비트 동일. 단계2 배치 `run_P036_stage2_arenas.bat` 실행 가능하나 **단계1B 증거가 선행**한다(4.5h) | [P036](P036_Arenas-잔차시냅스-weight-trapping.md) |
| **P035** | **삼진 어닐의 형태**(선형 램프 vs 계단) — cooldown-QAT 기전이 우리 코드에 존재하는가 | **✅완료 = 결과 022. 종결** — 2×2 네 값이 **1.07σ 안**이고 형태 효과의 **부호가 전이점에 따라 뒤집힌다** → 검출 불가. P026 과 합쳐 **cooldown-QAT 계열 종결**. 부수: 계단 어닐이 `grad_max` 2.6배 | [P035](P035_어닐형태-QAT전이스케줄.md) |
| **P038** | **wsd 표준 스케줄에서 후보 A·C 재확인**(로드맵 R7) | **✅완료 = 결과 024** — 서열 불변(mA−mC: cosine +0.0150 → wsd +0.0136), 상호작용 없음. **mC 기본 확정 권고**. 배치 `run_P038_wsd_recheck.bat`(4.3h). 상대 비교는 같은 스케줄이면 성립하므로 **급하지 않다** — 앞으로의 모든 비교에서 각주를 없애는 것이 목적. 말미에 `paired_eval.py` 로 자동 짝비교 | [P038](P038_wsd표준스케줄-후보재확인.md) |
| **P034+** | **단계3(int8 저장)** — 왜 이론값까지 안 줄어드는가에 대한 정량 답 | **✅완료 = 결과 016 §10**(예측 96.2MB 적중, 게이트 0, 속도 −67%). **다음 레버는 임베딩**. 구현(2026-08-02) `to_int8()` + `--int8-store`. 배치 **`run_P034_stage3_int8.bat`**(수분, GPU 0). 예측 mA 278.2→**96.2MB**(5.44×, 이론값의 7.8배). ★**타잉분은 이미 실현돼 있고**(2.26×) 미실현은 **비트폭 하나**(fp32 32비트 vs 이론 1.375비트). 이론값 도달 조건 = ①삼진 패킹 ②**임베딩 패킹** ③커널이 직접 읽기 — 셋 다 필요 | [P034](P034_실행시메모리-실측및패킹추론경로.md) |
| **P030+** | **단계4** — 디코드 속도가 파라미터를 따라가나 층 수를 따라가나 | **✅완료 = 결과 014 §11.** 시간/토큰 = a + b×층통과(선형). §10.4 가설 **철회**(FLOPs 가 같으니 이상현상이 아니었다). mC 가 층당 21.5% 싸다(캐시 지역성). int8 은 층당 +2.1ms → **R8 은 융합 커널이어야 하고 융합해도 메모리만 번다**. 배치 `--infer-repeat` 로 **층 수만** 12~36 으로 바꾸고 파라미터·메모리 고정. **R8 의 값어치가 여기서 갈린다** | [P030](P030_추론속도-KV캐시-CPU배포.md) |
| **P022+** | **단계1 FP8 end-to-end** | **★차단 해제(2026-08-06)** — σ 실측·REVIEW1 두 선결 모두 충족. 배치 `run_P022_stage1_fp8.bat`(가드: `--fp8` 미구현이면 정지). ⚠️**기대 −15% 내외**(GEMM 은 스텝의 50%뿐, 결과 010) | [P022](P022_FP8-Ada-학습가속.md) |
| **P039** | **어휘 활용도·미학습 토큰 진단**(Fishing for Magikarp, arXiv:2405.05417) | — **설계 확정**(`docs/methods/08_paper_review_202608.md` §4). 비용 0. 32,768 어휘 / 300M 토큰 / 토크나이저 표본 20만 문서 + **그 표본에 SEO 스팸이 있었다**(결과 018) → 제품코드가 어휘를 차지했을 수 있고 **필터가 미학습 토큰을 새로 만들었을 수 있다.** 구현 후 배치 | (신규) |
| **P028+** | **단계1 공통 원문 bpb**(SQuAD context) | **✅구현·배치 완료(2026-08-06)** `scripts/common_bpb.py` + `run_P028_stage1_common_bpb.bat`(수분, GPU 0). 결과 023 §8 로 **차단 해제**. bpb 는 토크나이저 무관이라 **데이터셋 간 비교가 처음으로 성립**한다. ⚠️**영문 전용** | [P028](P028_데이터효율-공통텍스트bpb-재측정.md) |
| **P037++** | **단계3 L1 — 실제 한·영 토큰 비율** | **✅계측 구현·배치 완료** `run_P037_stage3_mixratio.bat`(캐시 빌드, GPU 0). `prepare()` 가 소스별 토큰·문서 수를 찍고 `meta` 에 남긴다. **"한국어 50%" 를 처음으로 실측**한다. L2(셔플)는 캐시 내용을 바꾸므로 별도 결정 | [P037](P037_데이터파이프라인-무결성검증.md) |
| **P034++** | **단계3B 재측정**(동시 실행 오염 정정) | **✅배치 완료(선택)** `run_P034_stage3b_remeasure.bat`. 핵심 수치는 P030 단계4 가 이미 단독 조건에서 다시 쟀으므로 **필수 아님** — cuda·스레드 조건을 한 로그에 모으는 용도 | [P034](P034_실행시메모리-실측및패킹추론경로.md) |
| **P007B** | **풀 포화점(1200M)** | 🚫 **실행 완료·판정 불가 = 결과 006 §5.** 풀을 바꾸면 **val 셋이 바뀐다**(말미 분할 × 소스 고갈). val 한글 비율 600M 5.5% → 1200M **0.0%**. **재실행 보류** — 선결 = ①공통 val 고정(P037 단계5) ②L4 고갈 감지(P037 단계4). 즉시 가능한 대안 = **공통 원문 bpb 로 재판정** `run_P007B_stage2_commonbpb.bat`(GPU 0, 수분) | [P007](P007_토큰스윕-격차품질곡선.md) §실행 이력 |
| **P028++** | **단계1 공통 원문 bpb** | ✅ **완료 = 결과 011 §1.** 교차 데이터셋 비교 **처음 성립**(+0.1685). ⚠️**영어 예산 1.49배 교락으로 귀속 불가**. ★부수 소득이 더 크다 — **모델 서열 mC<mA<p6d 가 코퍼스 밖 텍스트에서 재현**. **단계2 신설: 한국어 공통 원문**(KorQuAD/KLUE context, `common_bpb.py` 재사용, GPU 0). 한·영 두 벤치를 함께 보면 예산 효과와 품질 효과가 부분 분리된다 | [P028](P028_데이터효율-공통텍스트bpb-재측정.md) |
| **P037+++** | **단계3 L1 토큰 비율** | ✅ **완료 = 결과 023 §9.** `ko-en` ±0.6%p(무시 가능) / `ko-edu-en` **16.9%p**(정정 필요). ★★**L4(소스 고갈)·L5(말미 val 분할) 신규 발견** → **단계4(고갈 감지, 최우선·비파괴)·단계5(공통 val 고정, L2 와 함께)** 신설. 배치 `run_P037_stage4_exhaust.bat` | [P037](P037_데이터파이프라인-무결성검증.md) |
| **P034+++** | **단계3B 단독 재측정** | ✅ **완료 = 결과 016 §12.** 메모리 완전 재현, 속도는 2~9% 낮음(정본은 014 §11 유지). ★**CUDA int8 감속 −12~15%** = CPU 의 1/5. ★**GPU 가 CPU 1스레드 대비 1.18배뿐.** **단계3C(언팩 캐시)·단계5(임베딩 패킹) 신설** | [P034](P034_실행시메모리-실측및패킹추론경로.md) |
| **P034++++** | **단계3C — 언팩을 유니크 MLP 단위로** | ⏸ **신설·구현 선결.** 근거 = `docs/20260806_레이어타이잉-메모리와속도-원인분석.md` §5. 현재 `_wq_from_i8()` 이 **forward 호출마다** 불려 g8 에서 **같은 언팩을 16번** 한다. 층 스케줄이 연속이므로 **한 번 언팩해 g 층 재사용** 가능(버퍼는 항상 1개 = 메모리 무손상). **예측 mC 14.23→≈21.5 tok/s(1.51×), `p6d` 불변(대조군)**. 게이트 = 로짓 `0.000e+00`. **★단계4(R8)보다 먼저 한다** | [P034](P034_실행시메모리-실측및패킹추론경로.md) |
| **P040** | **타잉의 학습속도 이득 A/B** | ⏸ **신설.** `EXPERIMENT_BASELINES` §2.1 은 dense 97.5 / g4 94.9 / g8 89.9분(−7.8%)인데 **같은 g4 두 런이 3.5분 벌어진다**(노이즈 ±3.7%) 게다가 구 로그라 grad-ckpt·EMA 조건이 없다. 이론상 타잉이 줄이는 것은 **AdamW 스텝(전체의 0.13%)뿐**이므로 −7.8% 가 사실이면 원인은 **캐시**다. **동일 조건 dense vs g8, 250스텝, 단독·직전 유휴.** 약 40분 | (신규) |
| **P007 [종결]** | 토큰 스윕 | ✅ **종결 = 결과 006 §6.5.** 같은 풀 안의 결론은 유효. **적정 데이터 스케일은 자원 제약으로 미측정**(한국어 코퍼스 약 300M 고갈 → 풀·언어비율·val 이 한 노브). ⚠️ **`-done` 접미사는 P037 단계5 이후에 붙인다** — §2-(1) 의 최종 처리(철회/확정)가 공통 val 재평가에 달려 있다 | [P007](P007_토큰스윕-격차품질곡선.md) |
| **P041** | **한국어 코퍼스 확장 → 데이터 스케일·수확체감** | ⏸ **신설(2026-08-06)·착수 조건 4개 전부 미충족** → 배치 없음. C1 P037 단계5(공통 val) / C2 P028 단계2(한국어 공통 원문) / C3 L1 토큰 믹서 / C4 **한국어 1.2B+ 토큰 확보**. 단계0(코퍼스 실사)의 게이트가 C4 다. 예측 P1 = 친칠라 비로 132M×20 = **2.6B 토큰**이 최적인데 우리는 그 **11.5%** | [P041](P041_한국어코퍼스확장-데이터스케일-수확체감.md) |
| **P034+++++** | **단계3C 언팩 캐시(1차 실패·2차 대기)** | 🚫**1차 무효 = 결과 016 §13**(대조군 `p6d` −40%). 수정 완료 → **`run_P034_stage3c2_unpackcache.bat`**. ★**단계5 재정의**: 문제는 임베딩이 아니라 **헤드 타잉**이다(상주 33.0MB 중 **32.0MB 가 `emb.weight`**, 헤드와 tie 돼 int8 화 시 forward 마다 32MB 복원). 어휘 축소는 **P039 예비 스캔이 닫았다**(빈도0 **0.28%**). ★★**남은 상주 레버 셋(R8·임베딩·헤드)이 전부 P014 하나로 수렴** | [P034](P034_실행시메모리-실측및패킹추론경로.md) |
| **P022 [보류]** | FP8 end-to-end | ⏸ **2026-08-06 판단: 지금 구현하지 않는다.** 기대 −15%(표준 런 **18분**)인데 메모리 목표와 무관하고, **A1(KD 교사 `freeze_quant`+`drop_latent`) + A2(`--no-ckpt` 개방)** 가 같은 자릿수를 구현 거의 없이 준다. **트리거 = 20 GPU시간+ 캠페인 확정(P041 단계2·P033)**. 배치는 삭제됨 | [P022](P022_FP8-Ada-학습가속.md) |
| **P039** | **어휘 활용도·미학습 토큰 진단** | ✅**구현 완료** `scripts/diag_vocab.py` + `run_P039_vocab_diag.bat`(GPU 0, 수분). 예비 스캔: `ko-en` 빈도0 **93/32,768 = 0.28%** → **어휘 축소는 메모리 레버가 아니다**. 정식 3스캔(ko-en / ko-edu-en / **filtered**)으로 확정. [3/3]이 "필터가 미학습 토큰을 만들었나" 를 본다 | (신규) |
| **P037++++** | **단계4 L4 고갈 감지 검증** | ✅**구현 완료** `_stream(exhausted_cb)` + 캐시 축소 경고 + `meta["mix_exhausted"]` → **`run_P037_stage4_exhaust.bat`**(`--data ko --tokens 400M` 로 강제 고갈, GPU 0, 20~30분). ★**부산물이 더 중요하다** — 경고가 찍는 숫자가 **우리 한국어 코퍼스의 실제 크기**이고 그건 **P041 의 핵심 입력**인데 한 번도 직접 측정된 적이 없다 | [P037](P037_데이터파이프라인-무결성검증.md) |
| **P014 [재개]** | **커스텀 삼진 커널** | ★**`-done` 철회 대상.** 결과 014 §11·016 §13 + `08_paper_review` §5 로 **남은 로드맵의 유일한 병목**이 됐다(R8·임베딩·헤드가 전부 이것 하나). ★**T-MAC(arXiv:2407.00088)·bitnet.cpp(arXiv:2502.11880)가 참조 구현으로 존재** — LUT 기반 mpGEMM, **역양자화 없음**, x86 **2.37~6.17×**·에너지 −71.9~82.2%. 우리는 정확히 반대(매 forward fp32 복원)를 하고 있다. **착수 전 실사 필요**(포맷·스케일 단위·라이선스·본문 확인) | [P014](P014_커스텀삼진커널-검증-done.md) |
| **P008 [재부상]** | 점진적 타잉 — 층별 LoRA/FiLM | 🧪 **재개 근거 성립**(`08_paper_review` §7, RRT arXiv:2410.20672). **타잉 격차 +0.15~0.17 이 토큰 6배에도 안 닫혔다** = 구조로 풀어야 한다. 구현은 **이미 있다**(`--lora-rank`·`--mlp-film`)는데 **현 후보 mA·mC 에서 둘 다 꺼져 있다.** 구 측정은 둘 다 도움(`t_lora32` 3.9220 / `t_film` 3.9420 vs `t_base` 3.9558). ★**FiLM 이 0.13MB 로 거의 공짜** — 단 분해능 이하라 `paired_eval.py` 필수 | [P008](P008_점진적타잉-LoRA어닐.md) |
| **P042** | **KD 교사 추론화 + `--no-ckpt` 개방** | 💡 **신설 제안.** 교사는 가중치가 고정인데 매 스텝 `refresh_quant()` 를 돌고 fp32 latent 도 든다. **추론용으로 이미 구현된 `freeze_quant()`/`drop_latent()`** 를 학습 루프의 교사에 붙인다 → 교사 몫 절감 + **VRAM 확보로 tied+KD 에 `--no-ckpt`(−17.3%) 개방**. **FP8(−15%)보다 값싸고 먼저다** | (신규) |
| **P028+++** | **단계3 필터 재학습** | ✅ **완료 = 결과 011 §2.** **−0.296 nats(24.7σ)** — 필터가 모델을 실제로 좋게 만든 **첫 측정**. `ko-edu-en` 격차의 **63%가 스팸**이었다 → **결과 009 부분 철회**. **후속 [필수] `ko-en` 스팸률 측정**(같은 서명으로 훑기만, GPU 0) — 0 이 아니면 표준 코퍼스에도 필터가 필요하고 그건 전 기준선 재학습이라 **먼저 측정만** 한다 | [P028](P028_데이터효율-공통텍스트bpb-재측정.md) |
| **P034++++++** | **단계3C 2차·3차** | 2차 ✅ = 결과 016 §14(**mC +17.1% / mA +11.3%**, 대조군은 어텐션 캐시 결함으로 −23%). 3차 수정 완료(`_armed_tlinears` 단일 목록) → **`run_P034_stage3c3_unpackcache.bat`**. **예측 mC 1.74× / mA 1.59× / p6d 불변** | [P034](P034_실행시메모리-실측및패킹추론경로.md) |
| **P039 [완료]** | **어휘 활용도·미학습 토큰 진단** | ✅ **완료 = 결과 025.** 빈도0 **0.16%** → **어휘 축소 폐기**. ★부수: **타잉 헤드에서 노름 휴리스틱 부호 역전**. ⚠️**계획서를 뒤늦게 작성**(2026-08-07) — 배치를 만들면서 계획서를 안 썼다(P040 도 동일) | [P039](P039_어휘활용도-미학습토큰-진단-done.md) |
| **P040** | **타잉의 학습속도 이득 A/B** | ⏸ **배치 준비 완료·미실행.** `run_P040_tying_trainspeed.bat`(약 30~40분). 기준표 §2.1 은 g8 −7.8% 인데 **같은 g4 두 런이 3.7% 벌어지고 구 로그라 grad-ckpt 조건 미상**. 이론상 타잉이 줄이는 건 **AdamW 스텝(0.13%)뿐** → 5~10% 면 원인은 **캐시**. ⚠️**계획서를 뒤늦게 작성**(2026-08-07) | [P040](P040_타잉의-학습속도-이득-AB.md) |
| **P042** | **KD 교사 추론화 + `--no-ckpt` 개방** | ⏸ **계획서 신규 작성(2026-08-07).** 교사에 `freeze_quant()`+`drop_latent()` → 교사 latent **472.5MB** 회수 → **`--no-ckpt`(−17.3%) 개방 시도**. ⚠️**P3 가 불확실**: 15.7GB − 0.47GB = 15.2GB 로 **스필벽 13~14GB 에 여전히 못 미친다** → 단계2 가 그걸 값싸게 확인하고, 안 되면 §7 대안(교사 int8, GPU 언팩은 −12~15%로 싸다) | [P042](P042_KD교사-추론화-및-nockpt개방.md) |
| **P014B** | **LUT 커널 실사·속도 게이트** | ⏸ **신설.** T-MAC(arXiv:2407.00088)·bitnet.cpp(arXiv:2502.11880) 실사. 단계0 문헌·라이선스 → 단계1 포맷 변환 → 단계2 속도. **게이트 G2 = 우리 int8 대비 1.3× 미만이면 P014 를 접는다**(언팩 캐시가 이미 1.7× 추정이라 외부 의존을 질 이유가 없다). ★미지 3건: **U1 dim 768 에서 LUT 가 이득인가**(논문은 3B급) / **U2 g128 그룹 스케일** / **U3 활성값 int8 요구** | [P014B](P014B_LUT커널-실사-및-속도게이트.md) |
| **P008 [재정의]** | **점진적 타잉 — LoRA 스케일 어닐** | ⏸ **전면 재작성 + 구현 완료(2026-08-07).** `--lora-decay` 신설(s(t) 1→0, 기본 0=끔). ★**목적이 바뀌었다** — 격차는 KD 가 이미 닫았다(결과 021: mC 가 dense 를 이긴다). 새 목적 = **KD 파이프라인 258분 → 95분(2.7×)** 대체 가능성 + 더 공격적 g. ★**사용자 질문 답**: ①고정 LoRA 는 추론에 남아 메모리 손해(2.07→1.94×) **권장 안 함**, ②FiLM 은 0.13MB 라 값싸게 확인, ③**어닐 LoRA 는 s=0 이라 메모리 대가 0** = 진짜 실험. 구 데이터가 **KD 켜면 LoRA+FiLM 이득 −0.0065(분해능 미만)** 를 시사. 배치 `run_P008_stage0_gate.bat`(수분) → `run_P008_stage1_lora_anneal.bat`(약 6h) | [P008](P008_점진적타잉-LoRA어닐.md) |
| **P008 [종결]** | 점진적 타잉 — LoRA 스케일 어닐 | ✅ **종결 2026-08-07.** 원 목적("격차 축소")은 **KD 가 달성**했다(결과 021: mC 3.7393 < p6d 3.7608). "배포 메모리 대가 0" 은 **결과 026 이 구현 상태로 거짓임을 확인**. ⚠️ 이 계획은 **한 번도 실행되지 않았다** — "어닐 LoRA 무효" 라는 뜻이 아니다. 남은 질문은 **목적이 달라서** P043·P044 로 분리. `--lora-decay` 구현은 P043 이 승계 | [P008](P008_점진적타잉-LoRA어닐.md) |
| **P043** | **어닐 LoRA 로 KD 대체 (목적: 파이프라인 비용)** | ⏸ **신설.** 성공 기준이 "더 좋다" 가 아니라 **"같은데 싸다"**(KD 258.4분 → 약 95분 = 2.7×). ★**선결 C1 = `export_deploy.py`**(결과 026 이 세운 게이트 — LoRA 키를 실제로 제거해야 메모리 주장이 성립). ⚠️예측 P2 = **KD 에는 못 미친다**(구 `t_all`−`t_kdinit` = −0.0065 = 분해능 미만이라 KD 가 이미 그 자리를 채운 정황). **P2 가 맞아도 실패가 아니다** — "KD 대체 불가" 확정도 결과 | [P043](P043_어닐LoRA로-KD대체-파이프라인비용.md) |
| **P044** | **FiLM 층별 조건화 재검증 (목적: 거의 공짜인 품질)** | ⏸ **신설·배치 준비 완료** `run_P044_film_recheck.bat`(**3.5h, 무인 실행 가능**). FiLM 은 **65K = 0.13MB**(감축비 2.07→2.06×) 라 "조금만 이득이어도 채택" 이 성립하는 유일한 조건화 장치. **현 후보 mA·mC 에서 한 번도 안 켜 봤다.** 구 측정 −0.0138(plain)/−0.0065(KD+LoRA 합) 은 **둘 다 분해능 미만** → 지금은 `paired_eval`(SE 0.0005)가 볼 수 있다. **대조군 `mC_wsd` 재사용이라 런 하나로 끝난다** | [P044](P044_FiLM-층별조건화-현후보-재검증.md) |
| **P014B [단계0 착수]** | **LUT 커널 실사** | ⏸ **단계0 착수(2026-08-07)** — **사용자/AI 역할 분담표 작성 완료**(§0.A). 사용자 = 논문 PDF 2건 `article/` 저장 · 저장소 클론 · **필요 파일만** `article/lut_ref/` 복사 · (선택)빌드 시도. AI = 받으면 **U2 스케일 단위·U3 활성값 정밀도·포맷·라이선스·커널 API 판정**. ★**§0.C 병행 실행표** — 단계0 은 GPU 를 안 쓰므로 그동안 `run_P044_film_recheck.bat`(3.5h) 를 돌린다 | [P014B](P014B_LUT커널-실사-및-속도게이트.md) |
| **P037+++++** | **단계4 L4 고갈 검증(1차 실패)** | ⚠️ **1차 실행 실패 — 이 실험의 잘못이 아니다.** `TypeError: prepare() got an unexpected keyword argument 'lora_decay'` — `--lora-decay` 편집이 **문자열 비유일성** 때문에 `train()` 대신 `prepare()` 호출부에 들어갔고, 이 배치가 그 뒤 처음 돌아서 대신 죽었다. **수정 완료 + `scripts/check_call_kwargs.py` 신설**(AST 로 cli 호출 키워드 대조, 회귀 검증 통과) + **배치에 가드 추가**. 재실행 가능 | [P037](P037_데이터파이프라인-무결성검증.md) |
| **P014B [단계0 완료]** | **LUT 커널 실사** | ✅ **단계0 완료 = `docs/20260807_...분석.md` §13.** 사용자 U1~U4 로 논문 2건·코드 확보 → **코드로 확정**. **U2 🚫부정(스케일 텐서당 1개, 우리 428K개, 우회 불가 → 로짓 동등성 원리적 불가)** / **U3 ✅TL 은 F32 + 배치1 전용(우리와 일치)** / **U1 ⚠️갈림**(TL 은 3200/8640 하드코딩·codegen 필요 / **I2_S 는 shape 무관**) / 라이선스 ✅MIT. ★**단계0.5 신설** — `generate-dummy-bitnet-model.py --model-size 125M` 로 **변환기 없이 U1 선답**(이득 없으면 거기서 종료). 단계1 게이트를 "동등성"→"공통 원문 bpb 허용 오차" 로 개정. ⚠️**U5 빌드 실패**(§14) — 원인은 환경 아님 | [P014B](P014B_LUT커널-실사-및-속도게이트.md) |
| **P044 [종결]** | FiLM 층별 조건화 재검증 | 🚫 **기각·종결 = 결과 027.** paired **+0.0003**(SE 0.0004, t=0.65) = 거의 정확히 0. 메모리 대가는 **정직하게 계상**됨(0.13MB) → 전제는 맞았고 **살 것이 없었다**. 부수: `grad_max` 0.847→**1.855** = 층별 조건화는 gradient 를 키운다 | [P044](P044_FiLM-층별조건화-현후보-재검증.md) |
| **P037 [단계4 종결]** | L4 고갈 감지 검증 | ✅ **종결 = 결과 023 §10.** 경고 3종 전부 발화 + 무한루프 대신 정상 종료. 예측 4/4 적중. ★★**부산물: 한국어 코퍼스 = 280.6M 토큰(ko 전용) / 300.2M(ko-en)** → **P041 게이트 C4(1.2B+)의 23~25%**. "부족하다" 가 추정에서 **측정**으로 | [P037](P037_데이터파이프라인-무결성검증.md) |
| **P014B [중단 권고]** | LUT 커널 실사 | ⚠️ **U5 빌드 해결 불가 판정 → 중단 권고**(분석보고서 §16). 원인 확정: **`dequantize_row_i2_s` 정의가 `ggml-cpu`, 참조가 `ggml-base`** = 타깃 의존 방향이 반대. 서브모듈은 **BitNet 이 고정한 리비전**이라 재클론 무효. `GGML_SOURCES_BITNET` 은 **죽은 변수**(내 1차 진단 §14.2 철회). 고치려면 **업스트림 빌드 시스템을 우리가 패치**해야 하고 **빌드돼도 U2 부정 때문에 우리 모델을 그대로 못 넣는다** | [P014B](P014B_LUT커널-실사-및-속도게이트.md) |
| **P014C** | **우리 구조에 맞는 역양자화 제거 경로** | ✅ **단계0 구현·배치 완료** `run_P014C_stage0_alpha_group.bat`(GPU 수분, 학습 0). ★**질문을 하나로 좁혔다** — 커널 성능이 아니라 **"거친 α 를 감당하는가"**. `scripts/diag_alpha_group.py` 가 `_wq` 의 **부호를 보존**하고 α 만 `{g128,g256,g512,per-row,per-tensor}` 로 재추정(L2 최적 `A=mean(|w|)`) 후 **공통 원문 bpb** 를 잰다. 로직 검산 완료(**g128 상대오차 0.000000**). 분기: per-row Δbpb **<0.008 → 커널 불필요** / 0.008~0.05 → 재학습 검토 / **>0.05 → g128 커널 자체 제작**. ★사용자 CPU = **Zen 5(AVX-512 완전지원) + L3 32MB** = 조건 양호 | [P014C](P014C_우리구조에맞는-역양자화제거-경로.md) |
| **P045** | **타잉 상한 g16 — 타잉 축 종결** | ✅ **배치 완료** `run_P045_g16_ceiling.bat`(약 3.5h). `n_middle=16` 이라 **구조적 상한**. ⚙상주 **−5.3%**, packed 2.07→**2.24×**. ⚠️**이득은 작다** — 본 목적은 **축을 닫는 것**. 예측 +0.01~0.03(분해능 경계) → `paired_eval` 필수 | [P045](P045_타잉상한-g16-종결.md) |
| **P046** | **임베딩 랭크 축소 E 256→128** | ✅ **구현·배치 완료** `run_P046_emb_rank128.bat`(약 3.5h). ★**int8 이후 남은 상주의 최대 항**(87.1MB 중 32.0MB 가 `emb.weight`). ⚙**상주 −18.8%**, packed **2.22×** — g16(−5.3%)의 **3.5배**. `--emb-rank` 신설 + `init_from_dense` shape 가드. ⚠️**교락**: 부모초기화가 임베딩에 안 걸린다(shape 불일치 → 건너뛰고 경고) | [P046](P046_임베딩랭크축소-남은상주의최대항.md) |
| **P014C [단계0 완료]** | **역양자화 제거 — α 규약** | ✅ **단계0 완료 = 결과 028.** **G0 통과** — per-row Δbpb 최대 **+0.0063 < 0.008**(여유 21%). per-tensor **+0.0862 = 0.261 nats(21.7σ)** → **P014B 중단 권고에 품질 근거 추가**(빌드돼도 못 넣는다). ★**단계1B 신설** — per-row 는 **저장 −6.2% 레버**이기도 하다(α = fp16 `16/g` bpw, 코드로 확인. G1B 통과). g256 은 **분해능 1/5** 로 공짜지만 **재학습 필요**하니 무료는 아니다. **다음 = 단계1**(`_weight_int8pack_mm` 존재 확인, 1분) | [P014C](P014C_우리구조에맞는-역양자화제거-경로.md) |
| **P047** | **표준 코퍼스 `ko-en` 의 스팸률** | ⏸ **신설·구현·배치 완료** `run_P047_stage0_spam_rate.bat`(**GPU 0**, 스트리밍 20~60분, 무인). ★**남은 레버 1위.** 결과 011 §2 의 **−0.296 nats(24.7σ)** 는 우리 최대 단일 이득이고 **데이터에서 나왔다** — 그런데 **전 기준선이 `ko-en` 이고 `ko-en` 스팸률은 한 번도 측정 안 했다**(결과 018 의 '건강한 대조군' 은 표본 몇 개다). `prepare.spam_signature()` 를 **호출**(함정 14) + `_stream` seed 0 → **실제 캐시 집합과 동일 순서**. **양성 대조군 `ko-edu-en` 을 같은 배치에**(함정 17 — 거기서 검출 안 되면 도구 고장). ★**0 건일 때 95% 상한**을 찍어 '못 찾았다' 를 '최대 이만큼' 으로 바꾼다. ⚠️게이트는 **'토큰 비중' 이 아니라 '존재'** — 결과 023 §8 에서 스팸은 토큰의 소수인데 **val 손실의 53%** 였다. ★**REVIEW2 보다 먼저** — 양성이면 리뷰를 고치는 게 아니라 **기준선을 다시 만드는** 문제다 | [P047](P047_표준코퍼스-ko-en-의-스팸률.md) |
| **P048** | **prelude/coda 2+2 → 1+1** | ⏸ **신설·구현·배치 완료** `run_P048_prelude_coda.bat`(약 3.2h, 무인). ★**남은 packed 레버 1위** — prelude·coda MLP 는 **타잉 대상이 아니라 층마다 독립**이라 **층의 20%(4/20)가 유니크 MLP 의 67%(4/6)를 먹는다.** 유니크 6→4: ⚙삼진 54.85→**42.76M(−22.0%)**, packed 12.93→**10.47MB(−19.0%)**, int8 상주 86.70→**74.81MB(−13.7%)**. 감축비 **2.34×**(자기 dense 규약) / 2.58×(고정 p6d 대비) — **섞어 쓰지 말 것**. 프리셋 `m100R1p` 신설(`m100R1c` 에서 2필드만). ★★**리스크 1위** — 결과 002 는 prelude/coda 를 '단일 최대 이득' 이라 했으나 **그건 0+0 대비**이고 **1+1 은 한 번도 측정 안 한 중간점**이다. ⚠️**깊이 20→18 교락** — 유지하려면 g9 가 되고 그러면 `mid_mlps[17]` 로 **부모 재사용 불가**(+3.5h) → 부모 재사용을 골랐다. ★**채택 임계 +0.02 를 결과 전에 못 박았다**. `init_from_dense` 에 **구조 불일치 경고 신설**(종전 `zip` 이 조용히 잘랐다 — G-init 게이트) | [P048](P048_prelude-coda-1+1-남은최대-packed레버.md) |
| **P045 [종결]** | 타잉 상한 g16 | ✅ **종결 = 결과 029. 채택 가능.** paired **+0.0161 < 분해능 0.024**. packed 2.07→**2.24×**, fp32 상주 **−8.0%**. `n_middle=16` 이라 g16 이 구조적 상한 → **타잉 강도 축 종결**. ★부수: `grad_max` 0.847→**0.738**(타잉 강화가 gradient 를 안정시킨다) | [P045](P045_타잉상한-g16-종결.md) |
| **P046 [판정 불가]** | 임베딩 랭크 E=128 | 🚫 **판정 불가 = 결과 030.** paired +0.1768 인데 **`grad_max` 19.32**(기준선 0.847) → `CLAUDE.md` 판정기준의 *"10 이상이면 학습 문제"*. **step 0 `ce` 10.4051 = 완전 무작위** = 임베딩만 난수로 시작한 탓(변수 2개). ✅회계는 적중(int8 상주 **70.56MB = −18.8%**). **조치: SVD 절단 이식 구현**(Eckart–Young 최적 랭크근사, 검산 완료) + **G-init 게이트 신설**(`SVD 절단 이식` 줄 + step0 ce<8.5 + grad_max<3). ⚠️**재측정 우선순위 낮음** — packed 기준 최악 레버이고 **int8 배포를 확정한 뒤**에 재면 된다 → **배치 미작성** | [P046](P046_임베딩랭크축소-남은상주의최대항.md) |
| **P047 [재실행 대기]** | `ko-en` 스팸률 | ⚠️ **1차 측정 0건 = 결과 031. 실험 잘못이 아니다.** `runlog.py` 에 `--note` 와 명령을 한 줄에 줘서 **명령이 조용히 버려졌다**(종료코드 0). **3중 방어 구현 완료**(runlog 거절 + lint 9c + 9d). **배치 수정 완료, 재실행 가능** `run_P047_stage0_spam_rate.bat`(GPU 0, 20~60분). ★**질문은 그대로 남았고 여전히 레버 1위** — REVIEW2 앞에 답해야 한다 | [P047](P047_표준코퍼스-ko-en-의-스팸률.md) |
| **P048 [단계1 완료]** | prelude/coda 1+1 | ✅ **단계1 완료 = 결과 032.** paired **+0.0493**(예측 대역 안). **packed −19.0% / fp32 상주 −20.4% = 저장소 최대**. ⚙계산 소수점까지 적중. ★★**효율이 g16 과 같다**(0.0199 vs 0.0167 nats/MB, 세 지표 전부 1.19~1.20배) → **"더 나쁜 거래" 가 아니라 "같은 거래의 2.6배 용량"**. 게이트상 보류지만 **A-6 축은 닫지 않는다**. ⚠️깊이 20→18 교락으로 **귀속 불가**(배포 판정에는 충분). **다음 = 단계2 `m100R1q`(g16+p1c1, 유니크 MLP 3)** — ⚙packed −26.3%/상주 −28.4%, **국소선형 예측 Δ +0.062 를 검정**. 배치는 야간큐 편성 시 작성 | [P048](P048_prelude-coda-1+1-남은최대-packed레버.md) |
| **P014C [단계1 완료]** | 역양자화 제거 — α 규약 | ✅ **G1 통과.** `torch._weight_int8pack_mm` 존재·CPU/CUDA·우리 shape 3종 동작(상대오차 1.7e-3 = bf16 자릿수). **커널 불필요.** **다음 = 단계2**(속도 게이트, **선결: `TLinear` per-row 융합 경로 기본 off 플래그**) → **배치 미작성**(구현이 선결) | [P014C](P014C_우리구조에맞는-역양자화제거-경로.md) |
| **P047 [종결]** | `ko-en` 스팸률 | ✅ **종결.** 스팸 문자 **0.06229% vs 대조군 18.75654% = 301배**. 위키 **0건**. 기대 이득 **−0.001 nats = 분해능 1/24** → 단계1·2 안 한다. ★**REVIEW2 착수를 막던 유일한 위험 제거** | [P047](P047_표준코퍼스-ko-en-의-스팸률.md) |
| **P048 [단계2 완료]** | prelude/coda 1+1 (+g16) | ✅ **단계2 완료 = 결과 032 §8.** +0.0745(예측 +0.062, **초과 = 상호작용**). ★**볼록성 발견** — 같은 g16 이 작은 기반 위에서 **1.57배** 비싸다. **평균가격 예측 금지.** 프리셋 `m100R1q` 신설. **단계3(깊이 교락 분리)은 P050 결과를 보고 결정** | [P048](P048_prelude-coda-1+1-남은최대-packed레버.md) |
| **P040 [종결]** | 타잉의 학습속도 이득 | ✅ **종결 = 결과 033.** −5.53%(타잉+CLA2), **타잉 단독 ⚙−3.15%**. 기준표 §2.1 −7.8% **정정**. ★**`report()` FLOPs 가 `cla_group` 을 못 본다**(함정 25). ★**VRAM −35.3%** = P042 전제 확인 | [P040](P040_타잉의-학습속도-이득-AB.md) |
| **P049** | **타잉으로 깊이를 사는가 (g16×2)** | ⏸ **신설.** ★**사용자 직관 판정**: MLP 는 **정확히 맞다**(그룹 수 같으면 MLP 메모리 동일, 28.31M). **그러나 어텐션은 타잉 대상이 아니다** → 깊이 20→36 에서 어텐션 **+80%**, 총 삼진 **+38.7%**, fp32 상주 **+35.9%**. 어텐션 비중 48.4%→**62.8%**. **기존 계획 없음**(`m100d` 는 그룹 수가 4개라 이 질문에 못 답한다). ⚠️**선결 2건** — ①`init_from_dense` 깊이 확장 이식(학생 36층 > 교사 20층이면 현재 **`IndexError`**) ②단계0 초기화 게이트. **채택 임계 −0.075 를 결과 전에 명시.** 배치 미작성 | [P049](P049_타잉으로-깊이를-사는가-g16x2.md) |
| **P050** | **부모 없는 단독학습 vs 부모증류** | ⏸ **신설·배치 완료** `run_P050_parent_ablation.bat`(**약 9.5h**, 3런 독립). ★**부모는 두 문으로 들어온다** — ①`--init-from`(shape 일치 필요, **아키텍처 바꾸면 깨진다**) ②`--kd`(**어휘만 같으면 됨**). **분리한 적이 없다.** 4팔 A/B/C/D 로 분해. ★**이건 과거 실험의 해석을 교정한다** — ①의 몫이 크면 **P046·P048·P048-2 의 델타에 초기화 열위가 섞여 있고 아키텍처를 과소평가**한 것이다. **임계 사전 명시**: ②가 ①의 2배 넘으면 부모초기화 격하 → **P049 선결 구현 불필요**. 보고서 = `docs/20260807_부모모델-의존성-분석...md` | [P050](P050_부모없는-단독학습-vs-부모증류.md) |
| **P042 [단계0 준비]** | KD 교사 추론화 | ✅ **구현 완료 + 배치** `run_P042_stage0_gate.bat`(tiny, 수 분). **`--kd-teacher-infer` 신설**(기본 off = 비트 동일). 교사에 `freeze_quant()`+`drop_latent()` → 매 스텝 `refresh_quant` 생략 + latent **약 472.5MB** 회수. **단계0 은 비트 동일성만 본다** — 손실이 조금이라도 다르면 **트레이드오프가 아니라 구현 버그**. ★**결과 033 이 전제를 확인**(타잉 VRAM −35.3%, 여유 8~9GB) | [P042](P042_KD교사-추론화-및-nockpt개방.md) |
| **P042 [단계0 통과]** | **KD 교사 추론화** | ✅ **G0 통과 = 결과 034.** on/off **전 인쇄 지표 완전 일치**(25초). G1(초기화 정합)도 동시 통과. ⚠️**속도 판정 안 함** — 참고값 −20.6% 는 교락 셋(순서·tiny·30스텝)이라 무가치하되, **계획 P1(−0~5%) 밖**이라 단계1 이 P1 을 기각할 수 있다는 것을 미리 적었다. **단계1 배치 `run_P042_stage1_speed_vram.bat`(60분, ★3런 OFF-ON-OFF 로 drift 를 직접 측정)** / **단계2 배치 `run_P042_stage2_nockpt.bat`(20분, OOM 이 답이다)**. ★단계2 는 "0.47GB 로 벽을 넘나" 와 **"기준표 §5 의 15.7GB 자체가 아직 맞나"**(결과 033 이 tied 5.06GB 를 쟀다)를 동시에 잰다 | [P042](P042_KD교사-추론화-및-nockpt개방.md) |
| **P022B** | **FP8 선택적 혼합정밀도(DeepSeek-V3 식)** | ⏸ **신설·단계0 구현·배치 완료** `run_P022B_stage0_fp8_probe.bat`(**GPU 수 분, 학습 0**) + `scripts/diag_fp8_precision.py`. ★★**핵심 발견: 우리 가중치의 FP8 오차는 원리적으로 0** — 어닐 후 `wq/α ∈ {−1,0,+1}` 이고 **E4M3 가 셋을 정확히 표현**한다. 논문이 128×128 블록으로 애써 얻은 미세 입도를 우리는 **행×128** 로 이미 갖고 있고 `micro_group=128` 이 논문 `N_C=128` 과 **일치**. ⚠️**그런데 그게 곧 벽** — 기술보고서 §3.3.2 원문 *"K축 그룹 스케일은 표준 FP8 GEMM 이 지원하지 않는다"* 라 DeepSeek 은 CUTLASS 커널을 직접 만들었다(DeepGEMM). **P014B 의 U2 부정과 같은 벽**이다. 단계0 이 ①`_scaled_mm` 이 실제로 받는 입도 ②E4M3 왕복 Δbpb 를 입도별로 잰다. **자기검증 = g128 상대오차 0.00000**. 게이트: [D] 최선 Δbpb <0.008 통과 / 0.008~0.02 QAT 필요 / >0.02 **종결**. ★부산물 = **논문 §3.3.3 의 BF16 옵티마이저 상태**(약 254MB, 메모리 목표 직결) → 단계2 **설계만**(커스텀 옵티마이저 = 구현 선결). **2026-08-06 의 "지금 구현 안 함" 판정은 유지** | [P022B](P022B_FP8선택적혼합정밀도-DeepSeekV3식.md) |
| **P051** | **`micro_group` 128→256 — 마지막 무료 축** | ⏸ **신설·구현·배치 완료** `run_P051_micro_group256.bat`(약 3.5h, 무인). ★**저장 bpw 에 항이 둘인데 하나만 건드려 왔다** — `code(1.5850) + scale(16/g = 0.1250)`. g256 이면 **1.7100→1.6475 bpw(−3.65%)**, packed 13.050→**12.573MB**, 감축비 2.07→**2.15×**. **아키텍처 무변**(층·깊이·타잉·임베딩 전부 그대로). ★★**직교 레버** — 다른 셋은 유니크 삼진 **개수**를 줄이는데 이건 **파라미터당 비트**를 줄인다 → 결과 032 §8.3 의 **볼록성 바깥**(용량을 안 덜어낸다)이고 **곱해진다**. ⚠️**packed 전용** — 상주식에 bpw 가 없다(함정 1). 사전 증거: 결과 028 의 사후 g256 Δbpb **+0.0010~0.0015 = 분해능 1/5~1/8**, 그리고 **사후는 상한**이다(재학습이 더 좋다). **768 % 512 ≠ 0 이라 g512 는 구조적 불가 → 런 하나로 축이 닫힌다**(P045 가 `n_middle=16` 으로 닫힌 것과 같은 형태). 게이트 G-a = 로그에 `[micro_group]` 줄 + packed 12.5MB 대. 없으면 **이 런은 무가치** | [P051](P051_micro-group-알파그룹-마지막-무료축.md) |
| **P046 [재측정 대기]** | 임베딩 E=128 | ⏸ **배치 개정 완료** `run_P046_emb_rank128.bat`(3.5h). 결과 030 의 **판정 불가**를 `_svd_emb_init`(Eckart–Young 최적 저랭크 근사) 로 해소한다. 태그 **`mC_e128svd` 신규**(구 `mC_e128` 보존 = 두 초기화 비교용). ★**게이트 G-init 3중**: `SVD 절단 이식` 줄 + step0 ce<8.5 + `grad_max`<3. 하나라도 실패면 **또 판정 불가**. ★부수: 이식이 찍는 **보존 스펙트럼 에너지 %** 는 새 정보 — 실효 임베딩의 몇 %가 상위 128방향에 있는지가 품질 대가의 **하한을 런과 독립으로 예측**한다. ★★**결과 030(+0.1768)과의 차이 = 초기화 핸디캡의 크기**이고 그건 P046 을 넘어선다 — P048·P049 가 같은 핸디캡을 진다. **P050 이 같은 것을 반대편에서 잰다 → 둘을 함께 읽는다**. ⚠️우선순위 **최하**(int8 배포 확정 후에나 값어치) | [P046](P046_임베딩랭크축소-남은상주의최대항.md) |
| **P022B [단계2 구현 완료]** | **BF16 옵티마이저 상태** | ✅ **구현·배치 완료(2026-08-07 사용자 요청)** `run_P022B_stage2_optstate.bat`(약 4.2h = 2a 수분 + 2b 40분 + 2c 3.5h). `tinylm/train/adamw_bf16.py` + **`--opt-dtype {fp32,fp32c,bf16}`**. ★**3모드인 이유 = 구현 위험과 dtype 위험의 분리** — `fp32c`(우리 구현·fp32 상태)가 `fp32`(torch fused)와 인쇄 4자리 일치해야 비로소 `bf16` 차이를 dtype 탓으로 귀속할 수 있다. **수식 검산 완료**(numpy 2289스텝, torch `_single_tensor_adamw` 대비 **최대 절대차 1.1e-16 = 기계 엡실론**). 바꾸는 것은 **상태 저장 dtype 하나** — master weight·gradient·**산술 전부 fp32**. 기대 **약 254MB 회수**(63.5M×2벌×2B). ⚠️**이 저장소에서 유일하게 "조용히 발산할 수 있는" 실험** → 계획서 **§2.3 에 실행 중 감시 항목 표**(＋`[opt]` 줄 존재 / step0 ce 일치 / `skip 0` / val 2회 연속 상승 시 중단 / VRAM −0.25GB). ⚠️**결정론적 반올림**(stochastic 아님)이라 **2289스텝 결과를 더 긴 예산에 외삽 금지** — 논문은 1T 토큰이다. ⚠️**S5: 254MB 로 `--no-ckpt` 는 안 열린다**(1.7~2.7GB 필요) | [P022B](P022B_FP8선택적혼합정밀도-DeepSeekV3식.md) §4 단계2 |
| **P052** | **시드 반복 — 체크포인트 판정을 아키텍처 판정으로** | ⏸ **신설·배치 완료** `run_P052_seed_replicate.bat`(**약 7h**, 2런 독립). ★**REVIEW2 선결.** 리뷰가 `mC_g16` 을 승격하려는 근거가 **시드 1개**다. 결과 029 의 SE 0.0005 는 **eval 노이즈**만 없앤 것이고 **학습 노이즈 σ=0.012 는 그대로** 남아 있다 — 두 런 차이의 스케일 **σ√2 ≈ 0.017 이 +0.0161 과 같은 자릿수**다. 즉 지금 아는 것은 *"이 두 체크포인트가 0.0161 다르다"* 뿐이고 **결과 029 §3 이 이미 그렇게 적어 두었다**. `--seed 2024`(결과 012 와 같은 값)로 **두 팔 모두** 재학습해 Δ₂ 를 잰다. ★**[4] 가 대조군** = `mC_wsd` vs `mC_wsd_s2`(같은 아키텍처·시드만) → **이 조건에서의 σ√2 재확인**(결과 012 는 p6d·dense·cosine 에서 쟀다). 게이트: 부호 재현 / \|Δ₂−Δ₁\| < 0.017 / 평균 < 0.024. ★**성공 시나리오가 "아무것도 안 바뀜" 이고 그 지루함이 값어치다** | [P052](P052_시드반복-체크포인트판정을-아키텍처판정으로.md) |
| **P018 [재개]** | **압축 교사 distill — 전제 2개가 틀렸다** | ⏸ **재개·배치 완료** `run_P018_compressed_teacher.bat`(약 4.2h, 5단계). ★★**원문 전제 2건을 우리 측정이 반박한다**: **X1** *"압축 교사면 forward 0.55×"* → 🚫**거짓. 타잉은 컴퓨트를 안 줄인다**(`report()` FLOPs 에 `mlp_group` 없음, 층 수 불변, 실측 −3.15%, 결과 033) → **KD 가속 실험이 아니라 VRAM 실험이다**. **X2** *"압축 교사 ≤ dense 라 학생이 그 이하"* → ⚠️**방향이 의심스럽다**. 결과 021 은 공통 val 에서 mC ^< p6d 라 했고 **현행 KD 교사는 p6d 가 아니라 300M 풀 `dense`** 다(풀 2× = −0.12, 결과 006). ★**그래서 [1/5] 가 `paired_eval --models dense mC_wsd` 로 두 교사 후보를 같은 val 에서 처음 비교한다**(GPU 수 분). 교사 상주 **945 → 419 MiB = 약 −0.51GB**, CLA2 로 KV 소유 20/20→10/20. **P042 §7 안 B 의 리스크 평가를 뒤집을 수 있다.** ⚠️**자기증류**(같은 아키텍처·같은 부모) — 어느 방향으로도 일반화 금지. ⚠️R3 본런에 **`--no-ckpt` 금지**(조건 일치) | [P018](P018_압축교사-distill.md) §R |
| **P042 [단계1·2 완료]** | KD 교사 추론화 | ✅ **단계1 = 결과 034 §9**(속도 **측정 불가**, VRAM alloc **−490.6 MiB** = P2 적중) / ⚠️ **단계2 = 결과 034 §10 조건부 통과**(reserved **15.37 GiB**, 여유 0.62, **−20.5%**, 단 **스필 허용 상태에서만** 완주). **표준 조건 승격 불가.** ★기준표 §5 의 15.7GB **의심 철회**(실측 15.37). ★★**§7 안 B(압축 교사)가 결과 037 로 "기본" 이 됐다**(−1.88 GiB). **단계3(본 런)은 압축 교사 조합이 정리된 뒤에** | [P042](P042_KD교사-추론화-및-nockpt개방.md) |
| **P022B [단계0 완료]** | FP8 선택적 혼합정밀도 | ✅ **G0 전부 통과 = 결과 035.** [D] 최선 Δbpb **+0.0002** = 게이트선의 **1/40**. ★★**결론 전환: 커널 불필요** — sm_89 는 per-tensor 만 받는데(rowwise·blockwise FAIL) **그 per-tensor 가 +0.0005** 다. ★**P2·P3·P4 가 빗나갔고 그게 발견이다**: per-tensor **E4M3 왕복**(+0.0005) vs 결과 028 per-tensor **α 재추정**(+0.0862) = **173배**. **E4M3 는 α 를 보존하고 α 재추정은 α 를 버린다** — *같은 단어가 다른 규약을 가리켰다*. **P6(지금 구현 안 함) 유지** — 품질이 아니라 값어치 문제(메모리 0, −15%, 같은 밤 `--no-ckpt` 가 −20.5%). ⚠️누산 오차·backward 미측정 | [P022B](P022B_FP8선택적혼합정밀도-DeepSeekV3식.md) |
| **P051 [종결]** | `micro_group` 128→256 | ✅ **종결 = 결과 036. 채택.** paired **−0.0007**(t −1.65) = **구분 불가**. packed **−3.62%**, 감축비 **2.15×**, **fp32 상주 소수점까지 불변**. 예측 4/5 적중(P2 오차 0.04%). ⚠️P4(`grad_max` 1.621) 빗나갔으나 같은 밤 `mC_ct` 도 1.580 이라 **귀속 불가**. ★**직교 레버**(볼록성 바깥) — `m100R1q` 위에 얹으면 계산상 약 9.26MB. ⚠️**packed 전용**. `768 % 512 ≠ 0` 이라 **축 종결** | [P051](P051_micro-group-알파그룹-마지막-무료축.md) |
| **P018 [단계1 완료]** | 압축 교사 distill | ✅ **RG0~RG3 전부 통과 = 결과 037.** ★★**[1/5] 이 최대 산출물** — 현행 KD 교사 `dense`(3.8080)가 `mC_wsd`(3.6984)보다 **+0.1096 나쁘다**(분해능 4.6배). ⚠️풀 핸디캡 섞임. 학생 `mC_ct` paired **−0.0220**(동급, 방향은 압축 교사 쪽), **reserved −1.88 GiB**(R-P3 을 3.7배 과소예측 — 할당 churn 감소분을 못 셌다), 속도 무변(**X1 "0.55×" 실측 반박**). ⚠️`--no-ckpt` 는 완주했으나 **스필**(p90/p10 +48.5%, +26.4% 느림). ★**단계2 신설(§R9): 압축 교사 + `--kd-teacher-infer` 조합** — 계산상 reserved ≈ 9.6 GiB, `--no-ckpt` 얹어도 **여유 2.0~2.5 GiB**. 배치 미작성(40분) | [P018](P018_압축교사-distill.md) §R |
