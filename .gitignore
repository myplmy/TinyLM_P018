# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
.env
.venv/

# ── 대용량·재생성 가능 학습 산출물 ─────────────────────
# 토큰 바이너리·체크포인트는 커밋하지 않는다(재생성 가능, 수백 MB~GB).
runs/
**/run100m/ckpt/
**/run100m/data/
**/data_cache/
*.pt
*.bin

# ── Hugging Face 로컬 캐시(모델·데이터셋) ───────────────
HF/

# ── 로그(logs/*.json)는 추적한다 — 실험 기록이므로 무시하지 않음 ──

# ── scratch ─────────────────────────────────────────────
*.tmp
