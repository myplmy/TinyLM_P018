# TinyLM 세션 인수인계 — 아키텍처 v5 / 코드 v6 (참고용 스냅샷)

> **정본은 `tinylm/` 코드**다. 이 메모는 확정 스펙·설계 근거·안정화 이력을 한 곳에 보존한
> 참고용 스냅샷이다. CLAUDE.md는 간결 유지를 위해 상세를 이 문서로 옮겼다.
> 방법론 원장은 [`docs/METHODS.md`](../docs/METHODS.md), 실험 계획은 [`test_plan/`](../test_plan),
> 실험 결과는 [`test_result/`](../test_result) 참조.

## 아키텍처 (v5) — 확정 스펙

```
prelude 2층      어텐션·MLP 전부 독립          (임베딩 직후 조정)
   ↓
중간 16층        어텐션: 층마다 독립 (16세트)
                 MLP:    4층씩 묶어 4그룹만 저장 (mlp_group=4)
                 K/V:    인접 2층이 공유 (CLA2)
                 adaLN:  층별 scale/shift/gate
   ↓
coda 2층         어텐션·MLP 전부 독립          (lm_head 직전 조정)
   ↓
최종 RMSNorm → factorized lm_head (임베딩 전치 공유)
```

**검증용 100M급 프리셋 `m100`** (`d=768 ffn=2048 heads=12/3 vocab=32768 E=256`)

| | tied | dense 기준선 |
|---|---|---|
| 파라미터 | 72.9M | 132.5M |
| 배포 메모리 (1.95bpw) | 17.1 MB | 30.9 MB |
| KV 캐시 | 7.5 KB/token | 15.0 KB/token |
| 토큰당 FLOPs | 0.248 GFLOP | 0.248 GFLOP (동일) |

메모리 감축 **1.82배**. FLOPs가 같은 건 의도된 것 — 타잉은 메모리를 줄이지 연산을 줄이지 않는다.
이 실험이 재는 것은 **그 감축의 대가(손실 격차)가 얼마인가** 하나다.

`tiny` 프리셋(d=256, 1+4+1)은 파이프라인 동작 확인 전용. `m100d`(2+24+2, g6)는 "깊고 얇게+공유"
검토용 프리셋.

## 유지 중인 설계 요소

- g128 삼진 양자화 (llama.cpp Q2_0_g128 / T-MAC 호환), TWN 임계 + L2 alpha, AMP-안전 STE
- 삼진 어닐링(배포 시점 = 학습 종료 시점), 공유 MLP LR 1/√g, weight decay 미보정
- prelude/coda 분리(단일 최대 이득), 어텐션 층별 독립·MLP만 타잉, CLA2, factorized embedding
- adaLN 층별 조건화 (mode routing/mode-delta는 기본 off)
- depth-delta 폐기 (rank_spectrum_v3 측정: excess/비용 효율 전 항목 1.0 미만)

## v5 학습 안정화 (메모리·파라미터·FLOPs 중립)

- **QK-norm** — q·k 내적 전 파라미터 없는 RMSNorm. v4 300M 학습의 NaN 발산 주원인 해결.
- **NaN 가드** — clip_grad_norm_이 non-finite면 그 스텝을 건너뛴다(가중치 오염 방지).
- **torch.compile 안전 어닐** — quant_anneal을 버퍼 + `set_anneal()`로. 재컴파일 제거.
- **기본 LR 조정** — 구 2e-3은 과다. QK-norm 적용 후 1e-3까지 안정(|g|~0.5). CLI 기본 6e-4.

## v6 효율/실험 기능 (코드 v6, `tinylm/` 패키지)

- 모듈화(config/model/data/train/eval/infer/cli), HF 캐시 리다이렉트, 크기별 데이터 캐시.
- 자동 LR 탐색(range test 기본 / grid sweep 고급), EMA·WSD·베스트 체크포인트·조기종료·train-val 모니터.
- 격차 축소 실험 도구: dense 교사 KD, 부모초기화(average-init), 층별 ternary-LoRA, FiLM.
- 스케일별 체크포인트/로그 이름(`{preset}_{data}_{tokens}_{arch 또는 tag}`) — 재측정 오염 방지.
- torch.compile: `default` 권장. `reduce-overhead`(CUDA그래프)는 임베딩 타잉과 충돌해 부적합.

## 현재 상태 / 다음 관문

- dense 300M 기준선 확정(최종 val 3.8241, best 3.7797). tied 기준선 격차 **+0.1178**(undertrained 추정).
- 다음: KD+부모초기화 / ternary-LoRA / FiLM / 둘다 조건으로 격차 축소 측정([test_plan/P002](../test_plan)).
- 판정 기준(손실 격차): `≤+0.07` 성립 / `+0.07~+0.15` prelude·coda↑ 또는 g↓ / `>+0.15` |g|max 먼저.

## 참고 자료 (설계 근거 논문)

Tying the Loop(EPFL, 컴포넌트 ablation·prelude/coda·LR 1/√g), Relaxed Recursive Transformers
(ICLR 2025, layer-wise LoRA·average-init·KD), MobileLLM(ICML 2024, 깊고 얇게+블록 공유),
RMoE, KDA, TurboQuant, SISA, InfiniteHiP, Bonsai(삼진 g128). `article/`에 PDF 보관.
