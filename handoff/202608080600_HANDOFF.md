# 핸드오프 2026-08-08 06:00 — 야간큐 v2 결과 4건 + 도구 사고 2건

> 양식: `ai_dev_tool/02_핸드오프_규약.md`. **이전 메모**: `202608071400_HANDOFF.md`
> ⚠️ **파일명 시계열 주의** — `202608071400` 이 `202608080200` 보다 뒤에 쓰였다.
> **작성 순서 정본은 이 표다**: … → `202608072000` → `202608080200` → `202608071400` → **이 문서**.

---

## 1. 한 문단 요약

야간큐 v2 가 돌아 **레버 3개의 가격이 ⚙추정에서 ★실측으로 바뀌었다.**
**g16(+0.0161)과 prelude/coda 1+1(+0.0493)이 같은 효율선**(0.0167 vs 0.0199 nats/MB packed)에
있고, **E=128 은 판정 불가**(`grad_max 19.32` — 임베딩이 난수로 시작했다).
**P047 은 측정 0건** — `runlog.py` 가 `--note` 와 명령을 함께 받으면 **명령을 조용히 버린다**.
도구 사고 2건(`pause` 무방비 / `--note` 병용)에 **각각 린터 규칙 + 실행 경로 거절**을 넣었다.
**야간큐 v3 는 3.5~4.5h**(P047 재실행 → P014C 단계1 → P048 단계2).

---

## 2. 이번 세션에 확정된 것

| # | 사실 | 근거 |
|---|---|---|
| 1 | **per-row α 는 분해능 미만**(+0.0050~0.0063 bpb) → **커널 불필요 경로가 열렸다** | 결과 028 |
| 2 | **per-tensor 는 0.261 nats(21.7σ)** = bitnet.cpp 규약 → **P014B 중단 권고에 품질 근거** | 결과 028 |
| 3 | **g16 은 분해능 미만**(+0.0161) → **채택 가능. 타잉 축 종결**(g16 이 구조적 상한) | 결과 029 |
| 4 | **prelude/coda 1+1**: packed **−19.0%**, fp32 상주 **−20.4%**, 대가 +0.0493 → **저장소 최대 레버** | 결과 032 |
| 5 | **두 레버가 같은 효율선** — 국소 법칙 **약 0.0037 nats/유니크삼진 100만**(범위 4.7~12.1M) | 결과 032 §4 |
| 6 | **E=128 판정 불가** — `grad_max` 19.32, step0 `ce` = ln(32768) = 난수 임베딩 | 결과 030 |
| 7 | **fp32 상주 ≠ int8 상주** — E=128 은 int8 에서만 1위. **레버 순위가 경로에 따라 뒤집힌다** | 결과 032 §4.3 |
| 8 | `mem_breakdown()` 식이 **4개 런에서 소수점까지 재현** → ⚙계산을 신뢰할 수 있다 | 029·030·032 |

---

## 3. 도구 사고 2건 — 둘 다 "조용한 실패"

| | 사고 | 조치 |
|---|---|---|
| **A** | **무방비 `pause`** — `run_P014C` 만 가드가 없어 야간큐가 [1] 후 **밤새 멈췄다**(3개 중 1개) | `lint_bat.py` **9b**(에러) + 저장소 전체 **18건 가드 추가** |
| **B** | **`--note` + 명령 병용** — `runlog.py` 가 명령을 버리고 **종료코드 0**. P047 두 단계가 "성공" 인데 로그 **84바이트** | ①`runlog.py` **거절**(exit 2, 회귀검증 통과) ②`lint_bat` **9c**(에러) ③`lint_bat` **9d** + runlog 경고 = **`--name` 에 P번호 필수** |

> **★기존 게이트 4개가 B 를 전부 통과했다**(lint·check_batch_flags·check_call_kwargs·`errorlevel`).
> **함정 10 의 다른 얼굴**(*"파서에 있는데 전달이 안 된다"*)이고, 2026-08-06 `--doc-filter` 와 동형.
> ⚠️ **이번이 더 나쁘다** — 그때는 죽어서 알았고 이번은 **성공으로 끝났다.**
> `runlog._verify()` 도 **0바이트만** 경고하게 만들어 84바이트를 통과시켰다.
>
> **교훈: 가드를 "작성 시점"(린터)과 "실행 경로"(도구) 양쪽에 둔다.**
> 린터는 사람이 돌려야 하고, 도구는 항상 돈다.

---

## 4. 코드·문서 변경

**코드**
- `config.py` — 프리셋 **`m100R1p`**(1+16g8+1) · **`m100R1q`**(1+16g16+1) 신설, `PRESET_PARENT` 등록
- `train/init_utils.py` — **구조 불일치 경고**(`zip` 조용한 절단 차단) + **`_svd_emb_init()`**
  (Eckart–Young 최적 랭크근사로 임베딩 이식. numpy 검산: 상대오차가 하한과 소수점까지 일치)
- `scripts/runlog.py` — `--note`+명령 **거절** / `--name` P번호 경고
- `scripts/lint_bat.py` — 규칙 **9b·9c·9d** 신설(REM 주석 제외, 회귀검증 통과)
- `scripts/diag_alpha_group.py` — `detach()` + `g512` fallback(`*N` 표기)
- `scripts/diag_spam_rate.py` **신설** — P047 도구. `spam_signature()` 를 **호출**
- `scripts/check_fused_int8.py` **신설** — P014C 단계1. **속도는 일부러 안 잰다**

**결과문서** 028·029·030·031·032 + `실험목록.md` 5행
**계획서** P014C(단계0 결과+단계1B) · **P047 신설** · **P048 신설**(단계1 결과+단계2) · P046(판정불가+SVD 단계2) + `실험계획목록.md`
**기준표** `EXPERIMENT_BASELINES.md` **§2.2c 신설**(메모리 레버 계열 + 효율표) + 태그 5행
**CLAUDE.md** 함정 **21~24** 추가 / 프리셋·스크립트·배치표 갱신 / `grad_max` 사례
**병목 분석** **§A.0.1 신설** — ⚙→★ 전환, §A.3 우선순위 오류 자기정정

**배치** `run_night_queue_v3.bat` **신설(v3)** · `run_P047_stage0_spam_rate.bat`(수정) · `run_P048_prelude_coda.bat` · **`run_P048_stage2_stack_g16.bat`** 신설
전 게이트 통과: `py_compile` OK / `check_attrs` 0 / `check_call_kwargs` 0 / `lint_bat` 0 / `check_batch_flags` 0

---

## 5. ★다음 권장 순서

| 순위 | 실험 | 배치 | 시간 | 근거 |
|---|---|---|---|---|
| **1** | **야간큐 v3** | **`run_night_queue_v3.bat`** | **3.5~4.5h** | 아래 3건을 묶은 것 |
| 1-1 | P047 단계0 | (큐 안) | 20~60분 **GPU 0** | **레버 1위.** `ko-en` 스팸률 미측정. **REVIEW2 앞** |
| 1-2 | P014C 단계1 | (큐 안) | 1분 GPU 0 | 결과 028 이 열었다. **초당 정보량 최대** |
| 1-3 | P048 단계2 | (큐 안) | 3.0h | **예측 +0.062 검정** → 레버에 가격을 매길 수 있는가 |
| **2** | **P040** | `run_P040_tying_trainspeed.bat` | 30~40분 | ⚠️**단독·직전 5분 유휴.** 야간큐에 넣지 말 것 |
| 3 | **REVIEW2** | — | 문서 | 위가 끝나면. **단 D-1(P047)이 먼저** |
| 4 | P046 재측정 | 미작성 | 3.5h | **int8 배포를 확정한 뒤** |

---

## 6. 열린 질문

- **P047 이 양성이면?** 전 기준선이 오염된 데이터 위에 있다 → **리뷰 수정이 아니라 기준선 재작성**
- **P014C 단계1 이 False 면?** 결과 028 의 표가 자체 커널의 정량 근거가 된다(안 C)
- **P048 깊이 교락** — 분리에는 새 dense 부모(+3.5h). **단계2 결과를 보고 결정**
- **`grad_max` 1.253**(P048)이 FiLM 1.855 와 기준선 0.847 사이 → 구분선이 *"장치냐"* 가 아니라
  *"층별 자유도를 건드리냐"* 라는 가설. **미검증**
- **`micro_group` 은 한 번도 스윕 안 했다** — g256 이 분해능 1/5. 단 **재학습 필요**(무료 아님)

---

## 6.5 ★스모크 로그 폴더 사고 (2026-08-07 사용자 보고)

`run_smoke_check.bat` 을 돌렸는데 로그가 **`test_result/` 로 갔다.**

| | |
|---|---|
| **원인** | `tool_smoke.bat` 이 `--outdir` 을 **13곳 전부에서 한 번도 안 줬다.** `run_smoke_check.bat` 헤더는 *"Logs land in smoketest_logs, deliberately NOT in test_result"* 라고 **적어만 뒀고 구현이 없었다** |
| **성격** | **함정 13** — *"문서의 조치 기록은 코드 변경의 증거가 아니다."* 여기서는 문서가 **애초에 거짓**이었다 |
| **왜 안 잡혔나** | 어떤 게이트도 "로그가 옳은 폴더에 갔는가" 를 안 본다. `_verify()` 는 **크기만** 본다 |

**조치 — 규약에서 강제로 (13곳을 손으로 고치지 않는다, 함정 18)**

1. **`runlog.py` 가 `TL_OUTDIR` 환경변수를 읽는다** → 진입 배치에서 **한 번** 설정하면 그 아래 전부에 걸린다
2. **`test_result/` 는 실험 로그 전용 — `--name` 이 `P0NN` 으로 시작하지 않으면 거절**(exit 2).
   즉 스모크가 `TL_OUTDIR` 을 잊으면 **조용히 잘못된 폴더에 쓰는 대신 큰 소리로 실패**한다
3. **비실험 로그 파일명 = `{YYYYMMDDHHMM}_{name}_{sha7}.txt}`** (분 정밀도 + 커밋 SHA7)
4. **모든 로그 최상단에 `[commit] 5e063b1  +dirty 14개(커밋 안 된 변경)`**
5. `tool_smoke.bat` 자체에도 `if not defined TL_OUTDIR set TL_OUTDIR=smoketest_logs` — 모듈이
   **누가 부르든** 옳게 동작하게

> ★**dirty 개수를 함께 적는 이유**: 스모크는 **코드를 고친 직후** 돌린다 — 작업트리가 거의 항상
> 더럽다. SHA 만 적으면 *"이 커밋에서 검증했다"* 로 **잘못 읽힌다.** 정확한 진술은
> *"이 커밋 + 미커밋 변경 N개"* 다.

⚠️ **분 정밀도의 대가**: 그대로 쓰면 한 배치의 13번 호출이 13개 파일이 된다.
→ **같은 이름의 최근 파일이 `--stamp-reuse-min`(기본 30) 분 안에 수정됐으면 이어쓴다.**
완전 결정적으로 하려면 `TL_STAMP=202608071017`. **회귀검증 3케이스 전부 통과.**

---

## 7. 정리 필요 (사용자 수행)

```
del run_zz_regress_tmp.bat
del smoketest_logs\202608071025_zztest_5e063b1.txt
del test_result\log_20260807_P099_zztest.txt
del smoketest_logs\log_20260807_mC_g16.txt
```
⚠️ 위 4개는 **제가 회귀검증용으로 만든 임시 파일**입니다. 샌드박스에서 `unlink` 가 막혀
못 지웠습니다. **특히 `test_result/log_20260807_P099_zztest.txt` 는 실험 로그 폴더를
오염시키므로 꼭 지워 주세요** — 방금 세운 규약을 제 테스트가 어긴 것입니다.

**`-done` 배치 삭제 판정** — 삭제 규칙 ①실험 완료 ②재현 명령이 결과문서에 보존 ③재실행 예정 없음:

| 파일 | 판정 |
|---|---|
| `run_P014C_stage0_alpha_group-done.bat` | ✅ **삭제 가능** — 결과 028 §10 에 재현 명령 보존 |
| `run_P037_stage4_exhaust-done.bat` | ✅ **삭제 가능** — 결과 023 §10 |
| `run_P044_film_recheck-done.bat` | ✅ **삭제 가능** — 결과 027 |
| `run_P045_g16_ceiling.bat` | ⏸ **`-done` 만 붙이고 보류** — 결과 029 §5 에 재현 명령 있으나 **REVIEW2 에서 재확인 가능성** |
| `run_P046_emb_rank128.bat` | 🚫 **삭제 금지** — **판정 불가라 재실행 예정**(SVD 이식 후) |
| `run_P048_prelude_coda.bat` | ⏸ **`-done`** — 단계2 가 이걸 참조한다 |

---

## 8. 커밋 메시지

```
feat: 메모리 레버 3종 실측 + runlog/pause 조용한 실패 2건 차단

야간큐 v2 결과 4건을 결과문서 028~032 로 기록하고, 그 과정에서 드러난
도구 사고 2건에 린터 규칙과 실행 경로 거절을 넣었다.

측정 (기준표 §2.2c 신설)
- P045 g16: paired +0.0161 (분해능 0.024 미만) -> 채택 가능.
  n_middle=16 이라 g16 이 구조적 상한 = 타잉 강도 축 종결. 결과 029
- P048 prelude/coda 1+1: paired +0.0493, packed -19.0%, fp32 상주 -20.4%
  = 저장소 최대 레버. 프리셋 m100R1p 신설. 결과 032
- P046 E=128: 판정 불가. grad_max 19.32 (기준선 0.847) + step0 ce =
  ln(32768) = 임베딩이 난수로 시작. 변수가 2개였다. 결과 030
- P014C 단계0: per-row alpha 는 +0.0050~0.0063 bpb (분해능 미만) ->
  커널 불필요 경로가 열렸다. per-tensor 는 0.261 nats (21.7 sigma) ->
  bitnet.cpp 에 그대로 넣으면 품질을 그만큼 잃는다. 결과 028

발견
- g16 과 prelude/coda 가 같은 효율선: 0.0167 vs 0.0199 nats/MB(packed),
  세 지표 전부 1.19~1.20배. 국소 법칙 약 0.0037 nats/유니크삼진 100만
  (적용범위 4.7~12.1M, KD+부모초기화 파이프라인 안). P048 단계2 가 검정
- fp32 상주와 int8 상주에서 레버 순위가 뒤집힌다. E=128 은 int8 에서만
  1위(-18.8%)이고 fp32 는 -3.6%. 야간큐 순서를 확정하지 않은 경로의
  숫자로 정한 것이 오류였다 (CLAUDE.md 함정 24)

도구 사고와 차단
- 무방비 pause 로 야간큐가 밤새 멈췄다 (3개 중 1개만 가드 누락).
  lint_bat 규칙 9b 신설(에러) + 저장소 전체 18건 가드 추가
- runlog.py 에 --note 와 명령을 함께 주면 명령을 조용히 버리고 종료코드
  0 을 돌려준다. P047 두 단계가 "성공" 인데 로그가 84바이트였다.
  runlog 가 거절하도록 고치고 lint 규칙 9c 신설. 기존 게이트 4개가
  전부 통과했던 결함이다 (함정 10 의 다른 얼굴)
- 로그 파일명에 계획번호 누락 -> lint 9d + runlog 경고. 규약 P{번호}_{태그}
- run_smoke_check 로그가 test_result 로 갔다. tool_smoke.bat 이 --outdir 을
  13곳 전부에서 안 줬고, 배치 헤더는 smoketest_logs 로 간다고 적어만 뒀다
  (함정 13: 문서가 코드 변경의 증거가 아니다). 13곳을 고치는 대신
  runlog.py 가 TL_OUTDIR 을 읽게 하고, test_result 는 --name 이 P0NN 으로
  시작하지 않으면 거절하게 했다. 비실험 로그는
  {YYYYMMDDHHMM}_{name}_{sha7}.txt 이고 모든 로그 최상단에 커밋 배너를 쓴다.
  dirty 개수를 함께 적는다 - 스모크는 코드를 고친 직후 돌리므로 SHA 만으로는
  '이 커밋에서 검증했다' 가 거짓이 된다

구현
- config.py: 프리셋 m100R1p / m100R1q 신설, PRESET_PARENT 등록
- init_utils.py: 구조 불일치 경고(zip 의 조용한 절단 차단) +
  _svd_emb_init() = Eckart-Young 최적 랭크근사 임베딩 이식.
  numpy 검산으로 상대오차가 하한과 소수점까지 일치함을 확인
- scripts/diag_spam_rate.py, scripts/check_fused_int8.py 신설
- diag_alpha_group.py: detach + g512 fallback (1차 실행에서 g512 행이
  전부 비었다. 판정에는 영향 없었으나 재려던 양을 못 쟀다)

야간큐 v3: P047 재실행 -> P014C 단계1 -> P048 단계2 (3.5~4.5h).
P040 은 속도 측정이므로 큐에 넣지 않는다 (단독 + 직전 5분 유휴).
```
